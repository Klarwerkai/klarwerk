# OFFEN — das Gedächtnis des Projekts

**Stand: 30.07.2026, abends · Basis `d0b9e5e` (Ship 9, 30.07. ~17:45) · gepflegt vom Kopf bei jeder Zustandsänderung**

**Ship 9 ist gelaufen — `d0b9e5e`, mega61 bis mega66 zusammen, 114 Dateien.** Coolify Success in 48 Sekunden. **Im Arbeitsbaum liegen seitdem unausgeliefert: mega67** (ben sammel65: **ROT**, drei Auflagen), **mega68** (gebaut, aber der Lauf endete vor dem Ende des Prüftors — keine Zahlen, kein Diff) **und mega69** (Klara, im Eingang). **Regel von Pedi (30.07.): nicht nach jedem grünen Verdikt wird ausgeliefert** — abgenommene Scheiben werden zu EINEM Ship gebündelt; sofort nur, wenn es zum Weiterkommen nötig ist, und dann mit Begründung.

**Ship-Kette:** `93c6bd9` → `4cdde0e` (Ship 3) → `3805c49` (Ship 4) → `84060d2` (Ship 5) → `1fc55aa` (Ship 6) → `46aac36` (Ship 7) → `bcf1f15` (Ship 8, 30.07. 08:10, mega59+60) → **`d0b9e5e`** (Ship 9, 30.07. ~17:45, mega61–66).

Diese Datei liegt im Repository, nicht in einem Chat-Gedächtnis und nicht in einem Ordner, den sich jemand ausgedacht hat. Sie wandert mit jedem Klon mit, jeder neue Kopf sieht sie, und sie überlebt jeden Fensterwechsel.

## Die eine Regel

**Was hier nicht steht, existiert nicht.** Kein Auftrag, keine Idee, kein Wunsch, keine offene Entscheidung. Nicht als Drohung — als Tatsache: Was hier nicht steht, wird vergessen. Der Beleg dafür ist die Sichtung vom 28.07., die zwei fertig geschriebene Aufträge, vier unbeantwortete Vorschläge und einen ganzen Epos wiedergefunden hat, die alle irgendwo lagen, nur nicht an einer Stelle, die jemand liest.

## Wie eine Zeile gelesen wird

**Zustand** — `ENTSCHEIDUNG` liegt bei Pedi und blockiert etwas · `OFFEN` ist entschieden und wartet auf einen Auftrag · `BEAUFTRAGT` läuft gerade · `ERLEDIGT` · `VERWORFEN` mit Begründung.

**Anker** — wann es drankommt. **Niemals ein Datum.** Datums-Anker sind der Grund, warum fünf Vorgänge und zwei Konzepte auf „nach VIP-2" warten, seit es VIP-2 nicht mehr gibt. Erlaubt sind Ereignisse, die wir selbst herbeiführen: `SOFORT`, `VOR-VORTEST`, `NACH-VORTEST`, `MIT-SHIP3`, `NACH-SHIP3`, `MIT-SHIP4`, `MIT-SHIP6`, `MIT-SHIP7`, `MIT-ZUGRIFFSSCHUTZ`, `MIT-DESIGN`, `MIT-JAHRESRING`, `MIT-GROSSKUNDE`, `MIT-ADOPTION`.

**Warum** — die fünfte Spalte. Sie beantwortet „was passiert, wenn wir es nicht tun". Die Leseseite macht daraus das Aufklappen unter jedem Punkt. Steht sie leer, gibt es kein Aufklappen, und die Seite nennt am Fuß die Kennungen, denen sie noch fehlt — eine Lücke wird gezeigt, nicht versteckt.

**Kennung** — bleibt stabil, auch über Aufträge und Ship-Runden hinweg.

---

## 1 — Was bei dir liegt

**Ganz oben, weil es Fragen sind und keine Berichte.** Alles hier wartet auf eine Entscheidung oder einen Handgriff von Pedi; niemand sonst kann es tun. Sortiert nach Frist, nicht nach Alter.

### 1a — Vor morgen 14:00

| Kennung | Zustand | Anker | Sache | Warum |
|---|---|---|---|---|
| S2 | ENTSCHEIDUNG | VOR MORGEN 10:00 | **Bedingtes GO für mega70: ja oder nein.** ben hat GELB gegeben — **die Produktkorrekturen sind grün**, alle fünf Rollen-Sackgassen sachgerecht geschlossen, die gemeinsame Tor-Karte hat den älteren Stufe-2-Fall nicht verändert, keine sechste Sackgasse in der Vollerhebung der zwölf direkten Vorkommen. **Beide gelben Punkte liegen im Wächter, nicht im Produkt** — sie können an dem, was Natascha morgen sieht, nichts ändern. **Kopf-Empfehlung: bedingtes GO**, mit der Auflage mega72 nach dem Vortest; der Auftrag liegt bereits fertig in `_relay/hand/queue/` und trägt oben den Satz, dass er ausdrücklich nicht vorher gefahren wird. **Das GO gilt nur für diese Frage** — ob überhaupt ausgeliefert wird, hängt weiter an mega71 und an bens nächstem Verdikt | Ein Wächter mit einer Lücke bricht nichts; er schützt nur weniger, als er zusagt. Das ist ein Grund, ihn zu reparieren, und keiner, eine Auslieferung zu blockieren, die vier echte Sackgassen von Nataschas Weg räumt. Umgekehrt wäre es falsch, das Gelb einfach zu schlucken: deshalb steht die Auflage geschrieben und nicht nur gesagt. |
| A26 | ERLEDIGT | — | **Natascha hat nur einen kostenlosen Cloud-Zugang — das Interview wird deshalb schriftlich geführt, und sie bekommt zwei Texte zum Einfügen: `_relay/generalprobe/natascha-claude.html`** (mit Kopierknöpfen, Doppelklick genügt) plus die Quelle als Markdown. **Dazu auf Pedis Wunsch zwei weitere Wege:** `KLARWERK-Natascha-Claude.pdf` im Corporate Design (fünf Seiten, beide Texte vollständig in der Textebene — jede Zeile über 25 Zeichen gegengeprüft, nicht angenommen) und `EMAIL-an-Natascha.md`, eine fertige Mail **in meinem Namen** mit beiden Blöcken. **Verschickt habe ich nichts** — ich habe keinen Mailzugang und würde in Pedis Namen auch nichts absenden. Die Mail nennt offen, dass sie von der planenden KI des Teams stammt; das passt zu dem, was Natascha morgen testet. **Text 1 ist ein Begleiter für den Test selbst**, und sein wichtigster Teil ist ein Verbot: er kennt KLARWERK **nicht** und darf keinen Knopf, kein Menü und keinen Weg raten — auch nicht vorsichtig. Sonst misst der Vortest ihn statt das Produkt, und eine erfundene Klickanleitung schickte sie in die Irre. Was er stattdessen tut: die Stelle in **einem** Satz festhalten, ihr sagen, dass genau das ein Ergebnis ist und nicht ihr Fehler, höchstens einen allgemeinen Denkanstoß geben — und nach zwei Minuten zum Weiterziehen raten, wie es die Aufgabenkarte ohnehin sagt. Auf „Sammlung bitte" liefert er alle Hängestellen in ihren Worten. **Text 2 führt das Interview**, abgeleitet aus `INTERVIEW-PROMPT.md`, aber für **schriftlich** umgebaut: kürzere Nachrichten, eine Frage pro Nachricht, Wiedereinstieg nach Unterbrechung (ein kostenloser Zugang hat Grenzen). Die beiden Sperren der Lehrlings-Frage sind wortgleich übernommen, und **das Wort kommt in Text 1 an keiner Stelle vor** — nachgemessen, nicht angenommen | Der Zwang war eine Einschränkung und ist die bessere Lösung geworden: ein Begleiter, der nichts erklären darf, verwandelt jede Hängestelle in einen protokollierten Befund, statt sie zu überspielen. Was sonst als „teils" im Protokollbogen gestanden hätte, kommt jetzt mit ihren eigenen Worten zurück. |
| A25 | ERLEDIGT | — | **Von Pedi zugesagt und beantwortet (30.07. abends).** Die einzelnen Zeilen bleiben so lange auf LIVE-PRÜFUNG, bis er sagt, was er gesehen hat — fällt ihm dabei etwas auf, wird daraus ein eigener Punkt. Ursprünglich: **Die vier Blicke auf das laufende System — drei Minuten, und nur du kannst sie führen.** Ship 9 ist live (`d0b9e5e`), aber alles, was heute gebaut wurde, ist bisher **am Quelltext** belegt und nicht am Bildschirm. **Erstens:** `/impressum` und `/datenschutz` ohne Anmeldung aufrufen — erscheinen sie, steht der Entwurfsvermerk oben, sind die Fußlinks auch auf der Anmeldemaske? **Zweitens:** nach dem Anmelden der Hinweisbanner unten — steht er im Layoutfluss und verdeckt er kein Bedienelement? **Drittens:** ein Objekt in der Bibliothek öffnen — steht dort jetzt ein **Autorenname** statt „Unbekannte Person“? Das ist der Punkt, der zwei Anläufe gebraucht hat. **Viertens:** einmal „Lieferanten“ suchen — beide Objekte müssen kommen (Aufgabe 4). Solange diese Blicke fehlen, stehen sieben Registerzeilen auf LIVE-PRÜFUNG statt auf erledigt | Der Kopf kann jede Zeile Code belegen und keine einzige Fläche sehen. Acht Scheiben sind heute ausgeliefert worden, ohne dass ein Mensch das Ergebnis angesehen hat — und morgen um vierzehn Uhr sieht es zum ersten Mal jemand, der nichts davon weiß. Die drei Minuten sind der billigste Test des ganzen Projekts. |
| A12 | ERLEDIGT | — | **Übergabe ist erfolgt (Pedi, 30.07. abends): Natascha hat Zugang zum Konto.** Ursprünglich: **Das Konto existiert bereits:** „Natascha Fouquet", `text.gestaltung@web.de`, Rolle **Experte** — am 30.07. abends in der Nutzerliste belegt, also genau die Rolle aus A7. Offen ist nur noch die **Übergabe des Zugangs** an sie | Kommt sie am Freitag um 14:00 nicht herein, ist der Termin vorbei, bevor er beginnt. |
| A8 | ERLEDIGT | — | **Entschieden von Pedi am 30.07. nachts: „wir werden es auf jeden Fall versuchen oder nachholen."** Aufgabe 6 fällt also nicht aus, sie wird nur **hinter** die sechs anderen gestellt — scheitert die Einrichtung, kostet sie dann nichts von der Messung. **Reihenfolge des Versuchs, vom Billigsten zum Teuersten:** erstens Word im Browser oder der Hochlade-Dialog („Einfügen → Add-Ins → Mein Add-In hochladen"), zwei Minuten, keine Installation · zweitens der `wef`-Ordner mit Cache-Leerung und vollem Word-Neustart, nur am Mac, fünf Minuten · drittens gar nicht, dann Nachholtermin. **Vorbereitet:** `_relay/generalprobe/KLARA-Kurzanleitung-Natascha.html` — eine druckbare Seite, Hochladeweg zuerst, mit der ausdrücklichen Erlaubnis zum Abbrechen ganz oben und dem Hinweis, dass Klara auf dem **Start**-Tab sitzt und nicht unter Einfügen → Meine Add-Ins. **Offen und von Pedi zu beantworten: läuft Nataschas Word auf einem Mac?** Die vorhandene Anleitung ist Mac-only; auf Windows gibt es den `wef`-Weg nicht, dann bleibt nur der Hochlade-Dialog. **Vor dem Termin kein Office-Update** — ein Update eine Stunde vor einer Messung ist der Griff, der den Termin frisst | Er hat recht, und meine frühere Empfehlung war zu vorsichtig: Klara ist der kürzeste Hebel des Produkts, und ein Versuch, der ans Ende gestellt wird, kann nichts mehr kaputtmachen. Was ihn teuer machte, war der Platz vor dem Test, nicht der Versuch selbst. |
| A9 | OFFEN | VOR MORGEN 14:00 | Testordner mit `Arbeitsanweisung Hallenkran.docx` auf ihrem Schreibtisch | Die Import-Aufgabe braucht eine Datei, die sie selbst findet. Sucht sie erst im Dateisystem, messen wir den Finder statt Klarwerk. |
| A10 | OFFEN | VOR-VORTEST | Erstnutzerlauf auf ihrem Konto wiederholen — der bisherige lief als Admin | Als Admin sieht man Flächen, Knöpfe und Zahlen, die einem Experten fehlen. Alles, was wir bisher über die ersten zehn Minuten wissen, kann für ihre Rolle falsch sein. |
| I21 | ENTSCHEIDUNG | VOR MORGEN 14:00 | **`KLARWERK_DEMO_SEED` in Coolify: der Kopf empfiehlt, ihn NICHT zu setzen.** Ohne den Schalter gibt es den Knopf „Demodaten laden" nicht; **Entfernen und der lesende Stand funktionieren ohne ihn** — das hat die Hand ausdrücklich so gebaut. Für den Vortest braucht es ihn nicht: der Bestand ist am 30.07. um 10:06 live geprüft (A2), beide Lieferanten-Objekte stehen, der Papierkorb ist leer (E9), und Ship 9 liefert Code aus, keine Daten. Wer ihn „für alle Fälle" setzt, hebt genau die Sperre auf, die mega64 A gebaut hat. Wird er einmal gebraucht, wird er für zehn Minuten gesetzt und danach entfernt | Es ist der erste Schalter des Projekts mit Vorgabe AUS aus einem Sicherheitsgrund, und die erste Gelegenheit, ihn aus Bequemlichkeit zu untergraben, ist der Tag nach dem Einbau. |

### 1b — Diese Woche

| Kennung | Zustand | Anker | Sache | Warum |
|---|---|---|---|---|
| F30 | GEPRÜFT | NACH-VORTEST | **Gebaut in mega67 G — und mein Auftrag nannte eine Quelle, die es so nicht gibt.** `/api/ai-status` gibt je Aufgabe die **Nutzbarkeit** (auch ein kostenloses lokales Modell erfüllt sie), nicht die Betriebsart; `mode` ist hausweit. Die Hand hat **nicht auf der Annahme gebaut**, sondern ein Feld `billable` je Aufgabe ergänzt, gleiche Bauform wie `tasks`, ohne Providernamen — und es offengelegt als die eine Stelle, an der sie über den Auftragstext hinausging. **Der KI-Satz nach Artikel 50 bleibt unbedingt**, nur der Kostenhinweis ist bedingt; die Trennung ist an ben zur Prüfung gegeben. Ursprünglich: Pedis Befund vom 30.07. | **Pedis Befund vom 30.07.: der Kostenhinweis darf nur erscheinen, wenn tatsächlich eine öffentliche KI aktiv ist.** Heute steht „Ein Klick startet sofort eine echte, kostenpflichtige KI-Anfrage" an sieben Auslösestellen — **unabhängig davon, welcher Weg die Aufgabe wirklich bedient.** Läuft die Aufgabe über den deterministischen Ersatzweg oder ein lokales Modell, kostet der Klick nichts, und der Satz ist dann **eine falsche Tatsachenaussage in der Oberfläche** — dieselbe Klasse wie die 24-Stunden-Zusage, die mega65 entfernt hat. Die Auskunft dafür existiert bereits: `/api/ai-status` meldet je Aufgabe Zustand und Betriebsart. **Zuschnitt für den Auftrag:** der Hinweis wird an die Betriebsart der jeweiligen Aufgabe gebunden; **der KI-Satz nach Artikel 50 bleibt davon unberührt**, denn die Kennzeichnungspflicht hängt am KI-Erzeugnis und nicht am Preis. **Nicht vor dem Vortest** — morgen ist die Cloud aktiv (A21), der Satz stimmt also, und die Karte fängt die Nebenwirkung ab | Es ist der vierte Fund derselben Art an einem Tag, und diesmal kam er von Pedi: ein Text, der mehr behauptet, als der Zustand hergibt. Dass ausgerechnet eine **Ehrlichkeits**maßnahme unehrlich werden kann, wenn sie unbedingt gilt, ist die Lehre — eine Zusage muss an die Bedingung gebunden sein, unter der sie wahr ist. |
| E6 | OFFEN | VOR-VORTEST | Confluence-Testseiten anlegen — verschachtelt, in Untergruppen | Der heutige flache Aufbau testet den Import nicht. Verschachtelung ist genau der Fall, an dem Importe in der Praxis scheitern. |
| D4 | OFFEN | SOFORT | Artefakt-Prompt neu formulieren (die letzte Fassung war zu schwach) — heute Abend. **Auflage aus L1: je Los eine Begleittabelle** | Aus diesem Prompt entsteht der komplette neue Testbestand. Ist er schwach, erzeugen wir 12.000 Objekte, die wir danach wieder wegwerfen — der Fehler, der uns den heutigen Bestand eingebracht hat. |
| D5 | OFFEN | SOFORT | Klara-Zweitmeinung einholen — Prompt liegt fertig | Eine zweite, unabhängige Sicht auf den Erzeugungsprompt kostet fast nichts und fängt genau die Denkfehler, die ein einzelner Kopf nicht sieht. |
| I16 | OFFEN | SOFORT | **Teilweise erledigt: `tmp-probe.test.tsx` ist in mega63 über den vorgesehenen Weg nach `_relay/_to_delete/` verschoben** — ausdrücklich nicht aus der Scheibe heraus, sonst wäre sie mit Ship 9 gefahren. Dort liegen jetzt zwei Wegwerfdateien. Weiterhin zu löschen: `_relay/kopf/_to_delete/` und `_relay/_to_delete/` — fünf Wegwerf-Kopien der Leseseite und drei `.DS_Store`. Dazu der leere Ordner `_relay/kopf/wartet/`. **Kann nur Pedi**, der Kopf darf auf seiner Maschine nichts löschen | Die Kopien entstanden, weil die Dateiübertragung zwischengespeicherte Fassungen auslieferte und nur eindeutige Namen das umgingen. Sie sind Werkzeugschutt, kein Inhalt. |

### 1c — Entscheidungen nach dem Vortest

| Kennung | Zustand | Anker | Sache | Warum |
|---|---|---|---|---|
| C2 | ERLEDIGT | — | **Entschieden von Pedi am 30.07. abends: die Zugangsdaten bleiben in der UMGEBUNGSVARIABLE.** Damit nimmt **kein Feld der Oberfläche je ein Geheimnis entgegen** — der Bereich zeigt nur den Zustand, nennt den Variablennamen und sagt, dass sie auf dem Server gesetzt wird. Die bestehende Härtung bleibt: eigener Namensraum, Origin-Pinning, `redirect: "error"` auf jedem Hop, Redaction in jeder Fehlermeldung. **Zwei Folgen, die die Arbeit kleiner machen:** die Variante kostet **null Bauzeit**, und weil es nichts zu verwalten gibt, bekommt der Admin-Bereich **keine** neue Fläche — Entwurf C3 löst sich damit auf. Verworfen: der Schlüsselbund (trägt nur dort, wo die Desktop-App läuft, nicht bei einer Server-Installation) und die verschlüsselte Ablage (echter Bauaufwand mit Schlüsselverwaltung und Rotation — und ein Token in der Datenbank liegt danach in jeder `pg_dump`-Datei) | Es ist die Entscheidung, die der Entwurf seit der mega31-Zeit blockiert hat, und sie ist in einer Minute gefallen, weil die Frage sauber gestellt war: drei Wege, jeder mit seinem Preis. Die unbequemste war die sicherste **und** die billigste — das kommt selten zusammen. |
| C4 | ENTSCHEIDUNG | NACH-VORTEST | **Soll eine nicht restringierte Confluence-Seite als „intern" gelten?** Von Code in mega59 Block F messbar gemacht und ausdrücklich **nicht** gebaut, weil es eine Produktentscheidung ist: `confluenceGovernanceConfidentiality` liefert für nicht restringierte Seiten `undefined`, und der Stapelvertrag liest das vorsichtshalber als vertraulich. Folge: bei **jedem** echten Confluence-Import ist jeder Kandidat vertraulich, die KI-Gruppierung läuft dort **nie**, und die neue Vorwarnung aus Block F steht zwar zu Recht, aber immer. Der Fix wäre eine Zeile in `services/confluence/src/mapper.ts:31-35` — die Frage ist nicht technisch, sondern ob wir das Fehlen einer Beschränkung als Freigabe deuten dürfen | Solange das offen ist, hat der Import eine Funktion, die dem Kunden angeboten wird und in seiner Umgebung nie läuft. Das ist keine falsche Anzeige mehr — die ist geschlossen —, sondern eine Funktion ohne Wirkung. Und die Deutung „keine Beschränkung heißt intern" ist eine Vertraulichkeitsentscheidung, die kein Auftrag nebenbei treffen darf. |
| G13 | PRÜFEN | NACH-VORTEST | **Drei Konten tragen Administrator, und bei zweien ist zu fragen, ob sie es brauchen.** Aus der Nutzerliste vom 30.07.: `peter@kohnert.pro` (Administrator, richtig), `lars.kohnert@t-online.de` (Administrator — das Lars-Paket ist nach A14 gegenstandslos) und **`codex@codex.com` (Administrator)** — das ist bens Konto. **ben ist vertraglich read-only** und weist das in jedem Bericht mit einem No-Op-Nachweis nach; sein Konto kann aber alles. Das ist genau die Unterscheidung, die dieses Projekt den ganzen 30.07. verteidigt hat: **eine Zusage ist keine Sperre.** Kein akuter Befund — es gibt keinen Hinweis auf Nutzung, und die Rolle ist gewollt vergeben worden. Aber die Frage gehört gestellt, solange sie klein ist: braucht der Gegenprüfer Adminrechte, oder genügt Experte plus Leserecht? Und braucht das Lars-Konto seine Rolle noch? **Nicht vor dem Vortest anfassen** — eine Rollenänderung am Vorabend ist genau die Sorte Griff, die morgen um vierzehn Uhr niemand erklären will | Der Zugriffsschutz ist die Fläche, an der ein Industriekunde zuerst hinsieht, und drei Administratoren in einer Instanz mit sieben Konten sind erklärungsbedürftig — nicht falsch, aber erklärungsbedürftig. Beim ersten Kundenaudit ist „der benutzt es ja nicht" keine Antwort. |
| I26 | ERLEDIGT | — | **Die Leseseite hat wochenlang etwas anderes gezeigt als das Register — genau der Fehler, gegen den sie gebaut ist.** Beim Neuordnen am 30.07. abends gefunden: `offen-seite.py` kannte nur fünf Zustände (`ENTSCHEIDUNG`, `BEAUFTRAGT`, `ERLEDIGT`, `VERWORFEN`, `OFFEN`) und eine enge Ankerliste. Alles, was heute dazugekommen ist — `AUSGELIEFERT`, `GEPRÜFT`, `GEBAUT`, `PRÜFEN`, `FREI`, dazu die Anker `LIVE-PRÜFUNG`, `NACH-SHIP-9`, `VOR MORGEN 14:00` —, fiel **still** in die Sachspalte und wurde als Fließtext gerendert. Zwanzig Zeilen von heute waren betroffen. **Behoben, und zwar mit einer Kalibrierung statt mit einer längeren Liste:** der Erzeuger meldet jetzt jede Zeile, in der er keinen Zustand erkennt, statt sie zu schlucken. Dazu trägt er `###`-Untergruppen mit, damit die Fristen auf der Seite stehen und nicht nur im Markdown | Ein Werkzeug, das Unbekanntes still schluckt, ist dasselbe wie ein Prüfer ohne Kalibrierung — die Lehre steht seit Wochen im Verfahren, und sie galt für Dateien, für Suchen und für Kanäle. Sie gilt auch für den eigenen Erzeuger. **Die Leseseite war die einzige Fläche, die niemand geprüft hat, weil sie ja nur anzeigt.** |
| I25 | OFFEN | NACH-VORTEST | **Was auf dem lokalen Gitea liegt, ist ungeklärt — und der Zweig `main` folgt ausgerechnet ihm.** `origin/main` trägt `78082c6` und eine Linie mit eigener Ship-Zählung bis „Ship 10". Woher sie stammt, wer sie gepusht hat und ob sie noch gebraucht wird, weiß niemand in diesem Fenster. **Die Gefahr ist nicht das Liegenlassen, sondern die Zugkraft:** solange `branch.main.remote = origin` steht, zieht ein versehentliches `git pull` diese fremde Linie in unseren Arbeitsbaum, und ein `git push --force` würde sie löschen. **Zwei Entscheidungen für Pedi, beide nach dem Vortest:** erstens, ob der Zweig künftig `github` folgen soll (`git push -u github main` setzt es beim nächsten Ship mit; es ist eine Konfigurationsänderung, deshalb entscheidet er sie und nicht der Kopf); zweitens, was mit dem Gitea geschieht — Archiv, Spiegel oder Altlast. **Bis dahin gilt: nicht anfassen, kein Pull, kein Merge, kein Force** | Zwei Wahrheiten in einem Repository sind derselbe Fehler wie zwei Wahrheiten in einem Register — nur dass diese hier beim ersten unbedachten `git pull` 114 fremde Dateien in den Baum zieht, und zwar am Tag vor einem Kundentermin. |
| D1 | PRÜFEN | NACH-VORTEST | **Beim Vorbereiten am 30.07. nachts nachgesehen: der Filter ist gebaut, und die Registerzeile war veraltet.** Die drei Merkmale des angenommenen Vorschlags sind alle da — **entfernbare Pillen je aktiver Auswahl** (`components/facets/FacetActiveBar.tsx:8,49-62`, mit der ausdrücklichen Regel, dass eine „0 Treffer“-Pille kein echter Wert ist), **live rechnende Zähler** (`FacetFilter.tsx:381-390`, `facet.countFiltered` gegen `facet.countAll`), **ausgegraute leere Facettenwerte** statt verschwundener (`facets/FacetGroupField.tsx:85-117`, `disabled` mit `opacity-45/50` und `cursor-not-allowed`). Dazu Bereichsfilter, Gruppen mit `hiddenCount`, URL-Abgleich in beide Richtungen und ein Effizienzvertrag für 10.000+ Objekte. Gebaut in mega9/mega10 und den sechs uxpol-Scheiben vom 24.07.; der Kopfkommentar sagt es wörtlich: „aus der Pillenwand wird eine Suchmaske“. **Offen ist deshalb kein Bauauftrag, sondern ein Abgleich:** was aus dem angenommenen Vorschlag (Artefakt `klarwerk-filter-vorschlag`, 25.07.) fehlt noch wirklich. Das ist Prüfarbeit, keine Bauarbeit | „Heute ist sie eine Liste, kein Werkzeug“ stand seit dem 25.07. im Register und war spätestens am 24.07. abends überholt. Wer danach plant, beauftragt Gebautes. |

---

## 2 — Beauftragt, läuft gerade

| Kennung | Zustand | Anker | Sache | Warum |
|---|---|---|---|---|
| B46 | GEPRÜFT | SOFORT | **ben sammel69 zu `patches182`: GRÜN — das letzte Verdikt vor der Auslieferung.** 90 Zeilen, 6.970 Bytes, Inhalts-SHA `8b04e7a5a1962f99fb14e1691bc2ebce5345dd6c140c2e2acedfb14f7cf53536`, **vom Kopf selbst nachgerechnet und identisch**, NUL-frei kalibriert. **Alle fünf Fragen grün beantwortet:** die Einordnung „latent, nicht aktiv“ trägt und spielt nichts herunter — die Sicherheitsmarge lag bei **genau einem weiteren asynchronen Hop**; die Messung ist sauber (12 Socket-Messungen, Kalibrierung mit `await` und zwei Async-Hooks erzeugte auf allen drei Klassen je zwei `writeHead` und `ERR_HTTP_HEADERS_SENT`, 6 dauerhafte Pins) · der Wächter kommt **ohne handgepflegte Liste** aus, 72 Dateien rekursiv, 4 von 4 Fundstellen, 6 Form-Kalibrierungen, leere Erhebung fail-closed rot, Grenze klar benannt · die Nachbarschaft schließt die Verhaltensspur · **der einzige für die Expertin erlaubte Rettungsweg auf der Fragenfläche bleibt begehbar** · und in den 86 Dateien kein Befund, der morgen früh nicht in Produktion sollte (vertieft geprüft: die 15 mega71-Dateien) | Vier ben-Runden an einem Abend, drei davon mit Befund, die letzte grün. Der Weg von ROT über ROT und GELB nach GRÜN ist genau der, den dieses Verfahren leisten soll. |
| B47 | OFFEN | SOFORT | **Ship 10 ist vorbereitet und wartet auf Pedis GO.** Dateiliste aus `patches182` abgeleitet nach `_relay/kopf/ship10-dateien.txt`: **86 Dateien**, kein `_relay/`, kein `LOT/`, kein `zz-spike`, kein `apps/web/dist`, keine `OFFEN.md`, keine Wegwerfsonde — **jede der 86 existiert**, einzeln geprüft. **Und die Sperrliste hat gehalten:** keine einzige Ship-Datei wurde nach dem Bau des Diffs verändert, mega72 hat außerhalb von `_relay/` bis jetzt nichts angefasst. Der Push geht an `github`, nicht an `origin` | Zum ersten Mal ist der parallele Takt gegen die Ship-Menge geprüft worden und nicht nur behauptet. Die Prüfung dauerte zehn Sekunden und ist ab jetzt Teil der Sequenz. |
| B45 | BEAUFTRAGT | VOR MORGEN 10:00 | **ben sammel69 über `patches182` — das letzte Verdikt vor der Auslieferung.** Fünf Fragen: trägt die Einordnung „latent, nicht aktiv“ oder weicht sie den Befund auf, ist die Messung sauber gebaut, **und stimmt die Zahl 54 oder ist sie eine Schätzung im Gewand einer Zahl** · kommt der neue Sammler wirklich ohne handgepflegte Aufzählung aus und sind seine 72 Dateien die richtige Grundmenge · ist die eine Filterstelle der Nachbarschaft wirklich die einzige und bedeutet ein Zähler jetzt etwas anderes · bleibt `/erfassen` für die Expertin begehbar, gibt es eine sechste Ask-Stelle, hat einer der sieben angepassten Bestandstests eine Zusicherung verloren · und die offene Frage. Auftrag 21 Zeilen, SHA `c6e6bef8…`, Formprobe 0, NUL-frei | Ist es grün, wird geliefert. Ist es das nicht, läuft der Vortest auf dem alten Stand mit vier bekannten Sackgassen. Beides ist tragbar, ein übersehener Fehler nicht. |
| B44 | BEFUND | NACH-VORTEST | **Die Zahl 54 ist von ben eingeordnet und war so, wie ich sie weitergegeben habe, zu stark.** Sie ist **keine** Anzahl erreichbarer Absturzpfade, sondern die Zahl syntaktischer Grep-Treffer für das enge Muster `reply.send(...);` mit unmittelbar folgendem `return;` unter `services/app/src` ohne Tests. Als „**~54 potenzielle** Produktstellen“ ist sie im Hand-Bericht korrekt gekennzeichnet — als Zahl semantisch verwundbarer Handler wäre sie nicht belastbar. **Am Fix und am Ship-Verdikt ändert das nichts.** Offen bleibt die Frage nach dem Vortest: fassen wir das Muster selbst an, oder reicht der Wächter, der den einen Auslöser verhindert | Ich hatte sie als „rund 54 Produktstellen tragen das verwundbare Muster“ weitergegeben und dazugesagt, dass ich sie nicht nachgerechnet habe. Genau deshalb ist sie geprüft worden — und genau deshalb steht die Einordnung jetzt hier statt einer Zahl, die niemand mehr hinterfragt. |
| B43 | GEPRÜFT | VOR MORGEN 10:00 | **mega71 geliefert, `./tools/check` grün (735 Testdateien / 5.450 Fälle).** Bericht 141 Zeilen, 16.538 Bytes, Inhalts-SHA `1a873d18bf57ab8a210cb7805fffad9abf1bd8011892539bcf866a87cc8f8a80`; Diff `_relay/patches182/mega71.diff` 6.998 Zeilen, 375.405 Bytes, 86 Dateiköpfe, SHA `2c7717ae519e2f7ec44dc6a4e93d39513a9bf32d9e17b7812a6f9fe23afb08c8`. **Beide vom Kopf selbst nachgerechnet und identisch**, NUL-frei bei kalibriertem Prüfer, keine verbotenen Pfade, **Gegenprobe gegen `patches181` sauber** — keine der 77 Dateien still verschwunden, 9 hinzugekommen. **Der Befund, der die Einordnung ändert:** die Hand hat das Doppel-Send-Fenster **am echten Socket vermessen** (`_relay/messung/a71-onsend-fenster.ts`) — mit dem einen nicht awaitenden Hook gewinnt die erste Pipeline um **einen Microtask**; der Blocker war **latent, nicht aktiv**. Ein `await` oder ein zweiter asynchroner Hook öffnet ihn sofort auf allen drei Antwortklassen. Repariert wurde trotzdem, weil die Regel die Bauform unabhängig verbietet — und der Sammler zitierte im roten Lauf genau `web-static.ts:46 [async]`, die Stelle, die die alte Liste nie gesehen hätte | Zum vierten Mal an diesem Tag hat eine **Messung** eine Annahme korrigiert — diesmal in die entlastende Richtung. Genau deshalb wird gemessen und nicht vermutet, in beide Richtungen. |
| B42 | OFFEN | SOFORT | **mega72 — der dritte Wächter derselben kaputten Bauart (A17).** Der kleine Parallelauftrag: geht in den Eingang, sobald mega71 berichtet hat, und läuft, während ben mega71 prüft. Der Modal-Sammler (`tests/app/mega47-modale-flaechen-sammler.test.tsx`) ist seit ben sammel46 als **statischer Musterwächter** bekannt — Alias, `createElement`, per Spread gesetztes `aria-modal` und Modalität ohne die Zeichenfolge gehen vorbei. **A** unabhängiger Zähler, der exakt gegen die beurteilten Flächen kalibriert; was nicht lesbar war, wird rot mit Datei und Zeile; drei synthetische Negativfälle sichtbar rot · **B** was der geschärfte Sammler über den heutigen Bestand sagt — **Funde werden gemeldet, nicht repariert.** **Harte Grenze: keine Zeile Produktcode, eigener Diff, nicht kumulativ.** 65 Zeilen | Es ist der dritte Wächter an einem Abend, der breiter zusagt als er erhebt. Und er ist der sicherste kleine Auftrag, den es gibt: reine Testarbeit kann die Ship-Runde nicht verunreinigen. |
| B41 | OFFEN | NACH-VORTEST | **mega72 — der Sammler, der breiter zusagt als er erhebt.** Liegt fertig in `_relay/hand/queue/`, 71 Zeilen, SHA `3a9b9965…`. **A** ein unabhängiger Rohzähler über alle `to`-Attribute, der exakt gegen die erfolgreich gelesenen kalibriert; jedes nicht lesbare Attribut wird rot mit Datei und Zeile; zwei synthetische Negativ-Kalibrierungen (verschachtelte Klammern, zwei unterschiedlich geschützte Template-Ziele) müssen sichtbar rot sein · **B** der hart codierte Zweier-Rollensatz wird durch die produktive Liste `ROLES` ersetzt; `FLAECHEN` und `HERKUNFT` bleiben, weil sie im Testkopf ehrlich als Geltungsbereich beschrieben und für erfasste Ausdrücke fail-closed sind · **C** Vorlage zu den 26 app-weiten Folgekandidaten, kein Bau. **Der Auftrag trägt oben den ausdrücklichen Satz, dass er NACH dem Vortest gefahren wird** — er ändert keinen Produktcode | Zweimal an einem Tag hat ein Wächter breiter zugesagt, als er erhebt: erst der Listen-Wächter gegen `async onSend`, dann dieser. Das ist keine Panne mehr, sondern ein Muster in der Bauart unserer Wächter. |
| B40 | GEPRÜFT | VOR MORGEN 10:00 | **ben sammel68 zu mega70: GELB** — 248 Zeilen, 12.211 Bytes, Inhalts-SHA `215a2ded1faedcf0e077cd68714c44d5362fb48d90ac6673eb1f838e6058af30`, **vom Kopf selbst nachgerechnet und identisch**, NUL-frei kalibriert. **Grün:** alle fünf Sackgassen sachgerecht über `RoleLink` geführt, keine entfernt die fachliche Angabe, keine erzeugt eine zweite Rollenlogik; die Erfolgskarte behält ihren Dirty-Wächter; die gemeinsame Tor-Karte lässt den Einschalt-Knopf im Stufenfall stehen und im Rollenfall weg; die betonte Handlung sitzt an einer Stelle und wird vor dem Rendern auf ein erreichbares Ziel gelegt; keine sechste Sackgasse in den zwölf direkten Vorkommen. **Gelb, beides im Wächter:** der Erhebungsausdruck trifft verschachtelte Klammern nicht und nimmt bei mehreren Template-Pfaden nur den ersten, ohne unabhängigen Zähler — fail-open; und im Inneren stehen drei handgepflegte Aufzählungen, von denen ben zwei als ehrlich beschriebenen Geltungsbereich durchgehen lässt und die dritte, den Zweier-Rollensatz, nicht | Ich hatte ihn gefragt, ob der neue Sammler dieselbe Schwäche trägt wie der Wächter, den er zwei Stunden vorher entlarvt hatte. Die Antwort war ja. Genau dafür ist er da. |
| B39 | BEAUFTRAGT | VOR MORGEN 10:00 | **ben sammel68 über `patches181` (mega70).** Fünf Fragen: sind seine vier Stellen aus sammel66 wirklich zu und gibt es eine sechste · hat der Umbau der Stufe-2-Karte zur gemeinsamen Tor-Karte den **älteren** Fall unbemerkt verändert (Einschalt-Knopf im Stufenfall erhalten, im Rollenfall abwesend) · sitzt die rollenabhängige Betonung an **einer** Stelle und lässt sie für jede der vier Rollen eine Betonung übrig · **trägt der neue Sammler dieselbe Schwäche wie der Wächter, den ben gestern entlarvt hat, nämlich irgendwo im Inneren eine von Hand gepflegte Aufzählung** · und die offene Frage. Auftrag 23 Zeilen, SHA `866b2b2d…4745b`, Formprobe 0, NUL-frei. Der `onSend`-Blocker und die Fragenfläche sind ausdrücklich ausgenommen — beide laufen gerade in mega71 | Läuft **parallel** zu mega71 auf getrennter Fläche. Ohne diese Parallelität ist der Schnitt um 10:00 nicht zu halten. |
| B38 | BEAUFTRAGT | VOR MORGEN 10:00 | **mega71 im Eingang, um Block E erweitert.** A der `onSend`-Hook wird synchron mit drei gepinnten Antwortklassen · B der Listen-Wächter wird Sammler · C die Nachbarschaft filtert vor der Ubiquitätsrechnung · D der zu absolute Typkommentar · **E neu: die fünf Rollen-Sackgassen auf der Fragenfläche.** 105 Zeilen, SHA `fdc615d4…6d551d`, NUL-frei. Die 26 app-weiten Folgekandidaten sind ausdrücklich **ausgeschlossen** | Aufgabe 3 des Vortests ist „stelle unter Fragen eine Frage“. Läuft die Frage in eine Wissenslücke — bei frischem Bestand der wahrscheinliche Fall —, steht sie vor genau diesen Wegen. Wir hätten die vier Sackgassen geschlossen und die fünf auf der Fläche stehen lassen, um die es in der Aufgabe geht. |
| B37 | GEPRÜFT | VOR MORGEN 14:00 | **mega70 geliefert, `./tools/check` grün (734 Testdateien / 5.439 Fälle / 18 Rauchproben, 2.492 Module gebaut, 370 Module / 1.258 Kanten ohne Verstoß, Biome 1.426 Dateien).** Bericht 243 Zeilen, 15.706 Bytes, Inhalts-SHA `5bd94bf8f4f67164a0ac7f8998e53aca48edf2209e7c605f6eac8d47534fde8f`; Diff `_relay/patches181/mega70.diff` 6.312 Zeilen, 338.041 Bytes, 77 Dateiköpfe, SHA `731ab4c420073b4cac19969e70f0524ca1adc2f97bf2552a928da3b172b7ed0f`. **Beide vom Kopf selbst nachgerechnet und identisch**, NUL-frei bei kalibriertem Prüfer, keine verbotenen Pfade, **Gegenprobe gegen `patches180` sauber** — keine der 65 Dateien still verschwunden, 12 hinzugekommen. **bens vier Stellen alle vier bestätigt, eine fünfte kam dazu:** der Leerzustands-Hauptknopf der Bibliothek wurde auch der Rolle `viewer` angeboten — **vom Sammler unabhängig rot gezeigt, nicht von Hand gefunden.** Das ist der Beleg, dass dieser Wächter ein Sammler ist und keine Liste | Die Reißleine wurde nicht gebraucht. Zum ersten Mal heute hat ein Wächter einen Fehler gefunden, den niemand gesucht hat — genau das ist der Unterschied zwischen Sammler und Liste. |
| B36 | OFFEN | VOR MORGEN 10:00 | **mega71 — bens Ship-Blocker und der Wächter, der ihn hätte fangen müssen.** Liegt fertig in `_relay/hand/queue/`, geht in den Eingang, sobald mega70 berichtet hat. **A** der `onSend`-Hook aus mega69 wird synchron, dazu drei gepinnte Antwortklassen (200, Wächter-4xx, Fehler-5xx) mit `unhandledRejection`-Spion, rot zuerst gegen die heutige Fassung · **B** `sync-onsend-hooks.test.ts` wird vom Listen- zum **Sammler**-Wächter, kalibriert rot an `web-static.ts` · **C** die Nachbarschaft filtert die Grundmenge **vor** `carriers` und der Ubiquitätsrechnung, Grenztest vier sichtbare plus ein vertraulicher Träger · **D** der zu absolute Typkommentar bei `billable` | Ohne A wird nicht ausgeliefert, und ohne Auslieferung trifft die Testerin morgen die vier Rollen-Sackgassen. |
| B35 | BEFUND | VOR MORGEN 10:00 | **ben sammel67 zu `patches180`: ROT** — 276 Zeilen, 14.032 Bytes, Inhalts-SHA `fb95352f9d712ffb06fb4615879465612b565a2c01a58837897b18beb5e43bf2`, **vom Kopf selbst nachgerechnet und identisch**, NUL-frei kalibriert. **Der Blocker:** der neue Hook `services/app/src/web-static.ts:46` ist `async` registriert. Auch ohne `await` erzeugt das eine asynchrone Hook-Grenze — **genau die Bauform, die in diesem Projekt schon einmal `ERR_HTTP_HEADERS_SENT` und einen Prozessabsturz verursacht hat**; belegt an vier eigenen Stellen (`addin-static-routes.ts:136`, `security-headers.ts:119`, `confluence-import-routes.ts:166`) und daran, dass **alle drei anderen app-globalen `onSend`-Hooks bewusst im Callback-Stil stehen**. Vom Kopf am Quelltext nachgesehen und bestätigt. B1, B2 und B3 aus sammel65 sind für ben **zu**; zwei gelbe Punkte bleiben (Ubiquität, Typkommentar) | Der Fund liegt auf dem Fehlerpfad, nicht auf dem Erfolgspfad — der neue Test prüft zwei erfolgreiche Abrufe und konnte ihn deshalb nicht sehen. Im schlechtesten Fall ein Neustart der Produktion, und zwar am Tag des Vortests. |
| I32 | BEFUND | SOFORT | **Der Wächter gegen genau diesen Fehler existierte — und schwieg, weil er eine Liste ist.** `services/app/src/sync-onsend-hooks.test.ts` ist ausdrücklich dafür gebaut, einen Rückbau auf `async onSend` rot zu machen, „bevor er das Doppel-Send-Fenster wieder öffnet“. In Zeile 21–26 steht `PRODUCTION_FILES` als **von Hand gepflegte Liste aus vier Dateinamen**; `web-static.ts` steht nicht darin. Er hat nicht falsch geprüft, er hat nur dort hingesehen, wohin ihn jemand geschickt hat. **Die Projektregel „Wächter als Sammler statt Liste“ gab es zu diesem Zeitpunkt bereits** — sie stand nur nicht in dieser Datei. mega71 Block B zieht sie nach | Ein Wächter mit einer gepflegten Liste ist kein Wächter, sondern eine Erinnerung. Er schützt genau so lange, wie jemand daran denkt — und das ist derselbe Mechanismus, den er ersetzen sollte. |
| B34 | BEAUFTRAGT | VOR MORGEN 10:00 | **ben sammel67 über den kumulativen Diff `patches180` (mega67+68+69).** Fünf Fragen im Fließtext: sind seine drei eigenen Auflagen wirklich zu und haben die gedrehten Alt-Tests dabei eine Zusicherung verloren · hält die **Idempotenz** der Bild-Umhüllung, bleiben die Bildkennungen über mehrere Läufe eindeutig, werden gespeicherte Textkörper verändert · trifft der neue `onSend`-Hook wirklich nur `/word-addin/*` und ist die Zusage am Draht gepinnt · sind unter den **dreizehn gedrehten Testverankerungen** welche, die mehr aufgegeben haben als die Schreibweise · und die offene Frage, ob in 5.141 Zeilen etwas steckt, das nicht ausgeliefert werden sollte. Auftrag 21 Zeilen, SHA `88d2a073…8a50c9`, Formprobe 0, NUL-frei. **Die Rollen-Sackgassen sind ausdrücklich ausgenommen** — sie werden gerade in mega70 geschlossen und kommen in der nächsten Runde | Ohne sein Verdikt wird nicht ausgeliefert. Und der harte Schnitt steht im Auftrag: grün bis 10:00 oder gar nicht. |
| B33 | GEPRÜFT | VOR MORGEN 14:00 | **mega69 geliefert, `./tools/check` grün (731 Testdateien / 5.421 Fälle / 18 Rauchproben, Lint 1.423 Dateien, 370 Module ohne Verstoß).** Bericht 462 Zeilen, 29.891 Bytes, Inhalts-SHA `2f112e0d50d70483b93fa9b72c4037505e27e67c02bee3863f913998312fc7fa`; Diff `_relay/patches180/mega69.diff` 5.141 Zeilen, 275.149 Bytes, 65 Dateiköpfe, SHA `55303f730eb607bbd0d151bb97c06f20bd4d09cd5082bba07d4387057b25653b`. **Beide vom Kopf selbst nachgerechnet und identisch**, NUL-frei bei vorher kalibriertem Prüfer, keine verbotenen Pfade, **Gegenprobe gegen `patches178` mit `comm` sauber** — keine der 27 Dateien still verschwunden, 38 hinzugekommen. **Der Befund der Hand widerlegt meine eigene Annahme:** der `figure`/`figcaption`-Anker entsteht nur auf dem Datei-Upload-Weg; über Klara kam ein Bild als nacktes `<img>` an, ohne Galerieeintrag, ohne Fußnote — der Weg zur Bildbeschreibung lief dort ins Leere. An der echten Produktfunktion gemessen, nicht behauptet. **Und meine Ausgrau-Hypothese zu `KnowledgeDetail.tsx:184` ist widerlegt:** ohne Modell ist nur der Vorschlagsknopf aus, mit sichtbarem Grund; die Handeingabe ging immer | Zum dritten Mal an einem Tag hat eine Messung eine Kopf-Annahme widerlegt. Das ist kein Unfall mehr, sondern der Beleg, dass die Regel greift: der Auftrag muss die Hand ausdrücklich einladen, dem Kopf zu widersprechen. |
| B32 | OFFEN | VOR MORGEN 14:00 | **mega70 — die stillen Sackgassen, liegt fertig in `_relay/hand/queue/`.** Fünf Blöcke, gebaut aus Bauteilen, die es schon gibt: **A** `Guarded` (`routes.tsx:69-79`) behandelt den Rollenfall wie den Stufe-2-Fall — eine erklärende Fläche statt `<Navigate to={HOME_ROUTE} replace>`; der Kommentar daneben sagt seit mega-UX-WOW ausdrücklich „KEINE stille Umleitung mehr“, **die Entscheidung ist längst getroffen und wurde nur für einen der beiden Fälle umgesetzt** · **B** die vier rohen Links auf gesperrte Ziele bekommen `RoleLink` (`RoleLink.tsx:46-80`, rendert `aria-disabled` + „Kein Zugriff“, die Startseite benutzt es schon) · **C** die betonte Handlung der Erfolgskarte richtet sich nach der Rolle, der Begleittext erklärt statt aufzufordern · **D** ein Sammler über die tatsächlichen Verlinkungen in `GUARDED_ITEMS` · **E** `Ask.tsx`, das ben nicht lesen durfte. Geht in den Eingang, sobald mega69 berichtet hat | Es ist der Unterschied zwischen „ich darf hier nicht hin“ und „das Programm hat mich rausgeworfen“. Für eine Testerin ohne Vorwissen ist das zweite ein Fehler des Produkts, und sie hat recht damit. |
| B31 | GEPRÜFT | VOR MORGEN 14:00 | **ben sammel66 zu `d0b9e5e`: ROT** — 181 Zeilen, 16.470 Bytes, Inhalts-SHA `b3b475c19f7639d15098af27eb714afbd637a30d6e1ed52a3256cc54b5a0d8dd`, **vom Kopf mit der Projektformel selbst nachgerechnet und identisch**, NUL-frei bei vorher kalibriertem Prüfer. bens eigener SHA über meinen Auftrag (`794e363d…98cb1e`) stimmt ebenfalls mit meinem überein. **Kern grün:** Zweck auf der Startseite, Erfassen, Fragen-Vertrag, Dateiweg, und alle Leertexte in DE/EN/NL ehrlich getrennt (27 i18n-Einträge geprüft). **ROT sind vier sichtbare Rollen-Sackgassen** — die Erfolgskarte nach dem Einreichen (`/validierung`, dunkel betonter Hauptknopf), der Erstbesuchs-Hinweis der Bibliothek, die Treffer-Handlung bei offenen und konfliktbegrenzten Objekten (`/validierung`, `/konflikte`), und „Re-Import (JSON)“ im Bibliothekskopf (`/import`, admin + Stufe 2). Jedes Mal folgt eine kommentarlose Umleitung auf `/start`. **Aufgabe 7 hat keinen Produktweg** — das ist richtig so, das Urteil steht auf dem Papierprotokoll der Aufgabenkarte | Der Befund trifft genau den Moment, den der Vortest messen soll: ihren ersten eigenen Erfolg. Sie reicht ihren Tipp ein, das System lobt sie, bietet ihr als betonten nächsten Schritt „Zur Prüfung geben“ an — und wirft sie wortlos auf die Startseite. |
| B30 | BEAUFTRAGT | SOFORT | **ben sammel66 — der ausgelieferte Stand `d0b9e5e`, gelesen mit den Augen einer Testerin.** Läuft **parallel** zur Hand, auf getrennter Fläche und ausschließlich gegen den eingefrorenen Commit (`git show`/`git grep`), nie gegen den bewegten Arbeitsbaum. Drei Fragen im Fließtext: was ein **Experte** auf dem Weg der Aufgaben 1–5 und 7 nicht sieht, was er nicht darf und wo das ohne Erklärung endet · ungedeckte Zusagen auf demselben Weg, je Fund mit der Angabe, ob eine Testerin ihn morgen wirklich trifft · der leere Fall bei Suche und Frage, in DE, EN und NL. Aufgabe 6 und alle Flächen von mega69 ausdrücklich ausgenommen. Auftrag 23 Zeilen, SHA `794e363d…98cb1e`, NUL-frei kalibriert, Formprobe `grep -cE` = 0 | Morgen um 14:00 läuft **nur** `d0b9e5e` — alles seither Gebaute ist für sie unsichtbar. Diesen Stand hat bisher niemand unabhängig daraufhin angesehen, was sie dort trifft. Es ist die letzte Gelegenheit, es vor dem Termin zu erfahren. |
| B29 | BEAUFTRAGT | SOFORT | **mega69 — Klara, bens drei Auflagen, die Restschuld aus mega68.** Acht Blöcke, **Block A mit ausdrücklichem Vorrang: das Eingabefeld für die Bildbeschreibung dort, wo das Bild ankommt, mit KI-Vorschlag aus dem umgebenden Text.** Liegt in `_relay/hand/inbox/AUFTRAG-mega69.md`, 141 Zeilen, SHA `343d67c5…8fedcd`, NUL-frei kalibriert. Modell `claude-fable-5`, von Pedi freigegeben | Klara ist von Pedi als Grundsatzaufgabe von Anfang an benannt und ist in jeder Runde hinter etwas zurückgestanden, das dringender aussah. |
| B28 | BEFUND | SOFORT | **mega68 ist ohne Evidenz zu Ende gegangen.** Der Bericht `_relay/hand/outbox/BERICHT-mega68.md` (200 Zeilen) trägt an drei Stellen `CHECK_PLATZHALTER`, `DIFF_PLATZHALTER` und `KALIBRIER_PLATZHALTER`; `_relay/patches179/` existiert nicht. Der Lauf endete, während `./tools/check` noch lief. **Die Arbeit ist da** — Nachbarschaftsroute mit Ubiquitäts-Regel und Deckel 12, 8/8 Routentests, Messung 12,8 ms / 1.358 Bytes gegen 7,4 s / 72 Mio. Kanten — **der Nachweis fehlt.** mega69 reicht ihn kumulativ nach (patches180 trägt mega67+68+69) | Ohne Nachweis kann ben nicht prüfen und nichts ausgeliefert werden. Der Nachbau kostet nichts, das Nichtbemerken hätte eine ganze Runde gekostet. |
| B27 | GEPRÜFT | NACH-VORTEST | **ben sammel65 zu mega67: ROT.** 259 Zeilen. Drei Auflagen vor jeder Auslieferung: die Fragenfläche (`Ask.tsx:526-531`, `:542-547`) zeigt den Kostenhinweis **unbedingt** und umgeht damit die zentrale Ableitung · `billable` (`service.ts:789-797`) sagt „Cloud kann verwendet werden“ und nicht „das kostet Geld“, der Wortlaut in `i18n.ts:4414` ist zu absolut · `importAccessState` verspricht vier Zustände, `ImportAccessPanel.tsx:58-61` übergibt fest `"active"`, `not-built` ist unerreichbar und der Test „die vier Texte“ erzeugt drei. Sicherheitskern und QueryClient-Korrektur ausdrücklich GRÜN. **Alle drei stecken in mega69 Block B** | Drei Zusagen, die der Code nicht einlöst — dieselbe Fehlerklasse, die diese Woche dreimal getroffen hat. |
| B14 | ERLEDIGT | — | **mega59 ausgeliefert in Ship 8 (`bcf1f15`).** ben sammel57: ROT — ausschließlich Block C; A, B, D, E, F, H, I und die Tor-Erweiterung sind von ben ausdrücklich als abnahmefähig bezeichnet, G2 ist GELB (siehe G2ci). Verdikt 372 Zeilen, 14.939 Bytes, Inhalts-SHA `27655cc2…c075b54`, vom Kopf nachgerechnet, NUL-frei kalibriert.** Ursprünglich: **mega59 gebaut — alle NEUN Blöcke, `tools/check` komplett grün, jetzt bei ben (sammel57).** Diff `_relay/patches170/mega59.diff`, 3.029 Zeilen, 181.553 Bytes, SHA `a40de0fe…7d8d7`, 46 Abschnitte; Bericht 476 Zeilen, Inhalts-SHA `3cbf1456…b506bc`. Vitest 690 Dateien / 5.177 Fälle grün, Browser-Smoke 18 grün in beiden Datenlagen. **Vom Kopf vollständig nachgerechnet:** beide SHAs auf je zwei Wegen, Bytezahl gegen die Maschine (I17), NUL kalibriert, verbotene Pfade 0/0, `_relay/` nur als Fließtext, **Egress null Treffer** in allen Plus-Zeilen, und am Endzustand belegt, dass beide Testhelfer aus `index.ts` raus sind, der tote Zweig entfernt ist und die absolute Wertetafel wirklich noch existiert (`mega57-suchbar-und-tragend.test.ts:166`, Werte 0–6). Inhalt: A vier neue Bestandsobjekte · B Herkunftstreue (`-ung`, beidseitig, fail-closed; `Haltung` als Regelfolge) · C Komposita mit drei notwendigen Bedingungen · D–F die drei Live-Befunde mit echten Klickpfaden · G Tor sieht, genau 3 Typfehler gefunden und behoben, 19 verwaiste Testdateien laufen mit · H fünf Rauchproben rot-zuerst umgehängt · I Regeln getrennt | Alles darin ist entweder etwas, das die Testerin morgen sieht, oder etwas, das uns blind macht, während wir „komplett grün" melden. Block A ist der harte: ohne „Lieferanten" im Bestand scheitert sie an Aufgabe 4 an einer unlösbaren Aufgabe — **und Block A wirkt nur, wenn der Bestand nach Ship 8 live neu geladen wird.** |
| B13 | ERLEDIGT | — | **mega58 ausgeliefert in Ship 7 — `46aac36`.** ben sammel56: **GRÜN**, 260 Zeilen, 10.597 Bytes, Inhalts-SHA `6e27f093…5655`, vom Kopf nachgerechnet, NUL-frei kalibriert. ben hat zusätzlich alle fünf Antwortpfade durchverfolgt (Ask-Service, deterministischer Provider, Modellprovider, Retrieval-Weg fürs Add-in) und den Recall-Preis **strukturell** bewiesen statt behauptet. Ohne Auflage; zwei Anmerkungen ohne Farbe, beide in mega59 aufgenommen | |
| B13a | ERLEDIGT | — | **mega58 gebaut, `tools/check` grün.** Block A schließt S6 in beiden Auswahlwegen, Block B klärt S7. Diff `_relay/patches169/mega58.diff`, 3.834 Zeilen, 221.028 Bytes, SHA `19606884…d5aab`; Bericht 299 Zeilen, Inhalts-SHA `f9515a87…10e47`. **Vom Kopf vollständig nachgerechnet:** beide SHAs auf zwei Wegen, NUL kalibriert, verbotene Pfade 0/0, `_relay/` nur als Fließtext, Gegenprobe `comm` gegen mega57 ohne Verlust, angefasst allein `provider.ts`, Egress-Treffer sämtlich `fileURLToPath` | Ohne S6 kann eine substanzlose Quelle allein in der Antwort stehen — genau der Vertrag, den Ship 6 dem Nutzer gegeben hat, wäre verletzt. Ohne geklärtes S7 gibt es kein ehrliches Ship-GO, und ohne Ship 7 fehlen am Freitag die geschlossenen Recall-Lücken aus mega54–57. |
| B12 | ERLEDIGT | — | **ben sammel55 zu mega57: ROT**, 258 Zeilen, 11.937 Bytes, Inhalts-SHA `c09e10fc…8710`, vom Kopf nachgerechnet, NUL-frei kalibriert. Zwei Blocker: S6 und S7. **Ship 7 wurde nicht gefahren** | Die Zielidee von mega57 nennt ben ausdrücklich richtig, und sechzehn von neunundzwanzig mehrdeutigen Einträgen hat er stichprobenweise geprüft und die Klassifikation grün gegeben. Der Vertrag ist trotzdem verletzt — nicht in der Idee, sondern in ihrer Verdrahtung. |
| B11 | BEAUFTRAGT | SOFORT | **mega57 — suchbar und tragend werden getrennt.** Mehrdeutige Formen (`woll`, `würd`, `wart`, `fall`) bleiben im Tokenstrom, zahlen aber nicht auf die Mindestsubstanz ein. Dazu drei Sammler-Härtungen aus ben sammel54. **Danach wird gestoppt.** | Vier Runden lang hat eine Liste zwei Fragen beantworten sollen: *ist ein Wort suchbar* und *trägt ein Wort eine Antwort*. Deshalb war jede Aufnahme ein Verlust und jede Auslassung ein Loch. Ab der Trennung ist jede Ergänzung kostenlos — das ist der Grund, warum die Kette hier endet und nicht bei mega58. |
| B10 | BEAUFTRAGT | SOFORT | **mega56 — die geschlossenen Wortklassen.** 218 neue Einträge in zehn benannten Klassen, Formen abgeleitet statt geraten, Sammler über 52.414 Paare rot-zuerst. ben sammel54: **ROT**, `0a1e2a65…faf4` — `woll` und `würd` blieben erreichbar | Der Kopf hatte die Grenze zweimal falsch gezogen: erst auf Pronomen, dann mit einer Längenregel, die `könnte` nicht fängt. Inhaltswörter sind die offene Klasse, Funktionswörter sind geschlossen und damit abzählbar. |
| B9 | BEAUFTRAGT | SOFORT | **mega55 — Stoppwörter überleben die Grundform nicht.** Zweites Sieb nach der Normalisierung, `Gesicht` bleibt über die `ge`-Rücknahme erhalten. ben sammel53: **ROT**, `14e1c93d…fe53` — Modalformen außerhalb der Liste | mega54 hatte die Tür aus mega53 unabsichtlich wieder geöffnet: `meinem` und `deinen` passierten den Filter und wurden danach zu `mein` und `dein`. Zwei Fürwörter erfüllten die Mindestsubstanz. |
| B7 | BEAUFTRAGT | SOFORT | **mega54 — die Token-Scheibe.** Kennungen wie „F3" überleben die Zerlegung, „geprüft" trifft „prüfen", eine Zerlegung statt zwei, plus Block E. ben sammel52: **ROT** wegen der Stoppwort-Rückkehr, aber Kennungsregel, Prefilter-Zusage, Mengenbildung, Embedding-Abgrenzung und Block E ausdrücklich abgenommen | Ohne diese Scheibe endet am Freitag jede dritte Frage in einer Wissenslücke, obwohl die richtige Antwort im Bestand liegt. Die drei Lücken sind seither zu und bleiben es in allen Folgerunden. |
| B8 | ERLEDIGT | — | **Ship 6 am 29.07., 15:32 — Commit `1fc55aa`**, mega52 + mega53. Integrationstests 44 grün, Coolify Success. ben sammel51 GELB: beide ROT-Blocker geschlossen, GELB nur wegen des Recall-Preises | Vorher konnte eine Antwort auf einem einzigen gemeinsamen Wort stehen und trotzdem einen hohen Vertrauenswert zeigen. Das war die eine Eigenschaft, an der das Produkt am Freitag scheitern konnte. |
| B6 | ERLEDIGT | — | **mega45–mega51** in Ship 3 bis Ship 5 ausgeliefert, ben sammel45–49 je GRÜN, Inhalts-SHAs vom Kopf nachgerechnet | |
| B5 | ERLEDIGT | — | `LOT/` steht in `.gitignore` (mega50 D2, 343 MB) | Ein einziges `git add -A` hätte den ganzen Jahresring-Testbestand ins Repository gezogen und es dauerhaft unbrauchbar gemacht. |
| B4 | ERLEDIGT | — | Versionsnummer auf `1.0.0-beta.1.27` gezogen (mega50 D1) | Eine Versionsnummer, die fünf Auslieferungen hinterherhinkt, identifiziert den laufenden Stand nicht mehr — bei einem Fehlerbericht weiß niemand, was der Kunde wirklich hat. |
| B3 | ERLEDIGT | — | **SCRUM-557** in mega50 Block C geschlossen — Auflage zum bedingten GO von Ship 3, vollständig eingelöst | |

**Hier wurde gestoppt.** bens Reihenfolge für den Wiedereinstieg, seine und nicht meine: erstens S6 schließen, zweitens S7 auflösen und das volle Drei-Engine-Tor grün belegen, drittens die Mehrdeutigkeiten kontextsensitiv machen — zuerst `Wartung`/`wart`, danach `nahe`, viertens den Testhelfer `meetsAnswerSubstance` wieder intern machen und absolute gegen relative Regel im Code sauber trennen, fünftens die Sammler-Paradigmen aus einem unabhängigen Vertrag ableiten und einen Mehrkandidaten-Sammler ergänzen, sechstens die Recall-Schulden: Komposita und `koCarBlau` (S5), Überstemmung, und die in mega56 vollständig entfernten Häufigkeitsadverbien.

---

## 3 — Vor dem Vortest

Fester Termin: **Freitag 14:00**, Natascha, sieben Aufgaben, kein Vorwissen.

| Kennung | Zustand | Anker | Sache | Warum |
|---|---|---|---|---|
| A1 | ERLEDIGT | — | Interviewleitfaden gegengelesen und erweitert: die Lehrlings-Frage aus K5 steht als **Schlussfrage**, mit zwei Sperren davor — sie darf erst fallen, wenn Nataschas eigener Satz aus Thema 1 gesagt ist, und das Wort fällt vorher an keiner Stelle. Im Protokoll stehen beide getrennt | So sehen wir, ob das Bild ihre Sicht **getroffen** oder **ersetzt** hat. Ein klares Nein ist im Prompt ausdrücklich als das wertvollste Ergebnis markiert. |
| A2 | ERLEDIGT | — | **Live geprüft am 30.07. um 10:06, von Pedi im Screenshot belegt.** Kategorie „Einkauf & Lieferanten" mit zwei Objekten: „Lieferanten für Hydraulikteile: immer eine Zweitquelle führen" (**validiert**, nutzbar, Vertrauen 70, Best Practice) und „Lieferanten des Presswerks: Konditionen und Preisstaffel" (**offen**, zu prüfen, Vertrauen 55, Technik). Schlagwörter `lieferanten`, `einkauf`, `hydraulik`, `konditionen`, beide als DEMO-BEISPIEL gekennzeichnet. **Aufgabe 4 des Vortests ist damit gesichert.** Ursprünglich: gebaut in mega59 A Zwei Lieferanten-Objekte, eines validiert, eines offen, „Lieferanten" in Titel und Schlagwörtern. **Bleibt offen, bis Pedi den Bestand live neu geladen und die Suche einmal live geprüft hat** | Aufgabe 4 des Vortests sucht danach. Fehlt der Begriff im Bestand, scheitert die Testerin an einer unlösbaren Aufgabe und wir messen unsere Datenlücke statt ihrer Bedienbarkeit. |
| A24 | ERLEDIGT | — | **Entschieden von Pedi am 30.07. abends, und die Karte ist neu gebaut: `KLARWERK-Generalprobe-Natascha-v3.pdf`.** Seine Einordnung zum Banner: „nicht einverstanden passt, es geht ja nicht kaputt" — der Knopf bleibt unangetastet, die Karte erwähnt ihn nur. Beide Sätze stehen jetzt **in dem Kasten, der ohnehin davon handelt** („Du kannst nichts kaputt machen") statt als zwei neue Kästen: die erste Fassung hat die Seite auf **vier** Seiten aufgeblasen, die Fußzeilen sagen aber „Seite 1 / 3". Am Ende belegt: drei Seiten, Fußzeilen stimmen, beide Sätze im Text — mit einer Kalibrierung, die einen Satz sucht, der **nicht** drinstehen darf. Ursprünglich: **Die Aufgabenkarte ist gegen Ship 6 gebaut — seitdem sind acht Scheiben ausgeliefert, und drei davon liegen auf ihrem Weg.** Durchgang in `_relay/kopf/VORTEST-Durchgang-gegen-Ship-9.md`, sieben Aufgaben gegen den Quelltext gelesen. **Drei Kollisionen.** **Erstens:** die Karte sagt zweimal „du kannst nichts kaputt machen" und „klick mutig auf alles" — seit Ship 9 gibt es unten den Knopf **„Nicht einverstanden"**, und der beendet die Sitzung. Gewollt und richtig gebaut, aber für jemanden mit dieser Einladung ein Stolperdraht. **Den Banner abzuschalten wäre die schlechteste Lösung:** der Schalter trägt seit mega62 BEIDE Hinweisflächen, nähme also den Hinweis nach Artikel 50 mit — ausgerechnet zur ersten Vorführung. **Zweitens:** „Ein Klick startet sofort eine echte, kostenpflichtige KI-Anfrage" steht seit mega62 an sieben Stellen, darunter genau an der von Aufgabe 3. Ein höflicher Gast klickt dann womöglich nicht — und wir messen Rücksicht statt Verständlichkeit. **Drittens:** Aufgabe 5 führt in die Import-Seite, die dreimal bewusst liegen blieb (C1). **Kopf-Empfehlung: zwei Sätze auf die Karte**, einer zum Banner, einer zu den Kosten — beide sagen, was passiert, und nicht, wo etwas liegt, damit Aufgabe 1 messbar bleibt. Die Karte hat seit dem 29.07. eine Quelldatei; ein neues PDF kostet Minuten | Es ist derselbe Fehler wie bei einem veralteten Register, nur an der Fläche, an der es morgen um vierzehn Uhr auffällt: **ein Dokument, das einen Stand beschreibt, den es nicht mehr gibt.** Die Karte verspricht Sorglosigkeit, und das Produkt hat seit heute einen Knopf, der die Sitzung beendet. Keines von beidem ist falsch — zusammen sind sie es. |
| T6 | ERLEDIGT | — | **Entschieden von Pedi am 30.07.: „da sehe ich kein Problem, keinen Konflikt."** Natascha bekommt die **Kurzfassung** vor Aufgabe 1 — sie ist genau dafür geschrieben, kostet fünf Minuten und erklärt die Oberfläche mit keinem Wort. Die vollständige Unterrichtung wird danach nachgereicht, mit eigenem Datum im Nachweis; sie liegt seit heute auch im Produkt unter `docs/compliance/`. **Damit ist Artikel 4 zum ersten Mal erfüllt** — achtzehn Monate nach Inkrafttreten. Ursprünglich: **Die Unterrichtung nach Artikel 4 ist geschrieben — mega61 Block J ist damit ohne Bauauftrag erledigt.** `_relay/kopf/RECHT-Unterrichtung-Artikel-4.md`, sieben Teile, Textfassung `2026-07-30.1` — **dieselbe Kennung, die das Produkt in `services/auth/src/notice.ts` führt**, damit Produkt und Unterrichtung nicht auseinanderlaufen. Teil 2 sind die zwei Seiten, die eine Person liest; Teil 3 der Nachweis mit Datum, Kreis und Textfassung; Teil 4 der Hinweis, dass **der Kunde selbst Betreiber ist** und Artikel 4 in eigener Verantwortung trägt; Teil 5 belegt **jede** Tatsachenaussage am Quelltext (dreizehn Belegstellen). Sechs Platzhalter, alle im Nachweis, alle nur von Pedi zu füllen. **Die Kurzfassung ist geschrieben und liegt bereit:** `_relay/kopf/RECHT-Unterrichtung-Artikel-4-Kurzfassung.md`, Textfassung `2026-07-30.1-kurz`, fünf Minuten Lesezeit, mit eigenem Nachweisfeld. Sie nennt Grenzen, Prüfpflicht, verbotene Inhalte und Meldepflicht — und **die Oberfläche mit keinem Wort**: keine Knöpfe, keine Wege, keine Bezeichnungen, keine Reihenfolgen. Ein eigener Abschnitt listet auf, was bewusst fehlt, damit es später niemand für eine Auslassung hält. **Die Entscheidung, die bei Pedi liegt, ist der Zeitpunkt:** Natascha bedient das System am Freitag in unserem Auftrag und fällt damit unter Artikel 4 — aber wer Teil 2 vorher liest, ist nicht mehr vorwissensfrei, und Aufgabe 1 misst genau das. Drei Wege stehen in Teil 6, **Kopf-Empfehlung ist der dritte:** eine gekürzte Fassung vorher, die nur Grenzen und Meldepflicht nennt, ohne die Oberfläche zu erklären. Dann bleibt Aufgabe 1 messbar und die Kompetenzpflicht ist im Kern erfüllt | Artikel 4 gilt seit dem 2. Februar 2025 und lag bei uns **achtzehn Monate** offen, ohne dass es jemand wusste — die Verordnung war im ganzen Repository nirgends bewertet. Es ist die einzige Pflicht, die niemand technisch erfüllen kann, weil sie rein organisatorisch ist: wer sie erfüllt, hat bei einer Prüfung sofort etwas vorzuweisen, wer sie übersieht, hat eine Lücke, die achtzehn Monate zurückreicht. Der Vortest ist die **erste** Gelegenheit, sie überhaupt zu erfüllen. |
| A6 | ERLEDIGT | — | Aufgabenkarte neu gebaut: `_relay/generalprobe/KLARWERK-Generalprobe-Natascha-v2.pdf`, drei Seiten, toter Termin raus, Einstieg **wortgleich neutral**. Quelle ab jetzt `_relay/generalprobe/quelle/generalprobe-natascha.html`. **Pedi vergleicht und ersetzt das Original selbst** | Es gab keine Quelldatei — für einen falschen Satz musste die Karte neu entstehen. Jetzt kostet die nächste Änderung zwei Minuten. Der Einstieg blieb neutral, weil Aufgabe 1 misst, ob sich das Produkt selbst erklärt; ein Rahmen davor würde nur noch die Karte messen. |
| E9 | OFFEN | VOR-VORTEST | **Entschieden: der Bestandsreset schließt den Papierkorb ein — aber die registrierten Nutzer bleiben bestehen.** Gelöscht werden Wissensobjekte und Papierkorb, nicht die Konten | Der heutige Bestand ist Entwicklungsschutt: halbe Objekte, Testtitel, 72 Prozent Papierkorb. Den sieht die Testerin sonst als „euer Wissen". Die Konten müssen bleiben, sonst ist auch ihr Zugang weg. |
| G3b | OFFEN | VOR-VORTEST | **Gebaut und ausgeliefert (mega59 A, Ship 8) — wirkt erst nach dem Live-Laden.** Alle drei Vertraulichkeitsstufen kommen vor, ein Anhang hängt an einem **nicht-internen** Objekt, dazu ein neuer Wächter (`mega59-vortest-bestand.test.ts`), weil es dafür vorher keinen gab. **Bleibt offen bis zum Live-Laden** | Ohne Objekte mit Stufen und Anhängen lässt sich weder der Zugriffsschutz noch die Anlagenkopplung vorführen — zwei der Dinge, die Klarwerk von einem Notiz-Werkzeug unterscheiden. |
| E4d | OFFEN | VOR-VORTEST | D2 Klara-Refresh vor Freitag schneiden | Klara ist die Fläche, die dem Kunden am schnellsten den Unterschied zeigt. Wenn sie am Freitag alt aussieht, trägt der Rest die Vorführung allein. |
| A17 | OFFEN | NACH-VORTEST | Der Modal-Sammler ist ein statischer Musterwächter, kein AST-Wächter: Alias-Nutzung, `createElement`, Spread-gesetztes `aria-modal` und Modalität ohne die Zeichenfolge gehen vorbei (ben sammel46) | Der Wächter hält, was heute im Code steht, aber nicht jede Bauform von morgen. Er verspricht mehr Deckung, als er hat — und genau solche Zusagen fallen uns später als „war doch grün" auf die Füße. |
| A18 | OFFEN | NACH-VORTEST | Die Browsermessungen prüfen Bedienbarkeit, Fokus und `inert`, nicht die Sprachausgabe (ben sammel46) | Barrierefreiheit ist bei Industriekunden eine Vergabefrage. Wir behaupten sie heute weiter, als wir sie gemessen haben. |
| A19 | OFFEN | NACH-VORTEST | Firefox und WebKit liefen einmalig grün, im Tor läuft nur Chromium (ben sammel46) | Ein einmaliger grüner Lauf ist keine Garantie. Bricht Safari beim Kunden, erfahren wir es vom Kunden. |
| A21 | ERLEDIGT | — | **KI-Modus geprüft, 29.07. 15:5x:** `/api/ai-status` meldet `active: true`, `mode: cloud`, `reachable: active`, alle acht Aufgaben am Modell. Der Cloud-Schlüssel in Coolify ist gesetzt **und erreichbar** | Ohne diesen Nachweis wäre jeder Live-Beleg vor Freitag wertlos gewesen: bei fehlendem Schlüssel läuft alles still im deterministischen Ersatzmodus weiter, und wir hätten geprüft, was die Testerin gar nicht sieht. |
| A7 | ERLEDIGT | — | Rolle `Experte` — von Pedi entschieden | |
| A14 | ERLEDIGT | — | Lars-Paket gegenstandslos | |
| A3 | ERLEDIGT | — | Sichtbarer Knopf „Datei auswählen" — mega34 Block D, `capture-file-pick` plus Ablagefläche als echter `<button>` | |
| A4 | ERLEDIGT | — | Zur Antwort scrollen und Ladezustand — mega38 Block A, `scrollIntoView` und Ladezustand am Ergebnisort | |
| A5 | ERLEDIGT | — | Ein Urteil je Antwort — drei benannte Zustände, `trustNote` („kein Wahrheitsversprechen"), „N Quellen **herangezogen**" | |
| A15 | ERLEDIGT | — | **Bildbeschreibung: Formular und KI-Vorschlag fehlten auf der Vordertür.** Geschlossen in mega50 — `ImageDescribeContext` als eine Quelle, in `App.tsx` montiert | |
| A16 | ERLEDIGT | — | Sammler über optionale Verträge: derselbe Fehler wie `FacetFilter` ohne `backgroundRef` — ein Aufrufer lässt einen optionalen Prop weg, nichts wird rot | Zwei Fälle in zwei Tagen aus derselben Ursache. Ohne den Sammler wäre der dritte nur eine Frage der Zeit gewesen. |

---

## 4 — Nach Ship 6, vor dem Großkunden

| Kennung | Zustand | Anker | Sache | Warum |
|---|---|---|---|---|
| K4 | OFFEN | MIT-ADOPTION | **Adoption: wie bringen wir Anwender dazu, das System zu benutzen.** Sieben Konzeptvorgänge SCRUM-475–481. Läuft parallel als Kopf-Arbeit, kostet keinen Code-Takt | Die KO-Frage des ganzen Produkts. Ein Wissenssystem, das niemand füttert, ist leer — und ein leeres Wissenssystem beantwortet keine Frage, egal wie gut das Retrieval ist. |
| K5 | OFFEN | MIT-ADOPTION | **Das Lehrlings-Bild — Pedis Fund vom 29.07.** Nicht „dokumentiere dein Wissen", sondern „bilde einen Lehrling aus, dann bleibt das Wissen im Haus". Dieselbe Handlung, umgekehrtes Vorzeichen. Der Interviewweg im Produkt IST bereits der fragende Lehrling — heute eine Funktion, noch kein Rahmen. Gehört als Antwort zu K4 | Dokumentieren ist unbezahlte Verwaltung und riecht danach, sich selbst überflüssig zu machen. Einen Lehrling anlernen ist das, was ein Meister tut: es hebt den Rang, statt ihn zu senken, und der Betrieb hat einen Namen und eine Kultur dafür. Das Bild beantwortet zum ersten Mal ehrlich die Frage „was habe ich davon". **Vorsicht:** wer „Lehrling" sagt, verspricht ein System, das aus Korrekturen besser wird — das können wir heute nicht belegen. Ohne Deckung wird aus dem Bild eine Falschzusage (I12). |
| S3 | BEAUFTRAGT | SOFORT | **Der Recall-Preis, entschieden und beauftragt.** Drei von zehn Fragen endeten in einer Wissenslücke, darunter Pedis P0-Frage „Wie oft muss der Filter F3 geprüft werden?". Pedi: „token jetzt". ben widerruft in sammel51 seine eigene Reihenfolge. Läuft als mega54 | Eine ehrliche Lücke ist ein Ergebnis. Eine Lücke, hinter der das richtige validierte Objekt nachweislich im Bestand liegt, ist ein Fehler — und sie trifft ausgerechnet die Frage, die wir selbst als Prüffrage gewählt haben. |
| S6 | ERLEDIGT | — | **Geschlossen in mega58 Block A, ausgeliefert in Ship 7, von ben GRÜN.** Das Tor steht in `keywordSelect` und `rankCandidates` im Kandidatenfilter, vor Bestwert und relativer Regel; die `Math.max`-Vorabprüfung ist in beiden entfallen. ben hat belegt, dass **keiner** der fünf Antwortpfade es umgeht und dass kein Kandidat mit zwei eigenen Fachwörtern dadurch verlieren kann. Der ursprüngliche Befund: das Substanztor galt für den ganzen Kandidatenpool statt je Kandidat. `keywordSelect` (`provider.ts:1236-1240`) und `rankCandidates` (`:1295-1300`) prüfen `Math.max` über **alle** Substanzwerte; danach entscheidet die relative Regel auf dem Überschneidungswert. Eine Quelle mit `substanz = 0`, aber hohem Wert kommt dadurch mit — und kann den tragenden Treffer sogar **verdrängen**: bei Wert 5 gegen Wert 2 gilt `2·2 > 5` nicht. Vom Kopf am Code nachgeprüft. bens kleinster ehrlicher Fix: erst jeden Kandidaten unter der Mindestsubstanz entfernen, **dann** Bestwert, relative Regel, Ranking und topK | Der Kommentar der Hand nennt die Absicht ausdrücklich: ein Kandidat ohne eigene Substanz solle „nur als Mitläufer eines substanzstarken Treffers erscheinen — nie allein". Genau diese Annahme fällt, sobald der substanzstarke Treffer an der relativen Regel scheitert. Dann steht der Mitläufer allein in der Antwort. |
| S7 | ERLEDIGT | — | **Geklärt in mega58 Block B, ausgeliefert in Ship 7, von ben GRÜN — ausdrücklich als „ehrliche Korrektur eines unmöglichen Testkontexts, kein Ersatzgrün".** Der Ausgangsbefund: `npm run smoke:ui` war in allen drei Engines rot an `tests-smoke/ui-smoke.spec.ts:112`, der Knopf „Fragen" wurde ohne Modell als deaktiviert erwartet und war aktiviert. Der Befund war schärfer als die Hypothese, vom Kopf am Quelltext bestätigt: `playwright.smoke.config.ts` bricht außerhalb des hermetischen Tors **hart ab**, wenn der Ship-Schlüssel fehlt — jeder `smoke:ui`, der bis zu den Tests kommt, trägt zwangsläufig ein Modell, der Fall war dort **strukturell unerfüllbar**, nicht wackelig. Gehärtet allein im Testfall: sichtbares Überspringen an `ANTHROPIC_API_KEY`, ausdrücklich nicht am Ship-Schlüssel; in `smoke:ui:gate:drei` läuft er in allen drei Engines voll. **Offene Frage an ben: ehrliche Bauform oder Ersatzgrün** — ein vorher rotes Tor wird grün, ohne dass sich das Produkt geändert hat | Ein rotes Tor ist kein Ship-GO, unabhängig davon, wer recht hat. Und es ist dieselbe Datei, in der schon der hohle Rauchproben-Fall (S1) saß — die Fläche ist in Bewegung. |
| T3 | AUSGELIEFERT | LIVE-PRÜFUNG | **Gebaut in mega61 B, E, F, G, H und in mega62 E, F vervollständigt; Artikel 4 ist mit T6 geschrieben.** Damit steht die Umsetzung bis auf zwei Dinge: der Zeitpunkt der Unterrichtung (T6, liegt bei Pedi) und der Umzug der Unterrichtung nach `docs/compliance/`, ein Dateiumzug ohne Bau. Ursprünglich: **Entschieden: KOMPLETT umsetzen (Pedi, 30.07.) — läuft als mega61 B, E, F, G, H.** Verspätung gegenüber dem 02.08. ist in der Entwicklungsphase ohne echte Kundendaten hinnehmbar, danach soll alles sauber stehen. Einordnung, Pflichten und Umsetzung ausgearbeitet: — `_relay/kopf/RECHT-KI-Verordnung-Umsetzung.md`, elf Abschnitte, am Verordnungstext und am Quelltext belegt. **Drei Ergebnisse:** (1) **Anhang III ist nach heutigem Stand NICHT einschlägig** — kein Hochrisiko, weil die personenbezogenen Flächen keine KI sind (deterministische Aggregate; das Modul importiert nachweislich keinen Reasoner) und die KI-Flächen keine Personen bewerten; die Nicht-Rangfolge ist testgesichert (`service.test.ts:483-487`), die namentliche Fläche liegt hinter einem Merkmalsschalter mit Vorgabe AUS. (2) **Artikel 4 (KI-Kompetenz) gilt seit 02.02.2025** — seit achtzehn Monaten offen, rein organisatorisch, mit zwei Seiten und einem Teilnahmenachweis erfüllbar. (3) **Artikel 50 ist einschlägig:** Absatz 1 Information über die KI-Interaktion, Absatz 2 maschinenlesbare Kennzeichnung — Letzteres je Aufgabe unterschiedlich: `answer`, `interview`, `describe` erfasst, `assist`, `structure`, `extract`, `group`, `select` voraussichtlich als unterstützende Standardbearbeitung ausgenommen. Sanktionsrahmen seit 08/2025 scharf. **Einbau in sechs Schritten in Abschnitt 10; der Eintrittshinweis erfüllt T1 und Artikel 50 in EINER Fläche** | Die einzige Frist mit festem Datum, und sie trifft den Kern des Produkts. Schwerer noch: Artikel 4 ist längst abgelaufen, ohne dass es jemand wusste — die Verordnung war im ganzen Repository nirgends bewertet, `docs/compliance/` kennt sie nicht. Dieses Dokument ist die erste Einschätzung und zugleich der Nachweis, dass die Frage gestellt wurde. |
| T2 | AUSGELIEFERT | LIVE-PRÜFUNG | **Gebaut in mega61 A: beide Seiten ohne Anmeldung erreichbar, Fußlinks überall einschließlich Anmeldemaske, Entwurfsvermerk am Anfang beider Seiten, 96 Schlüssel × 3 Sprachen wörtlich aus dem Entwurf, 18 × `legal.tbd.*` mit Platzhalterwert und einem Sammler über die Bauform — kein erfundener Wert kann durchrutschen, und kein Rechtstext nennt das aufgehobene TMG.** Der Schalter hat auf Pedis Entscheidung **Vorgabe AN**. **Was noch fehlt, kann nur Pedi liefern:** die echten Firmenangaben für die 18 Schlüssel. Solange sie fehlen, steht auf beiden Seiten sichtbar „wird ergänzt" — ehrlich, aber nicht öffentlichkeitsreif. Ursprünglich: **Entschieden: Vorabversion einbauen (Pedi, 30.07.) — läuft als mega61 A.** Zwei Seiten `/impressum` und `/datenschutz`, ohne Anmeldung erreichbar, Fußlinks auf jeder Seite einschließlich Anmeldemaske, alles hinter `KLARWERK_RECHTSSEITEN` mit Vorgabe AUS. Die fehlenden Firmenangaben bekommen eigene Schlüssel mit dem Wert „— wird ergänzt —"; ein Sammler prüft, dass **kein** erfundener Wert drinsteht. Entwurf liegt pro forma fertig: `_relay/kopf/RECHT-Entwuerfe-und-Overview.md`, Leseseite `klarwerk-recht.html` (Skript `recht-seite.py`, eine Quelle wie beim Register). Rechtsgrundlagen recherchiert und aktuell: **§ 5 DDG** (hat am 14.05.2024 § 5 TMG ersetzt — „§ 5 TMG" unter einem Impressum zitiert eine aufgehobene Vorschrift), Art. 13 DSGVO, **§ 25 TDDDG** (bis 05/2024 TTDSG). Jede Tatsachenaussage der Datenschutzerklärung ist am Quelltext belegt, Herkunft in Teil 5. **37 Platzhalter**, eindeutig markiert und gezählt; die Firmendaten kann nur Pedi liefern. **Noch nicht gebaut:** zwei Seiten `/impressum` und `/datenschutz`, die **ohne Anmeldung** erreichbar sind, plus Fußlinks auf jeder Seite einschließlich der Anmeldemaske | Heute gibt es **keine** Rechtsfläche: 22 Seiten in der Routentabelle, keine davon rechtlich. Der Entwurf beseitigt das Risiko für den Fall, dass die Testphase doch als geschäftsmäßiger Dienst gilt — eine Frage, die niemand sicher beantworten kann und die ein vorhandenes Impressum gegenstandslos macht. |
| T1 | AUSGELIEFERT | LIVE-PRÜFUNG | **Gebaut in mega61 B–D, gehärtet in mega62 A–C.** Banner als **Geschwister** der Inhaltsfläche statt `fixed` — er nimmt echten Layout-Platz und verdeckt deshalb kein Bedienelement, ohne Ausgleichs-Polsterung, die irgendwann jemand vergisst; dreimal montiert (breit, schmal, mobil), mobil auf `40vh` gedeckelt, echte `<button>` in der Tabreihenfolge, Haupt- und Nebenaktion durch **Form und Beschriftung** unterschieden statt durch Farbe. Ein Sammler über alle drei Sprachen belegt, dass **kein** Bannertext „Zustimmung", „Einwilligung", „consent" oder „toestemming" enthält — mit Kalibrierung, dass das Muster ein verbotenes Wort überhaupt findet. Kenntnisnahme und Prüfprotokoll sind seit mega62 B **eine Transaktion**, die Ablehnung ist seit mega62 C fail-closed. Ursprünglich: **Entschieden von Pedi (30.07.): Banner UNTEN, wie auf deutschen Seiten — nicht als Sperre.** Läuft als mega61 B–D. Konzept in `_relay/kopf/RECHT-Klaerung-und-Bannerkonzept.md` Teil B. Zwei Knöpfe „Verstanden — weiter" und „Nicht einverstanden"; **die Wörter „Zustimmung" und „Einwilligung" kommen NICHT vor** (Scheineinwilligung), ein Sammler hält das fest. Ort ist die Anwendungshülle, nicht der Torwächter — damit hängt nichts im Anmeldeweg. Vermerk serverseitig am Konto mit Textversion. Bei Ablehnung echtes Abmelden, weil das Cookie zu dem Zeitpunkt schon gesetzt ist. Alles hinter `KLARWERK_HINWEISBANNER`, Vorgabe AUS. Ursprünglicher Befund: **Hinweis auf Browser-Token beim Anmelden.** Konzept liegt: `_relay/kopf/KONZEPT-Login-Hinweis-Browser-Token.md`. Der Befund am Code: genau EIN technisch zwingendes Sitzungscookie (`kw_session`, HttpOnly, SameSite=Lax, 14 Tage, kein JWT, serverseitig nur als Hash), der Anmelde-Token liegt **nicht** im Browser-Speicher, kein Tracking und kein Dritt-Skript — die CSP macht es technisch unmöglich. Im `localStorage` fünfzehn Einträge, alle Komfort, **einer davon (`kw.offlineQueue.v1`) enthält echte Entwurfsinhalte.** Drei Entscheidungen offen: **Bezeichnung** (Kenntnisnahme statt Einwilligung — bei erzwungener Wahl wäre „Zustimmung" eine Scheineinwilligung), **Umfang** (nur Cookie oder auch die Übermittlung an die Cloud-KI), **Rechtsweg** (Kanzlei vorher fragen oder als beschreibenden Hinweis bauen). Einhängepunkt technisch geklärt: `Gate()` in `App.tsx:27-53`. Kopf-Empfehlung: **nicht vor Freitag bauen** — es ist die erste Rechtsfläche des Produkts und liegt genau auf dem Anmeldeweg, den die Testerin benutzt | Es gibt heute **keine** Rechtsfläche: keine Datenschutzerklärung, kein Impressum, kein Häkchen bei der Registrierung. Der Dialog wäre die erste — und ein Cookie-Hinweis ohne Datenschutzerklärung bleibt ein halbes Haus. Zugleich ist das Sitzungscookie der harmloseste Teil: Inhalte gehen serverseitig an Cloud-KI und Transkription. Wer über das Cookie informiert und darüber schweigt, informiert über das Kleine. |
| T4 | ERLEDIGT | — | **Alle sieben Fragen so weit geklärt, wie es ohne Kanzlei geht** — `_relay/kopf/RECHT-Klaerung-und-Bannerkonzept.md` Teil A, je Frage Einordnung, Begründung und benanntes Restrisiko. **Zwei Recherchen haben die Lage verbessert:** die Orientierungshilfe der Aufsichtsbehörden nennt Authentifizierung und Einstellungen ohne Kennung ausdrücklich als einwilligungsfrei — damit ist fast unser ganzer Browserspeicher gedeckt, zwei Grenzfälle bleiben benannt. Und: **Anthropic ist NICHT im EU-US Data Privacy Framework zertifiziert**, die Übermittlung läuft über Standardvertragsklauseln; ein Auftragsverarbeitungsvertrag existiert für kommerzielle Angebote. Handlungsoption: dasselbe Modell über AWS Bedrock in einer EU-Region, AWS ist DPF-zertifiziert. Kanzleiprüfung bleibt vorbehalten. Ursprünglich: **sieben Rechtsfragen, nach Dringlichkeit sortiert** (Teil 6 des Rechtsdokuments). Die schwerste ist strukturell: **sind wir bei den Inhalten unserer Unternehmenskunden Verantwortlicher oder Auftragsverarbeiter?** Davon hängt ab, ob es eine Datenschutzerklärung gibt oder eine Erklärung plus Auftragsverarbeitungsverträge mit jedem Kunden. Dazu: Drittlandgrundlage und AVV mit den KI-Anbietern, Bewertung der Browser-Einstellungen nach § 25 TDDDG, Datenschutzbeauftragter und Folgenabschätzung, Betriebsrat, Barrierefreiheitsstärkungsgesetz, Impressumspflicht in der geschlossenen Testphase. **In `docs/qm/claude-after-report.md` liegt ein altes „Einwilligungs-/Betriebsrats-Paket" und ein Kanzlei-Briefing — nie im Register, vor einem Termin zu sichten** | Der Kopf kann die Texte schreiben, aber nicht die Rechtsfragen entscheiden. Ohne die Rolle gegenüber Kunden ist die Datenschutzerklärung in ihrem wichtigsten Abschnitt geraten statt begründet — und das fällt beim ersten Kundenaudit auf. |
| B27 | ERLEDIGT | — | **Ship 9 ist gefahren UND live: Commit `d0b9e5e`, Coolify Success in 48 Sekunden, Deploy-Commit gegengelesen und identisch (30.07.2026, 13:52–13:53 UTC).** mega61 bis mega66 in einem Zug.** 114 Dateien, 9.402 Einfügungen, 295 Löschungen; die gestagte Liste stimmte Zeile für Zeile mit den 113 aus dem Diff abgeleiteten Pfaden plus `OFFEN.md` überein, alle `M` oder `A`, nichts unter `_relay/`, keine Wegwerfsonde, kein `LOT/`. Push nach **`github`** (`bcf1f15..d0b9e5e`), `github/main` danach als `d0b9e5e` gegengelesen. **Der erste Versuch prallte ab**, weil der lokale Zweig `origin` folgt — dem Gitea mit der fremden Linie; nichts wurde dabei beschädigt (I24, I25). **Was damit live geht:** zwei Rechtsseiten ohne Anmeldung mit Fußlinks überall, der Hinweisbanner im Layoutfluss, der dauerhafte KI-Satz und das maschinenlesbare Feld, die Vertraulichkeitshärtung bis zum Chokepoint, die Kennzeichnung in den Exporten, der Kostenhinweis an allen Auslösestellen, die Autorennamen mit drei Zuständen, AA-Kontrast auf den Pflichtflächen, der Demo-Seed hinter einem fail-closed Tor mit Einmalkennwörtern, die tabübergreifende und fristlose Abmeldeschuld, die bytegenaue Kennwortübergabe und die Unterrichtung nach Artikel 4 in `docs/compliance/` | Sechs Scheiben, sechs Gegenprüfungen, **sechs echte Funde** — drei davon Dinge, die kein Auftrag verlangt hatte, und drei Korrekturen an Aussagen des Kopfes. Kein einziger Befund wurde weich gemacht, weil ein Termin drückte; der Rückfallplan P2 stand von 14 Uhr an bereit und wurde nicht gebraucht. Das ist das eigentliche Ergebnis des Tages: die Kette endete, weil nichts mehr da war. |
| B26 | ERLEDIGT | — | **ben sammel64 zu mega66: GRÜN. „Mega66 ist abnahmefähig. Ship 9 ist aus Code-Review-Sicht frei."** 179 Zeilen, 8.314 Bytes, Inhalts-SHA `1aea5fea…abbe04`, vom Kopf nachgerechnet, NUL-frei kalibriert. Die Terminalübergabe verarbeitet Teil-Schreibvorgänge bytegenau, bricht bei Stillstand oder Ausnahme ehrlich mit `false` ab und meldet Erfolg erst nach Annahme des **vollständigen** Puffers; Vertrag, einziger Produktionsaufrufer und Sammler passen zusammen. Ein nicht blockierender Präzisionshinweis: die Zahlen 45 Bytes und 44 Zeichen stimmen, aber der Schnitt liegt **nicht** mitten im „ö" — das vollständige Zweibytezeichen liegt bereits im 45-Byte-Präfix, und der Unterschied entsteht dadurch, dass es enthalten ist. **Den Fehlschluss hatte der Kopf von der Hand übernommen und weitergegeben** (I22, vierter Fall am selben Tag). Der eigentliche Mehrbyte-Grenzbeweis liegt in A-K7 und bleibt belastbar. **Offene Betriebsvoraussetzung: genau eine** — die manuelle Kennwortrotation der live vorhandenen Demo-Konten, nicht ersetzbar durch Löschen | Sechs Scheiben, sechs Gegenprüfungen, sechs echte Funde — und die Kette endet nicht, weil die Zeit ausgeht, sondern weil nichts mehr da ist. Das ist der Unterschied zwischen einem Ship und einem Abbruch. |
| B25 | AUSGELIEFERT | — | **mega66 gebaut, ein Block, `./tools/check` grün (720 Dateien / 5.364 Fälle / 18 Rauchproben) — liegt bei ben als sammel64.** Der Text wird einmal als `Buffer` erzeugt und in einer Schleife über den **Byte-Offset** geschrieben; nachgeschoben wird nur der Rest, nie der ganze Puffer — **Duplikate sind strukturell unmöglich**, und die Begründung steht dabei, warum weder ein erneuter Versuch am selben Offset (Endlosschleife) noch ein Sprung nach vorn (Lücke im Kennwort) in Frage kommt. Der Vertrag ist auf „alle Bytes wurden von `writeSync` angenommen" **verengt** und steht so im JSDoc; die alte, ab mega66 falsche Zusage „bei `false` wurde NICHTS ausgegeben" ist mitkorrigiert. Der Zweig in `seed.ts` nannte tatsächlich nur eine Fehlrichtung und nennt jetzt beide, samt dem Satz, dass ein abgeschnittener Anfang unbrauchbar ist. **Vom Kopf vollständig nachgerechnet:** Diff `_relay/patches177/mega66.diff` 11.578 Zeilen, 619.712 Bytes, 113 Dateiköpfe, SHA `310129ce…b6b3ee6f`; Bericht 315 Zeilen, Inhalts-SHA `728d136d…dce0860a`; beide NUL-frei kalibriert; verbotene Pfade null; Egress null; HEAD unverändert; keine Sonde im Wurzelverzeichnis; `comm` gegen `patches176` **beidseitig leer** — keine Datei verschwunden, keine neue, was zu einer Ein-Block-Scheibe passt. Fünf Stellen selbst gelesen. **Eine Formabweichung, nicht als Befund geführt:** der Bericht endet mit zwei SHA-Zeilen statt einer; beide Werte stimmen | Der Sammler ersetzt jetzt das **Gerät** statt des Helfers — damit ist die Grenze, die ben benannt hatte, geschlossen statt wiederholt. Und der stärkste Beleg des Tages ist einer, den niemand beauftragt hatte: bei 45 angenommenen Bytes stehen **44** Zeichen am Terminal, weil der Schnitt in das „ö" von „Einmalkennwörter" fällt. Eine Zeichenzählung wäre dort um eins daneben gelegen. Die Pufferregel ist damit gemessen, nicht behauptet. |
| B24 | ERLEDIGT | — | **ben sammel63 zu mega65: ROT mit EINEM engen Restbefund — das ROT aus sammel62 ist inhaltlich aufgelöst.** 254 Zeilen, 11.422 Bytes, Inhalts-SHA `bac07a2d…bba85`, nachgerechnet, NUL-frei kalibriert. Seine eigenen Worte: kein geprüfter Anwendungspfad schreibt Einmalkennwörter in einen Protokollkanal, die falsche Fristzusage ist **vollständig und konsistent** entfernt, und **alle drei Auswege sind einzeln grün** — damit ist die Wegnahme statt des Timers bestätigt, und zwar unabhängig. **Der Restbefund:** `writeSync` liefert die Zahl der tatsächlich geschriebenen Bytes; der Helfer ruft es einmal, ignoriert den Rückgabewert und meldet Erfolg. Ein Teil-Write lässt frisch angelegte Konten mit **unbekannten** Kennwörtern zurück, während der Lauf Erfolg meldet. Der Kanal ist richtig, die Zusage über den Kanal zu weit — dieselbe Fehlerklasse wie ROT-1, eine Ebene tiefer. Läuft als mega66, **ein** Block | Die Kurve zeigt in die richtige Richtung: ROT mit zwei Blockern, dann ROT mit einem, jetzt ROT mit einem engen, der den Serverweg nicht berührt. Das ist Konvergenz, nicht Kreisen. |
| B23 | AUSGELIEFERT | — | **mega65 gebaut, beide Blöcke, `./tools/check` grün — liegt bei ben als sammel63. Beide Blocker sind durch WEGNAHME geschlossen, nicht durch Mechanik.** Block A: neuer Kanal `services/app/src/kennwort-uebergabe.ts` über `/dev/tty` — das kontrollierende Terminal, das eine Umleitung und ein CI-Lauf nicht einfangen. **Die Wahl ist gemessen, nicht angenommen:** in der Werkbank ohne Terminal scheitert das Öffnen mit `ENXIO`, also die richtige Fehlrichtung. Die Datei mit `0600` ist begründet verworfen, weil sie die Zusage wieder an einen Aufräumschritt eines Menschen gehängt hätte — dieselbe Annahme, an der ROT-1 hing. Kein Terminal heißt: **nichts** ausgegeben, Verweis auf den Admin-Kennwortreset, `exitCode = 1`. Block B: der Eintrag ist ein Merker (`"offen"`), `abmeldeschuldGesetzt()` prüft nur noch das Vorhandensein — **kein `JSON.parse`, kein `Date.now()`, `ABMELDESCHULD_DAUER_MS` weg**, und damit ist die Klasse „beschädigter Eintrag" nicht geflickt, sondern verschwunden. Dazu bens GELB-1: ein Einlöseversuch beim Aufbau. **Vom Kopf vollständig nachgerechnet:** Diff `_relay/patches176/mega65.diff` 11.202 Zeilen, 597.900 Bytes, 113 Dateiköpfe, SHA `a23b928e…65bb1f`; Bericht 327 Zeilen, Inhalts-SHA `86ee83b3…1e42d80`, und die Hash-Zeile nennt genau diese beiden; beide NUL-frei kalibriert; verbotene Pfade null; Egress null; HEAD unverändert; `comm` gegen `patches175` ohne Verlust, vier neu. **Fünf Einzelbelege selbst gelesen:** die Kennwortzeilen gehen in **keinem** Zweig nach `console` · auf Codeebene steht in `abmeldeschuld.ts` **keine** Zeitrechnung mehr · die Fristzusage ist in **allen drei** Sprachen weg und steht nur noch als Historienkommentar · die Sperrmeldung benennt jetzt den Wiederholversuch statt einer Frist · und der Knopf `signout-blocked-retry` existiert unverändert | Die Hand hat die Lehre aus ROT-1 auf ihre eigene Lösung angewandt: eine Zusage über einen Kanal wird am Kanal gemessen, nicht an der erwarteten Nutzung. Und sie hat in Block B meinen Weg gebaut statt bens — die Begründung steht im Bericht obenan, wie verlangt. **Die eine Frage, an der die Wegnahme hängt, liegt jetzt bei ben:** trägt der Ausweg? Wenn einer der drei Wege heraus schwächer ist als angenommen, wurde aus einer Höflichkeit eine dauerhafte Aussperrung — dann ist bens ursprüngliche Fixrichtung richtig und meine falsch. |
| B22 | ERLEDIGT | — | **ben sammel62 zu mega64: ROT mit zwei Blockern — beide vom Kopf am Code nachgeprüft und bestätigt.** 261 Zeilen, 12.633 Bytes, Inhalts-SHA `93e53eb5…cfe6dc`, nachgerechnet, NUL-frei kalibriert. **Grün gegeben:** das Demo-Seed-Tor samt Zuschnitt und kalibriertem Test, die Entropie, der Serverweg der Übergabe, die neue Bestandslogik, das Erreichen bereits offener Tabs, die gedrehte Speicher-Aufzählung, der Banner-Pin — und der fail-open gehaltene kalte Start ausdrücklich als „vertretbar und für sich kein Ship-Blocker". **ROT-1:** der CLI-Seed gibt die Einmalkennwörter über `console.warn` nach `stderr` — derselbe Kanal, den derselbe Kommentarblock zwei Zeilen höher richtig als „jeder CI-Mitschnitt, jede Terminalhistorie, jedes Container-Log" beschreibt. Die Unterscheidung „Protokoll gegen Ausgabe" existiert technisch nicht. **ROT-2:** `abmeldeschuldGesetzt()` gibt bei `inDiesemLauf` sofort `true` und liest die Frist **nie**; ein beschädigter Eintrag sperrt ohne Ersatzfrist unbegrenzt — während DE, EN und NL in der Datenschutzerklärung zusagen, der Merker verfalle „spätestens nach vierundzwanzig Stunden von selbst". **Eine falsche Tatsachenaussage in einer Rechtsfläche.** Läuft als mega65, zwei Blöcke | Die dritte Runde hintereinander, in der die Gegenprüfung etwas Echtes findet — und die zweite, in der eine **Korrekturscheibe** neue Fehler einführt. Das ist kein Nachlassen der Hand: beide Befunde sitzen in derselben fummeligen Fläche (ein clientseitiger Merker mit Speicher-Fehlerfällen), gebaut unter Termindruck. Die Antwort darauf ist nicht mehr Sorgfalt, sondern **weniger Mechanik** — deshalb löst mega65 B die Zusage durch Wegnahme ein statt durch einen Timer. |
| B21 | AUSGELIEFERT | — | **mega64 gebaut, alle drei Blöcke, `./tools/check` grün (717 Dateien / 5.344 Fälle / 18 Rauchproben) — liegt bei ben als sammel62.** Block A schließt G11 von der Codeseite: Schalter `KLARWERK_DEMO_SEED` mit **Vorgabe AUS** und der Begründung am Eintrag, dass die Notausschalter-Richtung aus mega61 für Pflichtangaben gilt, „deren stilles **Fehlen** der Schaden ist — hier ist der Schaden die stille **Anwesenheit**". Einmalkennwörter aus `randomBytes`, nur in der Antwort; keine stille Wiederfreigabe und keine Rollenanhebung mehr auf Bestandskonten. Block B ersetzt `signOutLock.ts` durch `abmeldeschuld.ts`: `localStorage`, 24-Stunden-Ablauf, `storage`-Ereignis, Nachholversuch an `online`/`visibilitychange`, Auflösung bei bewiesenem 401 — der Zustand ist als **geschuldete, unbestätigte Abmeldung** modelliert, nicht als Sperre, und löst sich deshalb auf. **Vom Kopf vollständig nachgerechnet:** Diff `_relay/patches175/mega64.diff` 10.314 Zeilen, 551.119 Bytes, 109 Dateien, SHA `360c9df7…dc603a`; Bericht 402 Zeilen, Inhalts-SHA `4def9181…47fb09`, und die Hash-Zeile des Berichts nennt genau diese beiden; beide NUL-frei kalibriert; verbotene Pfade null; HEAD unverändert `bcf1f15`; **Egress null, und die festgeschriebenen Demo-Kennwörter sind als Literal nirgends mehr im Produktcode** — eigens gesucht. Die `comm`-Gegenprobe meldet **eine fehlende Datei, und zwar richtig**: `signOutLock.ts` ist umbenannt, existierte gegenüber der Basis nie als getrackte Datei und kann im kumulativen Diff daher nicht auftauchen — angekündigte Umbenennung, kein stiller Verlust. Fünf Einzelbelege am Endzustand selbst nachgesehen, darunter dass **der Tor-Test das Flag trotz global gesetzter Testumgebung ausdrücklich wieder entfernt** und auch Werte ungleich `1` ablehnt | Zwei begründete Abweichungen von meinem Auftrag, und ich halte beide für richtiger als meine Vorgabe. Das Tor sitzt **nicht** um die Plugin-Registrierung wie beim Confluence-Muster, sondern nur um den POST — sonst hätte es auch den Purge und den lesenden Stand eingeschlossen und dem Betrieb das Werkzeug genommen, mit dem er vorhandene Demodaten loswird: die Sperre hätte den Bestand eingeschlossen statt die Quelle zu schließen. Und beim **kalten Start** wird nicht fail-closed gesperrt, weil `removeItem` im abgeschotteten Speicher ebenfalls wirft und eine Nutzerin dann ohne Ausweg ausgesperrt bliebe. Eine Sperre, aus der es kein Heraus gibt, ist kein Schutz. |
| B20 | ERLEDIGT | — | **ben sammel61 zu mega63: ROT — aber nicht wegen mega63.** 194 Zeilen, 12.171 Bytes, Inhalts-SHA `f7be7da0…74122f`, vom Kopf nachgerechnet, NUL-frei kalibriert; **jede Belegstelle des Sicherheitsbefunds vom Kopf selbst am Code nachgesehen und bestätigt.** **mega63 für sich ist GELB mit EINER Auflage.** Vier von sechs Befunden grün: die vier Autorzustände (der Mengenwächter würde eine erneute Zusammenlegung rot machen, die Kanten „alte Daten plus Refetch-Fehler" und „Fehler plus Pending" sind gepinnt) · die Kontrastkorrektur ist **lokal** und zieht keine helle Fläche mit · der Themenkommentar trifft die Wirklichkeit und **es gibt keinen weiteren seit mega62 falschen Farbkommentar** · und die Frage, auf die es dem Kopf ankam: **es liegt KEINE weitere Testdatei außerhalb aller Einschlussmuster** — vollständig inventarisiert mit Belegen aus drei Konfigurationen. Die Blindstelle war die letzte. **Die eine Auflage (siehe A23):** die Abmeldesperre hält den Reload-Vertrag im selben Tab, ist aber tablokal. **Kopf-Entscheidung: das ROT ist ein Freigabeurteil, kein Inhaltsurteil über den Baum** — der Sicherheitsbefund ist älter als jede Scheibe dieser Woche, und ein Ship von mega61–63 macht ihn weder größer noch kleiner. Aufgehoben wird er nicht durch Nichtshippen, sondern durch Pedis Rotation (G11, Betriebshälfte). Läuft als **mega64**, zwei Blöcke plus einen kleinen; Ship 9 fährt danach mit mega61, mega62, mega63 und mega64 zusammen | Es ist die zweite Runde hintereinander, in der die Gegenprüfung etwas findet, das kein Auftrag verlangt hatte — und diesmal hat sie einen Befund geschärft, den der Kopf selbst eingebracht hatte: der Kopf sah die festen Kennwörter, ben fand die **ungetorte Route**, die sie auf einer laufenden Instanz verteilt. Das ist der Wert der Trennung von Bauen und Prüfen, in einem Satz. |
| B19 | AUSGELIEFERT | — | **mega63 gebaut, alle fünf Blöcke, `./tools/check` grün — liegt bei ben als sammel61.** Bens drei Auflagen sind geschlossen: Sperrmerker in `sessionStorage` (`kw_signout_pending`, eigenes Modul, Anfangswert aus dem Speicher statt aus festem `false`, Rot-zuerst als **Verhaltens**befund) · drei Zustände beim Autorennamen mit einem Wächter, der **vier verschiedene Antworten** verlangt — ohne den blieben zusammengelegte Zweige einzeln grün, genau so war der Befund entstanden · Kontrast über `--kw-brand-300` mit 8,795 auf Nachtblau, hellen Pflichtflächen bleibt der dunklere Token, und der Test **rechnet die Paarung aus der CSS-Datei auf** statt eine Zahl abzuschreiben. Dazu der Datenschutzabsatz zum neuen Browser-Token in drei Sprachen und die Unterrichtung nach `docs/compliance/` mit Versionskopplung an `HINWEIS_TEXT_VERSION`. **Vom Kopf vollständig nachgerechnet:** Diff `_relay/patches174/mega63.diff` 8.072 Zeilen, 431.264 Bytes, SHA `24c56828…5ac37c`, 81 Dateien; Bericht 462 Zeilen, 23.886 Bytes, Inhalts-SHA `559d6395…fa6ad5`; beide NUL-frei bei kalibriertem Prüfer; verbotene Pfade in keinem der 243 Dateiköpfe; `comm` gegen `patches173`: **keine Datei still verschwunden**, acht neu; HEAD unverändert `bcf1f15`, keine Git-Operation; die Unterrichtung in `docs/compliance/` per `cmp` **byte-identisch** mit der Kopfquelle; `--kw-brand-300` war vorher definiert, kein erfundener Ton; und **alle drei Kontrastwerte auf sechs Stellen unabhängig reproduziert** (3,444447 → 3,069501 → 8,795349) bei kalibriertem Rechner (Schwarz/Weiß 21,0, Weiß/Weiß 1,0) | Zwei Funde über den Auftrag hinaus, und der zweite ist der wichtigere: ein Kommentar in der Themendatei behauptete einen Farbwert, der seit mega62 D falsch war — **die stille Mitwanderung, die er bestritt, WAR der Kontrastbefund**; ein Kommentar mit einer Zahl ist eine Zusage, die niemand testet. Und `tmp-probe.test.tsx` lag seit mega61 im Wurzelverzeichnis und **ist nie mitgelaufen**, weil die `include`-Muster die Wurzel nicht abdecken — die vierte Blindstelle unseres Tors in vier Wochen. |
| B18 | ERLEDIGT | — | **ben sammel60 zu mega62: GELB, drei begrenzte Auflagen — kein ROT.** 212 Zeilen, 11.183 Bytes, Inhalts-SHA `ba896416…ee6c74`, vom Kopf nachgerechnet und NUL-frei kalibriert. **Zwei der drei alten Findings sind vollständig geschlossen** (ein Schalter für beide Hinweisflächen, Kontovermerk und Prüfprotokoll atomar — ben hat die Transaktion bis zum `PoolClient` und bis zur Produktionsverdrahtung durchverfolgt), **die Export-Kennzeichnung ist grün** samt der Einordnung, dass Objekt-Exporte kein KI-Etikett tragen: der Add-in-Pfad erzwingt `retrievalOnly` und die Output-Fabrik komponiert deterministisch, ein Etikett wäre dort **sachlich falsch**. Auch die Kostenhinweise grün. **Die drei Auflagen:** der Sperrmerker nach gescheitertem Abmelden übersteht kein Neuladen und die Zusage gilt deshalb nur bis dahin (bens wichtigster Punkt) · „lädt noch" und „fehlgeschlagen" sind beim Autorennamen ein Zustand statt zwei (A22) · Kontrastregression im Marker der modernen Seitenleiste (F27). **Kopf-Entscheidung: nicht mit offenen Auflagen shippen.** Es ist 12:10, der Termin ist morgen 14:00 — für eine Korrekturscheibe ist Zeit, und Punkt 1 betrifft eine Zusage auf genau dem Anmeldeweg, den die Testerin benutzt. Läuft als **mega63**, fünf Blöcke; Ship 9 fährt danach mit mega61, mega62 und mega63 zusammen | bens Urteil in einem Satz: „bedingt auslieferbar", kein Datenverlust- und kein Rechteeskalationsgrund, aber die Auflagen müssten sonst **als offene Ship-Auflagen geführt** werden. Genau das ist der Punkt, an dem dieses Projekt zweimal Zeit verloren hat — eine geführte Auflage ist eine Schuld, die niemand mehr sieht, sobald das Ship grün war. Solange der Termin eine Korrekturscheibe erlaubt, wird sie gefahren. |
| B17 | AUSGELIEFERT | — | **ben sammel60: GELB, drei Auflagen — Einzelheiten in B18.** Ursprünglich: **mega62 gebaut, `./tools/check` grün — liegt bei ben als sammel60.** Neun Blöcke: A bis C bens drei Anmerkungen zu mega61 · D Kontrast auf AA (F27) · E Kennzeichnung in den Exporten (F29) · F Kostenhinweis an allen Auslösestellen (F16) · **G Import-Seite NICHT ERREICHT** (C1) · H der Autorenname (A22) · I die 96 Werkzeugreste. **Vom Kopf vollständig nachgerechnet:** Diff `_relay/patches173/mega62.diff` 6.804 Zeilen, 361.628 Bytes, SHA `6199ccaf…0ff26b`, NUL-frei bei vorher kalibriertem Prüfer; Bericht 345 Zeilen, Inhalts-SHA `e2e25fd4…7e013fc`; verbotene Pfade in keinem Dateikopf; `OFFEN.md` und die Wegwerfsonde wie vereinbart draußen; **Egress-Gegenprobe über alle Plus-Zeilen null Treffer**; Gegenprobe der Dateilisten gegen `patches172` mit `comm`: **keine Datei ist still verschwunden**, vierundzwanzig sind neu. Die 96 `vitest.config.ts.timestamp-*.mjs` sind tatsächlich weg. Auftrag an ben 33 Zeilen, 7.401 Bytes, SHA `a1856a9f…bd6740` | Der Auftrag existiert, um bens drei Anmerkungen **vor** der Auslieferung zu schließen statt danach — und er hat unterwegs drei Dinge gefunden, die niemand beauftragt hatte: der KI-Satz stand außerhalb der Druckfläche, es gab drei Kontrastverstöße statt einem, und die Ursache von A22 war eine dritte, die keine der beiden Hypothesen des Kopfes traf. |
| B16 | AUSGELIEFERT | LIVE-PRÜFUNG | **mega61 gebaut, `./tools/check` komplett grün — erreicht A bis H, M und N, NICHT erreicht I, J, K, L; die Hand hat die vier Lücken selbst benannt statt sie zu verschweigen.** ben sammel59: **GELB** mit drei Anmerkungen, 336 Zeilen, 14.674 Bytes, Inhalts-SHA `475d26b9…9390c8`, vom Kopf nachgerechnet und gegen die SHA-Zeile des Berichts abgeglichen. **Alle drei Anmerkungen sind in mega62 A, B, C geschlossen** — ein Schalter für beide Flächen statt zwei, echte gemeinsame Transaktion plus umgedrehte Schreibreihenfolge, zweiter strenger Abmeldezweig. **Wird NICHT einzeln ausgeliefert:** Ship 9 fährt mega61 und mega62 zusammen (Pedis Bündelregel vom 30.07.), kumulativer Diff `_relay/patches173/mega62.diff`. Von den vier Lücken ist L in mega62 D erledigt und I in mega62 E; **J und K stehen weiter offen** (T6 und C1). Ursprünglich: **mega61 — Recht und Transparenz, VIERZEHN Blöcke gegen `bcf1f15`, Flächen SICHTBAR.** Pedis Entscheidung 30.07.: die Merkmalsschalter kommen mit Vorgabe **AN** und sind ein Notausschalter, keine Sperre. A Rechtsseiten mit ehrlichem Entwurfsvermerk · B Hinweisbanner unten · C Vermerk am Konto · D Ablehnungsfolge mit echtem Abmelden · E dauerhafter KI-Satz an jeder Fläche · F maschinenlesbares Feld · **G der Prüfblock zu T5** · H F16 „ein Klick kostet Geld" · I Export-Kennzeichnung samt F29 · J Unterrichtung nach Artikel 4 · K Import-Seite (C1, Entwurf lag im `queue/`) · L Kontrast auf AA (F27) · M das zweite Tor (G2ci) · N 96 Timestamp-Reste. **Damit fallen zehn Registerpunkte in einem Auftrag zusammen** | Was sichtbar ist, muss sauber sein — deshalb der Entwurfsvermerk auf den Rechtsseiten und der mobile Zuschnitt in den Testpflichten. Block G ist der wichtigste: er entscheidet, ob in der sichtbaren Datenschutzerklärung eine Zusicherung über vertrauliche Inhalte stehen darf. |
| B15 | ERLEDIGT | — | **mega60 ausgeliefert in Ship 8 (`bcf1f15`). ben sammel58: GRÜN ohne Auflage**, 259 Zeilen, 9.423 Bytes, Inhalts-SHA `39957c52…1b8c45`, vom Kopf nachgerechnet, NUL-frei kalibriert; alle sieben Einzelverdikte grün. Der Ship-Blocker war eng wie mega48: Der Kompositumtreffer zählt künftig auf den Suchwert, **nicht** auf die Substanz — beide Zweige, fail-closed. Dazu bens Gegenbeispiel als Negativtafel (`Stand`/`Widerstand`, `Wohlstand`, `Gegenstand`) und die Gegenprobe, dass die Quelle weiterhin **gefunden** wird. Die drei Positiv-Erwartungen in `mega59-komposita.test.ts` werden sichtbar als Rücknahme umgestellt. Kumulativer Diff `patches171` über mega59 **und** mega60, weil Ship 8 beide zusammen ausliefert | ben hat ohne Fehlerinjektion gezeigt, dass „Der Widerstand am Ventil ist vorgeschrieben" die Frage „Welcher Stand gilt am Ventil?" tragen kann — eine sachfremde Quelle trägt eine Antwort. Das ist genau die Eigenschaft, gegen die mega52 bis mega58 gebaut wurden, und sie war in einer einzigen Runde wieder da. |
| S5b | OFFEN | NACH-VORTEST | **Warum eine formale Kompositumregel den Fall nicht lösen kann — die Erkenntnis aus mega59/60, damit sie niemand wiederholt.** `Unterdruck` und `Widerstand` sind morphologisch **identisch** gebaut (Präposition + Substantiv), semantisch aber verschieden: ein Unterdruck **ist** ein Druck, ein Widerstand ist **kein** Stand. Jede Regel aus Zeichenposition und Länge, die den einen trägt, trägt auch den anderen — eine vierte Bedingung verschiebt die Grenze nur. Der tragfähige Weg ist bens Punkt 2: eine **begrenzte, deklarierte und getestete Domänenrelation** echter Fachkomposita, nach dem Muster der 218 Wortklasseneinträge aus mega56 und der 29 mehrdeutigen Formen aus mega57. Bis dahin bleibt `koCarBlau` bei „Welche Farbe müssen Firmenwagen haben?" ohne Trag-Wirkung: die Frage liefert nur `farb` und `firmenwag`, Substanzwert eins | Der zweite Anlauf auf dieselbe Schuld ist gescheitert, und diesmal wissen wir warum — das ist mehr wert als der Fix. Ohne diese Zeile schneidet der nächste Kopf wieder eine Zeichenregel und läuft in dieselbe Wand. **Die Wagenfarbe steht nicht auf Nataschas Aufgabenkarte**, der Termin ist also nicht betroffen. |
| G2ci | ERLEDIGT | — | **Geschlossen in mega61 M, und eleganter als beauftragt.** Code hat nicht die CI angefasst, sondern die drei Typprüfläufe in `npm run build` zusammengezogen — die CI ruft genau das ohnehin auf (`ci.yml:18`), brauchte also keine Änderung, und `tools/build` **ruft** die Definition auf statt sie zu wiederholen. Damit gibt es **eine** Definition des Typtors, dort wo alle sie abholen: `"build": "tsc --noEmit && tsc -p tsconfig.tests-tsx.json && cd apps/web && npx tsc --noEmit"`. Ursprünglich: das Tor hatte zwei Wahrheiten: der Web-Typecheck läuft nur lokal.** bens GELB aus sammel57, P1-Auflage ohne Ship-Blockade. `tools/build` fährt seit mega59 `apps/web && tsc --noEmit`, aber GitHub-CI ruft `npm run build` (`ci.yml:17-19`), und das Wurzelskript ist weiter nur `tsc --noEmit` über `services` und `tests`; der Docker-Build fährt ebenfalls nur `vite build`. Für mega59 liegt ein grüner lokaler `./tools/check` vor, deshalb kein Blocker — aber `npm run check` und CI müssen denselben Web-Typecheck fahren, damit es wieder **genau ein** belastbares Tor gibt | Wir haben in mega59 ein blindes Tor sehend gemacht und dabei ein zweites, blindes daneben stehen gelassen. Ein Tor, das lokal strenger ist als in der CI, erzeugt genau die Sorte „bei mir war es grün", die niemand mehr nachvollziehen kann. |
| G11 | ERLEDIGT | — | **bens ROT in sammel61 — der Befund ist bestätigt und schärfer als vom Kopf gemeldet.** Der CLI-Weg ist sauber abgesichert (`runSeed()` bricht bei `NODE_ENV=production` ohne `SEED_ALLOW_PROD=1` ab, `seed.ts:12-17`) — **die Adminroute `POST /api/admin/demo-seed` ist es nicht:** bedingungslos in **jeder** App registriert (`build-app.ts:1032`), kein Merkmals- und kein Umgebungstor, nur `users.manage`, und sie legt auf einer **bereits eingerichteten** Instanz die freigegebenen Controller- und Expertenkonten mit den festgeschriebenen Kennwörtern an. Der Login unterscheidet Demo-Konten nicht; der Brute-Force-Schutz greift nicht gegen bereits bekannte richtige Zugangsdaten. **Zwei Verschärfungen, die der Kopf beim Nachprüfen selbst gefunden hat:** `ensureDemoUser` rotiert das Kennwort eines vorhandenen Kontos **nicht** — und es **gibt das Konto wieder frei und setzt die Rolle neu** (`seed-demo.ts:194-205`). Wer die Demo-Konten deaktiviert, bekommt sie durch einen Klick auf „Demodaten laden" reaktiviert, samt Controllerrolle: eine stille Rechtevergabe. **Zwei getrennte Hälften.** Betrieb, nur Pedi, sofort: alle Konten unter `@demo.klarwerk` inventarisieren und über den **Admin-Passwortreset** rotieren oder deaktivieren, denn der macht bestehende Sitzungen ungültig (`services/auth/src/service.ts:398-409`). **Beide Hälften sind zu, und der Live-Bestand ist am 30.07. abends im Screenshot belegt.** `admin@demo.klarwerk` **existiert nicht** — bestätigt Pedi und die Nutzerliste; nur `seedDemoForAdmin` ist je gelaufen, und der legt keinen Demo-Admin an. Ein Konto mit Adminrechten und quelltextbekanntem Kennwort hat es live also **nie gegeben**. `carla@demo.klarwerk` (Rolle Controller) und `erik@demo.klarwerk` (Rolle Experte) existieren, wie aus der Seed-Kette abgeleitet — die Ableitung war richtig — und ihre Kennwörter sind rotiert. Die Codehälfte ist mit Ship 9 (`d0b9e5e`) live; die Betriebshälfte hat Pedi am 30.07. abends erledigt — **die Kennwörter der Demo-Konten sind zurückgesetzt**, und der Admin-Kennwortreset macht dabei bestehende Sitzungen ungültig. Damit ist die letzte offene Voraussetzung aus bens sammel61 geschlossen. **Ohne Nebenwirkung auf die Tests:** seit mega64 nehmen die siebzehn Bestandsdateien ihr Kennwort aus der Seed-Antwort statt aus einem festen Wert, und der Seed rotiert vorhandene Konten ausdrücklich nicht — die Rotation von Hand ist also der einzige und der richtige Weg gewesen. Code: **in mega64 A erledigt** — Tor mit Vorgabe AUS um ausschließlich den Anlege-Weg, Einmalkennwörter aus `randomBytes`, keine stille Wiederfreigabe mehr. **Für die Betriebshälfte gilt seit mega64 zusätzlich, und es ändert Pedis Weg:** ein vorhandenes Kennwort wird von der Software ausdrücklich **nicht** rotiert — begründet damit, dass eine Rotation eine Anmeldung ungültig machen würde, die jemand vielleicht gerade benutzt. **Die Rotation bleibt also unvermeidlich Handarbeit.** Und der Auth-Dienst kann ein freigegebenes Konto **gar nicht deaktivieren**: `approveUser` hat kein Gegenstück, das einzige Rückwärts ist `deleteUser`. Der aber löscht nur Nutzer und Sitzungen — **die Wissensobjekte behalten ihre Autoren-ID**. Ein Löschen der beiden Demo-Konten würde damit die Urheberschaft an genau den Objekten verwaisen lassen, mit denen die Testerin morgen arbeitet, und jedes davon zeigte danach den Unbekannt-Rückfall: genau das Bild, das mega62 H und mega63 B beseitigt haben. **Kopf-Empfehlung deshalb eindeutig: rotieren über den Admin-Kennwortreset, nicht löschen** — der Reset macht bestehende Sitzungen ungültig (`services/auth/src/service.ts:398-410`, `sessions.deleteByUser`) und ist in der Admin-Oberfläche vorhanden. Zur Bestätigung an ben in sammel62 gegeben, bevor Pedi es tut. **ben hat die Kennwortwerte bewusst nicht zitiert; das Register tut es ebenso nicht.** Ursprünglich: **Feste Kennwörter für drei Demo-Konten stehen im Quelltext, darunter ein Administrator** (`services/app/src/seed-demo.ts:126,173,179` — `admin@demo.klarwerk`, `carla@demo.klarwerk`, `erik@demo.klarwerk`), dieselben Werte zusätzlich in vielen Testdateien und in einer **Kopie unter `dist/insel/staging/`**. Gefunden am 30.07. beim Egress-Gegenlauf über mega63: zehn Treffer auf das Wort „Kennwort", neun davon harmlos — der Anlass war ein Fehlalarm, der Befund darunter nicht. **Was am Code belegt ist:** `seedDemo` ist auf eine **noch nicht eingerichtete** Instanz begrenzt (`needsSetup()` und leere Wissensbasis), `seedDemoForAdmin` läuft dagegen über eine Administratorroute auf einer **bereits eingerichteten** Instanz und benutzt denselben Nutzer-Seed. **Was aus dem Register folgt:** die Demo-Nutzer existieren live — ihre Namen stehen als Autoren an den am 30.07. live geprüften Objekten (A2, A22), und der Bestandsreset hat die Konten bewusst behalten (E9). **Was NICHT belegt ist und nur Pedi sehen kann:** ob in der Live-Instanz diese Kennwörter noch gesetzt sind und ob `admin@demo.klarwerk` dort überhaupt existiert. Zwei Minuten in der Nutzerverwaltung. Zusätzlich als eigene Frage an ben in sammel61, mit eigener Farbe getrennt vom mega63-Verdikt | Vor Freitag bekommt eine Testerin Zugang, und danach soll ein Großkunde das System sehen. Ein Administratorkonto mit einem Kennwort, das im Quelltext steht, ist kein Schönheitsfehler, sondern der kürzeste Weg an allen Rollen- und Vertraulichkeitsstufen vorbei, die wir diese Woche gebaut haben. Es ist **kein** mega63-Fehler und kein Ship-Blocker — es ist älter als jede Scheibe dieser Woche und wurde nur nie gefragt. Genau deshalb steht es hier: die Frage kostet zwei Minuten, das Nichtfragen kostet im Zweifel den Kunden. |
| A23 | AUSGELIEFERT | — | **In mega65 B durch Wegnahme geschlossen: der Code führt keine Frist, die Texte behaupten keine — in DE, EN und NL.** Ein beschädigter Eintrag ist jetzt schlicht ein vorhandener Merker; die Fehlerklasse ist weg statt geflickt. **Von ben in sammel63 bestätigt: alle drei Auswege einzeln GRÜN** (manueller Wiederholversuch, bewiesener 401, tokenloser 204), die Wegnahme vollständig in Code und Texten. Damit war die Entscheidung richtig. **Eine Anmerkung geht gegen den Kopf:** mein Satz „`signOut({ strict: true })` hat genau einen Aufrufer" steht als Kommentar in `abmeldeschuld.ts:48` und ist **falsch** — es sind zwei, die Ablehnung und der Wiederholknopf. Die tragende Aussage lautet richtig: die Schuld entsteht nur im Ablehnungsablauf, und derselbe strenge Weg wird auf der daraus entstandenen Sperrfläche erneut verwendet. Korrektur in mega66. Ursprünglich: **In mega64 B umgebaut, von ben in sammel62 als ROT-2 zurückgewiesen.** Die Frist wird nicht geführt, aber in drei Sprachen zugesagt. **Kopf-Entscheidung: die Zusage wird durch WEGNAHME eingelöst, nicht durch einen Timer.** Begründung: die Schuld entsteht ausschließlich auf dem Ablehnungsweg — `signOut({ strict: true })` hat genau einen Aufrufer —, und für jemanden, der „Nicht einverstanden" gedrückt hat, ist „gesperrt bis zur Bestätigung" der Zweck und keine Härte. Das Ventil gegen dauerhaftes Aussperren existiert bereits dreifach und ist von ben grün geprüft: bestätigter manueller Wiederholversuch, bewiesener 401, und die Abmelderoute nimmt einen tokenlosen Aufruf mit 204 an. Ein Eintrag **ohne** Frist macht das Auslesen von `bis` und die Sonderbehandlung beschädigter Einträge überflüssig — die Fehlerklasse ist damit weg statt geflickt, und der Text sagt die Wirklichkeit. Ursprünglich: **Umgebaut in mega64 B, und zwar nach der Form statt nach der Sperre:** der Zustand heißt jetzt „eine Abmeldung ist dem Server geschuldet und noch nicht bestätigt", gilt tabübergreifend und **löst sich auf**, sobald der Server erreichbar ist. Bleibende Kante, ausdrücklich benannt: wer den Browserspeicher abschottet, kann die Schuld durch ein Neuladen umgehen — fail-closed wäre dort eine Sperre ohne Heraus, weil auch das Löschen wirft. **Ob das eine Ship-Auflage oder ein Registereintrag ist, entscheidet ben in sammel62.** Ursprünglich: **Die Abmeldesperre nach abgelehntem Hinweis gilt nur für einen Tab — und die sichtbare Meldung verspricht mehr.** bens Auflage aus sammel61, läuft als mega64 B. Der Reload-Vertrag im selben Tab ist von ben **grün** bestätigt, auch das Feststecken ist gelöst (die Logout-Route nimmt einen tokenlosen Aufruf mit 204 an, es gibt einen Ausweg). Die Lücke ist der Zuschnitt: `sessionStorage` ist tablokal, ein **bereits offener** zweiter Tab zeigt mit demselben Cookie weiter geschützte Inhalte, und das Schließen des gesperrten Tabs entfernt den Merker, obwohl der Server nie bestätigt hat. Dazu bens Zusatzkante: verweigert der Speicher, fällt der Leser **fail-open** — bei einer Sperre die falsche Richtung. **Der Teil, der wirklich zählt:** die Datenschutzerklärung nennt die Tab-Grenze ehrlich, die **sichtbare Sperrmeldung** sagt unqualifiziert, es würden keine Inhalte gezeigt. Zwei Texte im selben Produkt versprechen verschieden viel, und der weitergehende steht dort, wo der Nutzer ihn liest. **Kopf-Vorgabe für den Bau:** nicht „gesperrt", sondern **„eine Abmeldung ist dem Server geschuldet und noch nicht bestätigt"** — daraus folgt die Tabübergreifung von selbst, und der Zustand **löst sich auf**, sobald der Server erreichbar ist, statt eine Sackgasse zu sein | Es ist die erste Zusage einer Rechtsfläche dieses Produkts, und sie steht auf dem Anmeldeweg, den die Testerin morgen benutzt. Eine Sperre, die ein zweiter Tab umgeht, ist keine Sperre; ein Text, der mehr verspricht als das Verhalten hält, ist der Anfang genau der Sorte Zusage, gegen die diese Woche gebaut wurde. |
| A22 | AUSGELIEFERT | LIVE-PRÜFUNG | **Behoben in mega62 H — und die Ursache war eine dritte, die keine der beiden Kopf-Hypothesen traf.** Beide sind widerlegt: die Nutzerliste liefert die Demo-Nutzer mit (**kein** Statusfilter in Route, Dienst oder Repository, dreifach belegt, der Seed gibt frei), und ein von einem Purge gelöschter Autor ließ sich nicht reproduzieren. **Der wirkliche Grund ist ein Ladezustand, kein Datenfehler:** alle sechs Flächen schrieben dieselbe Zeile `dir.data?.find(...)?.name`, und war die Verzeichnisabfrage noch unterwegs **oder fehlgeschlagen**, sagte jede Fläche gleichzeitig „Unbekannte Person" über **jeden** Autor. Behoben mit der Entscheidung an **einer** Stelle statt an sechs — **aber nur mit zwei Zuständen, nicht mit drei.** Das hatte der Kopf hier zunächst falsch eingetragen, ben hat es in sammel60 gefunden: `useDirectory` liefert den vollständigen Abfragezustand, der neue Haken reicht aber nur `directory.data` weiter — „lädt noch" und „fehlgeschlagen" fallen beide auf `undefined` und damit auf **denselben** Text, und der Test bestätigt die Zusammenlegung sogar ausdrücklich. **Auflage, läuft als mega63 B:** `isPending` und `isError` an den zentralen Auflöser, drei getrennte Texte in DE/EN/NL, vier Fälle einzeln getestet. Der ehrliche unterscheidbare Rückfall für einen wirklich fehlenden Eintrag bleibt. Der ursprüngliche Rückfalltext bleibt für den echten Fall erhalten; er war richtig. **Wirkt erst nach Ship 9 und dem Live-Laden.** Ursprünglich: **Kein Autorenname wird aufgelöst — überall steht „Unbekannte Person (83e361)".** Am 30.07. im Live-Screenshot gesehen, an beiden neuen Lieferanten-Objekten. Vom Kopf am Code geprüft, und zwar mit Gegenprobe: der Endpunkt `/api/directory` **existiert** (`services/auth/src/routes.ts:571`), liefert Kennung und Namen für **jeden angemeldeten Nutzer ohne Adminrecht**, und der Seed setzt echte Namen (`ensureDemoUser` → „Erik Experte", „Carla Controller", `seed-demo.ts:171-179`). Der Rückfalltext selbst ist gewollt und gut begründet (mega51 F2, `apps/web/src/lib/koAuthor.ts` — eine UUID ist kein Name, und zwei Unbekannte dürfen nicht wie einer aussehen). **Zwischen Seed und Anzeige liegt eine Lücke; zwei Hypothesen: entweder liefert `listUsers()` die Demo-Nutzer nicht mit (etwa weil sie als wartend gelten), oder das Objekt zeigt auf einen Autor, den ein früherer Purge gelöscht hat.** Vom Repo aus nicht entscheidbar — Pedi sieht in der Nutzerverwaltung in Sekunden, ob die beiden Namen dort stehen | Das Vermächtnis-Framing ist eine Kernzusage des Produkts: der Autor soll überall am Wissensobjekt sichtbar sein (SCRUM-70). Steht dort bei **jedem** Objekt „Unbekannte Person", ist die Zusage faktisch tot, und die Testerin liest es als unfertige Software — obwohl die Logik dahinter richtig ist. Trifft laut Kommentar sechs Flächen: Bibliothek, Objektdetail, Aufgaben, Fragen, Risiko, Validierung. |
| E9 | ERLEDIGT | — | **Bestandsreset gefahren, Papierkorb leer** — am 30.07. im Screenshot belegt („Der Papierkorb ist leer"). Damit ist der Entwicklungsschutt weg, den die Testerin sonst als „euer Wissen" gesehen hätte |
| T5 | AUSGELIEFERT | LIVE-PRÜFUNG | **Geschlossen in mega61 Block G: der Vertraulichkeits-Parameter und ein Laufkontext sind bis zum Chokepoint durchgezogen** (`services/reasoner/src/service.ts`, `answer(question, context, locale, confidential = false, runContext?)`) — damit darf in der sichtbaren Datenschutzerklärung eine Aussage über vertrauliche Inhalte stehen, und der Datensatz der häufigsten KI-Handlung trägt jetzt Handelnden und Gegenstand. **Die Verschärfung des Kopfes war falsch, und sie bleibt hier stehen, weil ein Register, das eigene Fehler tilgt, nichts mehr wert ist:** der Kopf hatte geschrieben, `services/ask/src/service.ts` enthalte „keinen einzigen Treffer" auf `confidential`, und daraus geschlossen, die Ask-Route filtere nicht. Die Sperre heißt `dropConfidential` — **großes C**, unsichtbar für ein kleingeschriebenes `grep`. Der Weg war **zu**. Regel daraus in I18 und im Verfahren §5: ein Befund, der auf einem Nulltreffer steht, braucht eine Kalibrierung, genau wie der NUL-Prüfer. Ursprünglich: **die Vertraulichkeitssperre greift im Antwortweg möglicherweise nicht.** Ursprünglich: **die Vertraulichkeitssperre greift im Antwortweg möglicherweise nicht.** `services/reasoner/src/service.ts:994-1000`: `answer(...)` ruft `runTask("answer", locale, ...)` und übergibt den Vertraulichkeits-Parameter **nicht** — er bleibt `false`. Auf Reasoner-Ebene wird die Cloud für die Aufgabe „Frage beantworten" damit nie wegen Vertraulichkeit ausgeschlossen. Das Sicherheitsnetz am Chokepoint (`ConfidentialEgressError`, `model-concurrency.ts:174-197`) greift weiter, und ob die Ask-Route vertrauliche Objekte vorher aus dem Kontext filtert, ist **ungeprüft** — genau das ist zu klären. Nebenbefund derselben Zeile: `answer` übergibt keinen `ModelRunContext`, der Datensatz der häufigsten KI-Handlung trägt also weder Handelnden noch Gegenstand | Solange das offen ist, darf die Datenschutzerklärung **nicht** zusichern, dass vertrauliche Inhalte nie an ein Cloud-Modell gelangen — und genau diese Zusicherung würde ein Kunde als erste verlangen. Ein Produktbefund, der eine Rechtsaussage blockiert. |
| S5 | OFFEN | NACH-VORTEST | **Zweiter Anlauf gescheitert, Erkenntnis in S5b — der tragfähige Weg ist eine deklarierte Domänenrelation.** Ursprünglich: **Komposita werden nicht zerlegt — und das kostet eine validierte Quelle.** Bei „Welche Farbe müssen Firmenwagen haben?" trägt nur noch die **offene** Quelle `koCarRot` („in der Farbe Rot"), während die validierte `koCarBlau` („Firmenwagen: Pflichtfarbe Blau") wegfällt: „Farbe" steckt dort nur im Kompositum. Das Modalverb „müssen" hatte die Lücke bisher zufällig zugedeckt. Vom Kopf am Bestand nachgesehen | Nicht repariert, und zwar bewusst: ein Test darf nicht dadurch grün werden, dass man die Prüfdaten passend schreibt, und jede Messung dieser Woche wäre danach nicht mehr vergleichbar. Auf der Aufgabenkarte steht die Wagenfarbe nicht unter den Beispielfragen. |
| S4 | BEAUFTRAGT | SOFORT | **Zwei widersprüchliche „Nächster Schritt" auf einem Bildschirm.** `ask.contract.gap.next` (`i18n.ts:1836`) nennt das kostenlose Umformulieren, `ask.gapNext` (`:1783`) das Risiko-Board — beide in DE/EN/NL. Mobile kennt den kostenlosen Schritt gar nicht. Läuft als mega54 Block E | Genau in dem Moment, in dem die Testerin nicht weiterkommt, bekommt sie zwei verschiedene Anweisungen. Der teure Weg steht dabei prominenter als der kostenlose. |
| S1 | ERLEDIGT | — | **Geschlossen in Ship 8 (mega59 H), von ben GRÜN.** Der Kopf hat `tests-smoke/` vollständig inventarisiert und **fünf** hohle Fälle gefunden, nicht einen; alle fünf sind auf strukturelle Anker umgehängt, jeder zuerst rot gezeigt. Neu dazu eine Anfragezähler-Hilfe (`tests-smoke/support/anfragezaehler.ts`) — die Messung, die den Befund damals unmittelbar gefangen hätte. Ursprünglich: **der Fall „Fragen antwortet ehrlich" hat nie einen `/api/ask`-Aufruf ausgelöst** — grün allein durch einen statischen Einleitungstext, an der Playwright-Spur belegt | Ein grüner Test, der nichts prüft, ist schlimmer als kein Test: er verhindert, dass jemand hinsieht. Solange ungeklärt ist, ob weitere Fälle so gebaut sind, wissen wir nicht, was unser Tor wirklich deckt. |
| S2 | OFFEN | NACH-VORTEST | „klep" findet „Ventil" nicht: literaler Token-Schnitt und `ILIKE`, keine Synonyme, keine Übersetzung, keine Embeddings. mega54 deckt das ausdrücklich **nicht** ab | Für einen deutschen Betrieb ist das heute egal. Für den Großkunden mit niederländischen Standorten ist es der Unterschied zwischen „funktioniert" und „funktioniert bei uns nicht". |
| F21 | ERLEDIGT | — | **Zitierte gegen herangezogene Quellen** — geschlossen in mega52/53, ausgeliefert in Ship 6. ben sammel51: ROT-2 geschlossen | Vorher stammten Klasse und Vertrauenswert von der ersten angesehenen Quelle, auch wenn die Antwort eine ganz andere zitierte. Die Zahl, auf die sich das Produkt beruft, war damit falsch. |
| W1 | ERLEDIGT | — | **Antwort zog fachfremde offene Quellen heran** (P0, Handlauf 2) — geschlossen in mega52 und mega53. ben sammel51: ROT-1 geschlossen | Ein einziges gemeinsames Wort genügte, damit eine fachfremde Quelle eine Antwort trug — bei zugesichertem „nur validiertes Wissen". |
| W2 | ERLEDIGT | — | Ausgabesprache: der Antwort-Prompt schwieg dazu, alles Nicht-Englische wurde auf Deutsch abgebildet — geschlossen in mega52. **Der zweite Teil bleibt offen und heißt S2** | |
| A20 | ERLEDIGT | — | Der Antwort-Beleg band ALLE herangezogenen Quellen — ein „Hat geholfen" gab jedem bloß angesehenen Objekt ein Vertrauensplus. Geschlossen in mega52 A4 | Jede Daumen-hoch-Rückmeldung hat den Vertrauenswert unbeteiligter Objekte gehoben. Über Wochen hätte das die Rangfolge des ganzen Bestandes verzerrt. |
| F22 | ERLEDIGT | — | Trust → Vertrauenswert: 48 Schlüssel DE, 18 EN, 17 NL. Von ben in sammel50 gegengerechnet. Englisch bleibt unangetastet, „Trust" ist dort ein normales Wort | Halb übersetzt ist schlechter als gar nicht: der Nutzer hält zwei Wörter für zwei verschiedene Dinge. |
| F28 | ERLEDIGT | — | `lib.trustNone`/`lib.trustNoneHint` verwaist und von der alten Schlüsselliste gepinnt — mit F22 erledigt | |
| D3a | OFFEN | MIT-DESIGN | Design v2: E1 Werkbank-Richtung ja · E2 Klara-Prototyp ja · E3 Reihenfolge D1→D2→D3→D4 ja | Der heutige Eindruck ist generisch. Beim Großkunden entscheidet der erste Blick mit darüber, ob man uns für ein Industriewerkzeug oder eine Notiz-App hält. |
| F27 | AUSGELIEFERT | LIVE-PRÜFUNG | **Erledigt in mega62 D — und es waren DREI Verstöße, nicht der eine beauftragte.** In mega61 war L nicht erreicht; die Hand hat die Lücke ausdrücklich als Lücke gemeldet, statt eine ungerechnete Zahl zu nennen. Jetzt nach WCAG-Formel gerechnet — **und ben hat in sammel60 zwei Dinge daran korrigiert, beide berechtigt.** Erstens: `funke-soft` wird im heutigen Produktcode **nirgends als Fläche verwendet**, die Rechnung stimmt, aber es ist keine aktuell gerenderte Pflichtfläche; die anderen beiden tragen den Block allein. Zweitens, der Fund: die globale Verdunkelung von `brand-text` zieht eine reale **dunkle** Fläche mit — der `·2`-Marker der modernen Seitenleiste steht auf `--kw-night` ohne lokalen Override und **sinkt von 3,444 auf 3,069**. Dazu die tiefere Lücke: der Vollständigkeitssammler bildet nur **Mengen** von Tokens, nicht deren **DOM-Paarungen**, eine falsche Kreuzkombination bekannter Tokens bliebe grün. **Auflage, läuft als mega63 C.** Die drei gerechneten Paarungen, von ben unabhängig nachgerechnet und bestätigt: Markentext auf weicher Markenfläche **4,497 → 5,046**, gedämpfter Text auf der **Bannerfläche selbst** 4,448 → 4,788, Warntext des Entwurfsvermerks 4,201 → 4,760 (auf der Seitenfläche 4,294 → 4,865). Testbeleg `tests/legal/mega62-kontrast-pflichtflaechen.test.ts`, 23 Fälle, **Werte gerechnet statt eingetragen**, mit Kalibrierung (Schwarz auf Weiß = 21, Weiß auf Weiß = 1) und einem Sammler, der jede Farbklasse der Rechtsflächen in der Paarungsliste verlangt — ein neues, ungemessenes Token wird rot. Rot-zuerst mit allen vier Ausgangswerten gezeigt, zwei bestehende Pins nachgeführt statt gelockert, Klara in Word folgt dem Thema. **Von ben in sammel60 nachzurechnen.** Ursprünglich: **Läuft als mega61 L** — gehört in denselben Auftrag, weil Artikel 50 Absatz 5 ausdrücklich verlangt, dass Transparenzhinweise die Barrierefreiheitsanforderungen erfüllen. Markentext einen Tick dunkler (4,497:1 → AA), damit `--kw-funke-soft` nutzbar wird; Klara in Word muss dem gewählten Thema folgen | Knapp unter AA ist keine Geschmacksfrage, sondern in Ausschreibungen ein Ausschlusskriterium. |
| F16 | AUSGELIEFERT | LIVE-PRÜFUNG | **Erledigt in mega61 H und in mega62 F auf alle Auslösestellen erweitert** — sechs weitere Stellen samt einem rechnenden Sammler, der eine neue Auslösestelle findet, statt sie zu übersehen. Ursprünglich: **Läuft als mega61 H.** Der Hinweis existiert halb: „echte Anfrage" steht in der Oberfläche, **„kostenpflichtig" nur im Codekommentar** — ein Kommentar ist keine Aussage an den Nutzer. Beispielklick auf `/fragen` vorher als echten, kostenpflichtigen Aufruf kenntlich machen | Ein Klick, der Geld kostet, ohne es vorher zu sagen, ist genau die Sorte Unehrlichkeit, gegen die das ganze Produkt argumentiert. |
| W3 | OFFEN | NACH-VORTEST | Word übergibt Bilder nicht — der Verlust wird ehrlich gemeldet, die Bilder fehlen trotzdem | In Arbeitsanweisungen steckt das Wesentliche oft im Bild. Ehrlich zu melden, dass es fehlt, ist besser als zu schweigen, aber es ersetzt das Bild nicht. |
| E10 | OFFEN | NACH-VORTEST | **Entschieden: Umgebungsvariablen in Coolify.** Kein Feld der Oberfläche nimmt je ein Geheimnis entgegen; der Admin-Bereich zeigt nur Zustand und nennt den Variablennamen | Ein Eingabefeld für Schlüssel in der Oberfläche wäre eine dauerhafte Angriffsfläche und ein Prüfbefund bei jedem Sicherheits-Audit. |
| B28 | ENTSCHIEDEN | NACH-VORTEST | **ben sammel65 zu mega67: ROT mit drei Auflagen — und keine davon drängt, weil vor dem Vortest nichts ausgeliefert wird.** 259 Zeilen, 12.598 Bytes, Inhalts-SHA `a921fd6c…693ebc`, vom Kopf nachgerechnet, NUL-frei kalibriert. **Grün:** die Sicherheitsgrenzen der Confluence-Auskunft (kein Geheimnis, kein Egress), die mega32-Wege A/B sind wirklich vorhanden und durch aussagekräftige Mounted-Tests gehalten, der Artikel-50-Satz bleibt unbedingt, die QueryClient-Korrektur sitzt auf der richtigen Höhe. **Die drei Auflagen:** **(1)** die **Fragenfläche umgeht die neue Bedingung** — `Ask.tsx` zeigt seinen alten Kostenhinweis weiter unbedingt und setzt denselben Wortlaut als `title` auf jeden Beispiel-Chip; die Alt-Tests halten genau dieses Verhalten fest. Ausgerechnet die prominenteste Auslösestelle. **(2)** `billable` ist **keine Abrechnungstatsache**: es sagt „die Cloud kann für diese Aufgabe verwendet werden" — `unverified` gilt vorsorglich als `true`, vertraulicher Text nimmt die Cloud am Chokepoint aus der Kette, und ein Laufzeitfehler mit lokalem Rückfall erzeugt auch keine abrechenbare Antwort. Der Wortlaut „startet eine echte, kostenpflichtige Anfrage" ist dafür **zu absolut**; bens kleinerer Weg ist, den Text ehrlich als **Möglichkeit** zu benennen. **(3)** der **Vierer-Vertrag der Zugangsansicht ist in der echten Fläche nur dreifach** — `not-built` ist unerreichbar, weil die einzige Aufrufstelle fest `active` übergibt, und der Test mit dem Titel „die vier Texte" erzeugt auch nur drei. **Testname und Bauzusage sind stärker als der Beleg.** Läuft als eigene Scheibe nach dem Vortest | Zwei der drei Auflagen sind dieselbe Familie wie der ganze Tag: ein Text, der mehr behauptet, als der Zustand hergibt — einmal beim Preis, einmal bei der Zahl der Zustände. Und die erste zeigt, warum eine zentrale Ableitung nichts nützt, solange eine Fläche sie umgeht: **die Zusage gilt nicht dort, wo sie gebaut ist, sondern dort, wo der Nutzer klickt.** |
| C1 | GEPRÜFT | NACH-VORTEST | **mega67 gebaut, `./tools/check` grün (724 Dateien / 5.394 Fälle / 18 Rauchproben, davon 30 Fälle neu) — liegt bei ben als sammel65. Wird erst NACH dem Vortest ausgeliefert.** **Der Abgleich hat die Scheibe halbiert:** vier Siebtel des Entwurfs waren bereits in **mega32** gebaut, unter anderen Buchstaben — die Hand hat das nicht geglaubt, sondern vier Bestandstests laufen lassen (23/23 grün). Alle fünf genannten Stellen sind auffindbar, sehen nur anders aus, **weil sie repariert wurden**; `ImportExplore.tsx:307` trägt heute eine Grabinschrift auf genau den DOM-Durchgriff, den der Entwurf als Falle B3 beschreibt. Echte Arbeit waren damit **C, D und G** — genau die Blöcke, die mega32 mit der Begründung liegen ließ, ihnen fehle eine Datenquelle. **Vom Kopf nachgerechnet:** Diff 1.785 Zeilen, 94.168 Bytes, 27 Dateien, SHA `3bb77b39…bc8571`; Bericht 344 Zeilen, Inhalts-SHA `73591e4c…908aae`; NUL-frei kalibriert; verbotene Pfade null; HEAD unverändert; Egress **ein** Treffer, harmlos (ein erfundener Beispielwert `firma.atlassian.net`). **Und die Kernzusage mechanisch geprüft statt geglaubt:** außerhalb der Kommentare kein `<input>`, kein `<form>`, kein `<textarea>`, keine Punktmaske — meine erste Suche hatte zwei Treffer, beide in der Prosa, die erklärt, dass es sie nicht gibt (fünfter unkalibrierter Griff an einem Tag). Ursprünglich: **Läuft als mega67, an erster Stelle — wie nach Ship 9 versprochen.** Sieben Blöcke: „In Planung" einklappen · die Quellenwahl mit der DOM-Falle B3 · der Zustand je System **ohne jedes Eingabefeld** · vier benannte Freigabezustände · der Kostenhinweis an seine Bedingung gebunden (F30) · dazu die zwei Reste aus dem zurückgezogenen mega30. **Der Auftrag beginnt mit einem Abgleich:** der Entwurf stammt aus der mega31-Zeit und nennt fünf Stellen mit Datei:Zeile — jede wird gegen den heutigen Baum geprüft, bevor eine Zeile gebaut wird, und jede Abweichung ist ein Befund. **Wird VOR dem Vortest NICHT ausgeliefert:** gebaut heute Abend, gegengeprüft morgen früh in Ruhe, ausgeliefert danach. Ursprünglich: **Zweimal nicht erreicht, beide Male mit derselben ehrlichen Begründung — mega61 K und mega62 G.** Der Entwurf verlangt ausdrücklich, jeden Schritt **gegen den heutigen Code** zu prüfen statt ihn anzunehmen: vier inhaltliche Punkte mit je eigener Gegenprobe (Planungszustand, Quellenwahl, Freigaben, Konfliktsatz und Deckelkommentar). Beide Male war nach den übrigen Blöcken nicht genug übrig, um es ehrlich zu machen; der Entwurf liegt **unangetastet**, nichts ist halb angefasst. **Der Kopf hält beides für richtig entschieden** — eine halb gebaute Zugangsfläche ist schlimmer als keine. Bekommt nach Ship 9 einen eigenen Auftrag, in dem sie nicht die letzte Position hat. Ursprünglich: **Läuft als mega61 K** — der fertige Entwurf lag seit Tagen in `_relay/hand/queue/ENTWURF-importseite-zugaenge.md` und wird jetzt abgearbeitet. Import-Seite: „In Planung" einklappen · Quellenwahl wählt wirklich · Freigaben ehrlich · Konfliktsatz · Deckelkommentar. Auftrag liegt fertig, Block C ist seit E10 vollständig | Der Import ist der Weg, auf dem Wissen überhaupt hereinkommt. Eine Seite, die Geplantes wie Vorhandenes aussehen lässt, verspricht dem Kunden Funktionen, die es nicht gibt. |
| C3 | ERLEDIGT | — | **Geschlossen in Ship 8.** Der Kopf hat am Code nachgesehen: drei der vier Befunde waren bereits zu; die fünf Restlücken sind in mega59 D–F gebaut, ben sammel57 hat D, E und F je GRÜN gegeben. Ursprünglich: vier Befunde aus Pedis Live-Durchlauf (Stufe-2-Schalter vergisst sich · „OHNE KI GRUPPIERT" trotz aktivem Reasoner · Such-Leerzustand · rohe Kennung statt Titel) — jeder einzeln am heutigen Code nachsehen | Alle vier sind vom Chef selbst im Betrieb gesehen worden. „OHNE KI GRUPPIERT" bei laufender KI ist dabei kein Schönheitsfehler, sondern eine falsche Zustandsaussage. |
| F-Erstnutzer | ERLEDIGT | — | **mega51 ausgeliefert — Ship 5, Commit `84060d2`.** Handlauf 1 grün (49/0), ben sammel49 GRÜN — die ersten zehn Minuten einer Fremden, acht Befunde am Code als geschlossen belegt | |
| F23, F25, F26 | OFFEN | NACH-VORTEST | Prüfwerkzeug-Reste: `apps/web`-tsc ins Tor · Validierungs-Interaktionstest · Computed-Style-Pin · Kopfzeilen-Chips, eine Handlung je Fläche, Zahlen mit Bezug, Rohwerte, „gesichert"-Kollision, Re-Import zu prominent, Audit-Kette, Vertraulichkeits-Facette, Anlagenkopplung, sechs Routen ohne Live-Beleg | Der eigene `tsc` von `apps/web` läuft heute nicht im Tor — Typfehler in der Weboberfläche können also durchrutschen, während wir „komplett grün" melden. |
| F29 | AUSGELIEFERT | LIVE-PRÜFUNG | **Erledigt in mega62 E — und der Auftrag des Kopfes ging von etwas aus, das es nicht gibt.** Der Kopf hatte Dokumenteigenschaften in Word- und PowerPoint-Dateien und Metadaten in PDF verlangt; **es gibt im Repository keinen Word-, PowerPoint- oder PDF-Erzeuger**, die gleichnamigen Dateien sind Import-Extraktoren. Die Hand hat die real vorhandenen Wege gebaut — Markdown-Kopfblock, Quellenunterscheidung, Druckweg — und dabei einen echten Fund behoben: **der KI-Satz stand außerhalb der Druckfläche**, wäre auf Papier also nicht erschienen. Ihre Einordnung: Objekt-Exporte bekommen bewusst kein KI-Etikett, weil sie kein KI-Erzeugnis enthalten — **das prüft ben in sammel60**, denn es ist eine Rechtsaussage in Codeform. Ursprünglich: **Läuft als mega61 G, zusammen mit der KI-Kennzeichnung in Exporten — dieselbe Fläche, einmal anfassen statt zweimal.** Mobile und Markdown-Export markieren die Quellenliste nicht so deutlich in „tragend" gegen „nur konsultiert" wie der Desktop (ben sammel51, färbt nicht) | Wer die Antwort exportiert und weitergibt, verliert genau die Unterscheidung, die mega52 und mega53 hergestellt haben. |
| E2g | OFFEN | MIT-GROSSKUNDE | 12.08. **bestätigt** — elf Vorgänge leben wieder: Cloud-Instanz, Confluence in Scheibchen, Microsoft-365-Anmeldung, Spracheingabe, Demo-Drehbuch, Nullschulungs-Test | Zwei Wochen nach dem Vortest, und mehrere dieser Punkte brauchen Vorlauf beim Kunden — Anmeldung und Cloud-Instanz lassen sich nicht am Vortag erledigen. |

---

## 5 — Zugriffsschutz

Pedis Anordnung: „nach Freitag, aber nicht vergessen."

| Kennung | Zustand | Anker | Sache | Warum |
|---|---|---|---|---|
| G1 | ENTSCHEIDUNG | MIT-ZUGRIFFSSCHUTZ | **Bauplan liegt: `_relay/kopf/ZUGRIFFSSCHUTZ-Bauplan.md` (30.07. nachts, alles am Quelltext belegt).** **Diese Registerzeile war ungenau, und das ist der wichtigste Befund:** es gibt heute **zwei Welten**. Die **Egress-Wege setzen durch** — Bibliotheks-Export (`library-routes.ts:168-172`), Herkunftskette (`provenance-routes.ts:78-97`), Nachbarschaft (`library-routes.ts:381-389`), KI-Wege über `dropConfidential` —, alle nach derselben SCRUM-506-Regel `can(role,"ko.validate")`. Der **Hauptlesepfad setzt nichts durch**: `GET /api/kos/:id` (`ko-routes.ts:334-345`) fordert `ko.read` und gibt danach alles heraus; der Code vermerkt es selbst als „die ehrliche Grenze aus mega45". **Eine Freigabestufe je Nutzer existiert nicht** — die Rechtematrix kennt vier Rollen und sechs Rechte, keines ist eine Stufe. **Zu entscheiden: Variante A** (die vorhandene Rollenregel systemisch auf alle Lesewege ziehen, plus Autor-Ausnahme wie beim Löschen in `ko-routes.ts:1019`; eine Scheibe, kein Datenmodell-Eingriff) **oder Variante B** (Freigabestufe je Nutzer; neues Feld, Verwaltungsfläche, Wanderung, zweite Achse neben der Rolle — ein Vorhaben, kein Slice). **Kopf-Empfehlung: A jetzt, B als Frage an den Großkunden.** A schließt das Loch vollständig; B macht den Schutz feiner, nicht dichter, und ein feinerer Schutz auf einem offenen Loch ist die falsche Reihenfolge. **Ehrlich dagegen:** nach A sieht ein Experte, der nicht Autor ist, kein vertrauliches Objekt mehr. Ist das im Betrieb der Normalfall, ist A falsch — das kann nur Pedi wissen | Ein Schutz, der am Hinausgehen greift und am Hinsehen nicht, ist schwerer zu erkennen als gar keiner. Die halbe Wahrheit sieht aus wie die ganze. |
| G2 | OFFEN | MIT-ZUGRIFFSSCHUTZ | **Entschieden: Rückkante.** Der Anhang trägt die Kennung seines Wissensobjekts und wird behandelt wie es — keine Stufen-Propagation, kein Kompromiss | Anhang und Objekt sind Geschwister. Jede andere Regel führt dazu, dass die Zeichnung frei liegt, während der Text gesperrt ist. |
| G3 | OFFEN | MIT-ZUGRIFFSSCHUTZ | **Entschieden: fail-closed.** Es gibt keinen Altbestand, wir arbeiten nur mit Beispieldaten. Der Bestandsfall entfällt | Ohne Altbestand können wir die strenge Regel sofort scharf stellen, statt eine Übergangsregel zu bauen, die dann bleibt. |
| G4 | OFFEN | MIT-ZUGRIFFSSCHUTZ | **Belegt und schlimmer als notiert: G2 und G4 stehen in derselben Datei.** `services/app/src/routes/object-routes.ts` enthält **null** Bezug auf Vertraulichkeit — der Anhang wird ausgeliefert, ohne dass die Stufe seines Wissensobjekts je gefragt wird; die entschiedene Rückkante aus G2 ist am Lesepfad also gar nicht gebaut. Und `:151` setzt `Cache-Control: private, max-age=31536000, immutable`. Ursprünglich: Anhang bleibt ein Jahr im Browser gültig — Loch, sobald gesperrt wird | Wer einmal Zugriff hatte, behält ihn faktisch ein Jahr. Bei einem ausgeschiedenen Mitarbeiter ist das genau der Fall, den der Zugriffsschutz verhindern soll. |
| G5 | OFFEN | MIT-ZUGRIFFSSCHUTZ | Drei Nebenwege: Benachrichtigungen, Duplikat-Eigenanteile, Konfliktzitate | Ein Zugriffsschutz, der nur den Hauptweg deckt, ist keiner. Diese drei geben Inhalt preis, ohne dass jemand das Objekt öffnet. |
| G6–G8 | OFFEN | MIT-ZUGRIFFSSCHUTZ | Export umgeht Vertraulichkeit (SCRUM-506) · systemisch auf allen Lesewegen (508) · Medien-Egress vertraut dem Browser (521) | |
| G9 | OFFEN | MIT-ZUGRIFFSSCHUTZ | SCRUM-533, 449, 508 und 556 zu **einem** Vorgang zusammenführen | Vier Tickets für eine Sache heißt, dass vier Leute drei Viertel davon für erledigt halten. |
| G10 | OFFEN | MIT-ZUGRIFFSSCHUTZ | Der Platzhalter in `provenance-routes.ts` darf nicht scharf gehen, bevor G1 steht. **Nachgesehen:** die Datei kommentiert sich selbst als „schließt keine bestehende Lücke" (`:19`) und führt die SCRUM-506-Regel bereits (`:78-97`) — sie ist vorbereitet, nicht scharf | Sie ist heute richtig gebaut und wird erst mit G1 wirksam. Wer sie vorher anfasst, macht sie zur zweiten Wahrheit neben Block A des Bauplans. |

---

## 6 — Wissensnetz

| Kennung | Zustand | Anker | Sache | Warum |
|---|---|---|---|---|
| H1 | BEAUFTRAGT | SOFORT | **Läuft als mega68 mit Fable — Pedi hat am 30.07. abends zwei Stunden freigegeben, ausdrücklich für eine Aufgabe mit echtem Entwurfsspielraum.** Gebaut wird **Variante A, die Nachbarschaft**. Der Auftrag ist bewusst offener geschnitten als alle bisherigen: er nennt **was** entstehen soll und **woran es scheitern würde**, nicht Zeile für Zeile das Wie. Die harten Grenzen: die Auskunft geht von **einem** Objekt aus und ist gedeckelt · ein Schlagwort, das fast alle tragen, erzeugt keine Kante (die Regel wählt die Hand selbst und **begründet sie gegen die Alternativen**) · **ein Nachbar, den jemand nicht lesen darf, existiert für ihn nicht** — weder Titel noch Kante noch die Tatsache, dass es ihn gibt · der Ort ist das Wissensobjekt, nicht Stufe 2 · an jeder Kante steht das gemeinsame Schlagwort · ein Klick geht weiter. Der Stufe-2-Graph bleibt unangetastet (H5). Ursprünglich: **Drei Schnitte liegen zur Wahl, gezeichnet mit echten Daten: `_relay/kopf/wissensnetz-entwurf.html`.** **A Nachbarschaft** — das Objekt in der Mitte, drumherum was wirklich zu ihm gehört, an jeder Kante steht warum, ein Klick macht den Nachbarn zur neuen Mitte (Kopf-Empfehlung: es sitzt dort, wo ohnehin gelesen wird). **B Themenlandkarte** — Schlagwörter als Knoten, Größe zeigt Umfang, Farbe zeigt Belastbarkeit; beantwortet „wo ist unser Wissen dünn". **C Leseweg** — dieselbe Auskunft in Sätzen, ohne Zeichnung, billigste Variante und die einzige, die auf dem Telefon ohne Abstriche trägt. **A und C sind derselbe Schnitt in zwei Kleidern, B ist etwas anderes.** Ursprünglich: **Die Anwendersicht des Wissensnetzes.** Pedis Anweisung: nicht im Admin verstecken. Serverseite steht seit mega45, die Sicht fehlt ganz. **Das ist die „LLM-Wiki-Grafik", die Pedi am 29.07. genannt hat** — Q3 damit beantwortet. Seine zwei Wörter dazu: *Grafik* und *Wiki*. Beim Schnitt zu klären, ob damit ein gezeichnetes Netz aus Knoten und Kanten gemeint ist oder eine durchblätterbare Wissenssicht — nicht raten, ihn fragen | Das Wissensnetz ist gebaut und für den Anwender unsichtbar. Wir haben die teure Hälfte fertig und die billige nicht. Genau deshalb ist es der beste erste Punkt nach dem Stopp: viel Wirkung, wenig Bau. |
| I28 | ERLEDIGT | — | **Die Kopfzeile des Wrappers nennt das falsche Modell — das Protokoll sagt die Wahrheit.** Am 30.07. um 18:13 hat Pedi `hand mega68 --model claude-fable-5` gestartet. Die Anzeige meldete `Modell: claude-opus-5 · effort high` und darunter `Zusatz: --model claude-fable-5`; Pedi hat daraus geschlossen, die Umschaltung sei fehlgeschlagen. **Sie ist geglückt:** das Init-Ereignis der ersten Protokollzeile sagt `"model":"claude-fable-5"` (`_relay/hand/logs/mega68-20260730-181350.jsonl`). Die Kopfzeile zeigt also die **Konfiguration des Wrappers**, nicht das **wirksame** Modell — und weicht genau dann ab, wenn jemand ein Flag durchreicht. **Damit ist das Verfahren aus I19 nicht nur nützlich, sondern notwendig:** wer der Anzeige glaubt, bricht einen laufenden Fable-Auftrag ab, der längst richtig läuft. Die Prüfzeile steht im Verfahren und wird ab jetzt bei jeder Ausnahme gefahren, nicht nur im Zweifel | Beinahe hätten wir zwei freigegebene Stunden weggeworfen, weil eine Statuszeile die Absicht des Werkzeugs anzeigt statt seines Ergebnisses. Es ist dieselbe Familie wie alles andere heute — ein Text, der mehr behauptet, als der Zustand hergibt —, nur diesmal in unserem eigenen Werkzeugkasten und mit dem Vorzeichen umgekehrt: hier hat die Anzeige **untertrieben**. |
| K7 | BEFUND | NACH-VORTEST | **Ein ausgeliefertes Taskpane bleibt bei jedem Client im Cache, und wir haben keinen Weg, ihn zu entwerten.** Beim Klara-Befund am 30.07. aufgefallen: `apps/web/public/word-addin/taskpane.html` wird als gewöhnliche HTML-Datei von der App-Domain geladen und vom Office-Webview zwischengespeichert. Ändert sich nur die Seite und **nicht** das Manifest, hat kein Client einen Anlass, sie neu zu holen — genau das ist heute mit Ship 9 passiert (mega61 E hat den KI-Satz eingefügt, mega62 D den Markenton geändert). **Der Versionssprung im Manifest hilft nicht:** er steuert den Cache des Manifests, nicht den Inhalt des Taskpanes. Es gibt weder eine Versionskennung an der URL noch einen Namenswechsel. **Bei uns kostet das eine Cache-Leerung; bei einem Kunden mit dreißig Arbeitsplätzen kostet es dreißig** — und niemand weiß vorher, dass er sie braucht. Nicht angefasst, keine Umsetzung beauftragt | Es ist dieselbe Klasse wie der Schalter ohne Leser und die Anzeige, die das falsche Modell nennt: etwas, das bei uns funktioniert, weil wir wissen, wo man nachhilft. Der Unterschied ist, dass diese Stelle **beim Kunden** auftritt und dort niemand die Anleitung kennt. Für eine Schnittstelle, die als kürzester Adoptionshebel gilt, ist das der falsche erste Eindruck. |
| I27 | OFFEN | NACH-VORTEST | **Ein Schalter ohne Leser: `confluenceImport`.** Von der Hand in mega67 als Nebenbefund gemeldet, vom Kopf nachgesehen und bestätigt — der Name wird von `/api/features` gemeldet und ist in `api/types.ts:1170` typisiert, aber **kein Frontend-Code liest ihn**. Entweder ein toter Rest oder eine Lücke, in der jemand eine Wirkung erwartet hat. Nicht angefasst, an ben zur Einordnung gegeben | Ein Schalter, den niemand liest, ist die stille Variante derselben Familie, die uns heute viermal beschäftigt hat: eine Zusage ohne Wirkung. Er kostet nichts, solange ihn niemand für wirksam hält — und genau das wird irgendwann jemand tun. |
| H5 | BEFUND | SOFORT | **Der heutige Graph skaliert nicht und sagt im Demobestand nichts — am Quelltext belegt.** `graph()` in `services/library-analytics/src/service.ts:1330-1348` lädt **alle** Wissensobjekte und vergleicht **jedes Paar**; eine Kante entsteht, sobald zwei Objekte **irgendein** Schlagwort teilen. Begrenzt wird erst **im Browser** (`limitGraph`, 60 Knoten, `pages/Stufe2.tsx:1426`) — also **nachdem** die vollständige Antwort gebaut und übertragen wurde. **Erste Folge:** alle Demo-Objekte tragen `pilot-demo`, der Graph ist im Demobestand damit **vollständig** — an den vierzehn Beispielobjekten des Entwurfs sind es 91 Kanten, davon **81 bedeutungslos**. Eine Kante heißt dort nur „beide sind Demodaten". **Zweite Folge, die ernstere:** das wächst quadratisch. Bei den zwölftausend Objekten des Jahresrings sind das rund **72 Millionen** Paarvergleiche und im schlimmsten Fall ebenso viele Kanten in **einer** Antwort. **Daraus folgt die Bauvorgabe, nicht der Geschmack:** die Anwendersicht geht von **einem** Objekt aus, die Route wird begrenzt, und ein Schlagwort, das fast alle tragen, darf keine Kante erzeugen | Wir hätten das beim Bauen gemerkt — vermutlich erst mit dem Jahresring, also vor dem Großkunden und mit zwölftausend Objekten im Rücken. Es ist genau der Grund, warum ein Entwurf vor dem Auftrag steht: eine Stunde Nachsehen hat hier eine Sackgasse erspart, die man erst am stehenden Server bemerkt hätte. |
| H3 | OFFEN | NACH-VORTEST | SCRUM-545–551: Beziehungen erheben · Lücken schließen · Graph-Lesemodell · kuratierte Sicht „So arbeitet Klarwerk" · Adminseite · Qualitätsblick | |
| H4 | OFFEN | MIT-JAHRESRING | Modellläufe werden nicht protokolliert (SCRUM-554) | Vor dem ersten großen Erzeugungslauf zu klären, sonst laufen zwölftausend Modellaufrufe ohne jede Spur, was sie gekostet und was sie erzeugt haben. |
| H2 | VERWORFEN | — | Das „Zeitprojekt"-Verfahren mit fallenlassbarem Block Z | Es hat siebzehn Aufträge lang genau das bewirkt, was es verhindern sollte: der fallenlassbare Block wurde immer fallengelassen. |

---

## 7 — Jahresring und Testbestand

| Kennung | Zustand | Anker | Sache | Warum |
|---|---|---|---|---|
| L1 | OFFEN | MIT-JAHRESRING | **Entschieden: entfällt als Nachforderung — wir fangen von vorne an.** Die 120 undokumentierten Anhänge werden neu erzeugt. **Auflage für den Erzeugungsprompt (D4): je Los eine Begleittabelle, kein Los ohne sie** | Ohne Begleittabelle wissen wir bei jedem Los wieder nicht, was drin ist — genau der Zustand, der zur Nachforderung geführt hat. |
| L2 | OFFEN | MIT-JAHRESRING | **Entschieden: neu gestalten, in geringerer Anzahl.** Die 24.000 Kennungen gegen 12.480 Objekte waren ein Fehler; Mehrfachverwendung kommt zurück | Doppelt so viele Kennungen wie Objekte bildet keinen Betrieb ab. In der Wirklichkeit taucht dieselbe Anlage in vielen Dokumenten auf — das ist der ganze Sinn eines Wissensnetzes. |
| L3 | OFFEN | MIT-JAHRESRING | **Entschieden: weniger, dafür realistischer.** Keine Vektorzeichnungen als „Fotos", keine eingebrannte Kennung im Bild — **muss ausdrücklich im Erzeugungsprompt stehen** | Steht es nicht im Prompt, kommt es wieder so heraus. Der letzte Lauf ist genau daran gescheitert. |
| L4 | OFFEN | MIT-JAHRESRING | Aufgabe A nie geliefert: Anhangsgerüst neu herleiten, Datenbankgröße neu rechnen, Losgröße begründen | Ohne diese Rechnung erzeugen wir wieder auf gut Glück und merken die Größe erst, wenn die Platte voll ist. |
| L5 | OFFEN | MIT-JAHRESRING | Löschanteil aus dem Firmenalltag herleiten — die 72 % Papierkorb sind Entwicklungsschutt | Ein Testbestand, dessen Löschanteil aus unseren eigenen Fehlversuchen stammt, misst uns, nicht den Kunden. |
| L6 | OFFEN | MIT-JAHRESRING | Vier Datenbankbefunde: kein Fremdschlüssel auf der Objekttabelle, Lücken ohne Objektbezug, kein Waisen-Sweep, Prüfspur auf gelöschte Objekte | Fehlende Fremdschlüssel heißt, dass die Datenbank Widersprüche zulässt, die niemand bemerkt, bis eine Auswertung falsch rechnet. |
| L7 | ERLEDIGT | — | Ladeweg entschieden: Bulk-Import ohne Modellaufruf, Erzeugung in Losen mit Halt | |
| E3 | OFFEN | SOFORT | **Entschieden: Swap wird eingerichtet**, auf beiden Maschinen | Ohne Swap bricht der große Erzeugungslauf mitten drin ab, und wir wissen hinterher nicht, welche Lose vollständig sind. |
| E5 | OFFEN | MIT-JAHRESRING | **Entschieden: entfällt — wir starten neu.** Rolle und Schema entstehen mit dem neuen Aufsetzen | |
| E4s | OFFEN | MIT-JAHRESRING | Zweiter Server aufsetzen (entschieden: eigene Maschine) — nach Freitag | Der Erzeugungslauf und der laufende Betrieb dürfen sich nicht dieselbe Maschine teilen, sonst legt ein Lauf die Vorführung lahm. |
| E7 | OFFEN | SOFORT | Produktionsdatenbank umbenennen — Pedi gibt frei, Kopf führt aus | Der heutige Name lädt dazu ein, die Produktionsdatenbank für eine Testdatenbank zu halten. Das geht genau einmal schief. |
| E8 | OFFEN | MIT-JAHRESRING | RESERVE und leeren Jahresring-Container löschen, erst nach dem Nachweis | |

---

## 8 — Prozess: was das Vergessen verursacht hat

| Kennung | Zustand | Anker | Sache | Warum |
|---|---|---|---|---|
| I35 | ERLEDIGT | — | **Fable-Fenster vom 30.07.: fünf Läufe, alle im Protokoll als `claude-fable-5` bestätigt** (mega68 18:13, mega69 19:03, mega70 19:53, mega71 20:31, mega72 21:02) — die Prüfung aus I19/I28 wurde jedes Mal gefahren. **Was es gebracht hat, in einem Satz: es hat viermal gemessen, statt dem Kopf zu glauben.** mega69 widerlegte die Kopf-Annahme über den Bildanker an der echten Produktfunktion und fand nebenbei, dass `@fastify/static` die Kopfzeilen-Rückrufe app-weit überschreibt; mega70 fand über die Kalibrierung seines eigenen Sammlers eine fünfte Sackgasse, die niemand gesucht hatte; mega71 vermaß das Doppel-Send-Fenster am echten Socket und stufte bens Blocker belegt als latent statt aktiv ein. **Ende des Fensters: nach mega72.** Ab dem nächsten Auftrag wieder Standard — das Flag entfällt einfach, der Wrapper setzt `claude-opus-5` von sich aus. **Benannte Ausnahme für später: G1 Variante A**, wenn Pedi sie freigibt; sie ist die nächste Aufgabe mit echtem Entwurfsspielraum und einem Wächter, der beim ersten Mal stimmen muss | Der kritische Pfad bis morgen 14:00 läuft nicht mehr durch die Hand, sondern durch bens Verdikt und durch Pedis eigene drei Handgriffe. Ein besseres Modell kann dort nichts mehr abnehmen. |
| I34 | ERLEDIGT | — | **Der Takt hat sich geändert, und Pedi hat ihn festgeschrieben (30.07. abends): wenn die Hand fertig ist und ben prüft, bekommt die Hand einen kleinen Auftrag statt zu warten.** Bisher stand in KOMMUNIKATIONSWEGE §2 „während ben prüft, ist Codes Eingang leer“. Das war zu streng: ben liest immer eine **eingefrorene** Datei, die Hand baut im bewegten Arbeitsbaum — sie sehen einander nie. **Die eine Bedingung ist mechanisch:** der kleine Auftrag darf keine Datei anfassen, die im eingefrorenen Ship-Diff steht, sonst wächst eine Produktdatei ungeprüft in die Auslieferung hinein. Der Kopf erzeugt dafür vor jeder Übergabe `_relay/kopf/ship-sperrliste.txt`; der sicherste Zuschnitt ist reine Testarbeit ohne eine Zeile Produktcode. Beide Diffs bleiben getrennt. In §2 verankert | An diesem Abend hat die Parallelität viermal getragen und dabei vier Rollen-Sackgassen, einen Absturzweg und die Lücke im Wächter dagegen sichtbar gemacht. Wer wartet, verliert die Hälfte der Nacht — und die Hälfte der Funde. |
| I33 | OFFEN | SOFORT | **Die Zeilen dieses Registers werden beim Fund geschrieben und danach nie wieder geprüft — zweimal in einer Nacht aufgefallen.** Bei **G1** stand „heute sieht jeder angemeldete Nutzer jedes Objekt“; in Wahrheit setzen alle Egress-Wege seit SCRUM-506 eine Regel durch und nur der Hauptlesepfad nicht. Bei **D1** stand „heute ist sie eine Liste, kein Werkzeug“; der Filter war schon am 24.07. abends fertig, mit allen drei Merkmalen des angenommenen Vorschlags. **Beide Zeilen hätten einen Bauauftrag für bereits Gebautes ausgelöst.** Die Ursache ist die Bauart: die fünfte Spalte begründet, warum ein Punkt wichtig ist, und niemand kommt zurück, wenn sich der Zustand ändert. **Vorschlag, nach dem Vortest zu entscheiden:** eine Zeile, die älter ist als drei Ship-Runden und nicht angefasst wurde, wird vom Prozess-Wächter (P1) gelb — nicht weil sie falsch ist, sondern weil niemand mehr weiß, ob sie stimmt | Ein Register, das nur wächst und nie nachgesehen wird, wird mit der Zeit ein Archiv von Momentaufnahmen. Es sieht aus wie ein Zustand und ist eine Geschichte. |
| I31 | OFFEN | SOFORT | **ben wird nicht ausgelassen (Pedi, 30.07.).** Er läuft **parallel** zur Hand — andere Fläche, eingefrorener Commit, `git show`/`git grep` statt Arbeitsbaum. Das kostet keinen Takt und keine Wartezeit, es kostet nur, dass der Kopf daran denkt. In KOMMUNIKATIONSWEGE §4 verankert. Erste Anwendung: sammel66 gegen `d0b9e5e`, parallel zu mega69 | Er hat in dieser Woche jeden ernsten Fehler gefunden, den der Kopf selbst übersehen hat — die falsche Zahl der Aufrufer, den unbedingten Kostenhinweis, den vierten Zustand, den es nicht gibt. Wer ihn im Gedränge weglässt, verliert genau in dem Moment die Gegenprobe, in dem sie am nötigsten ist. |
| I29 | ERLEDIGT | — | **Die Zentrale war eigenmächtig außer Dienst gestellt — gegen eine Regel, die Pedi selbst festgeschrieben hat.** SCRUM-530 §5 legt EINE Seite dauerhaft rechts fest, Artefakt `klarwerk-board` plus `_relay/board/klarwerk-board.html`, mit einer **Sektionsreihenfolge, die kein Kopf eigenmächtig ändert** (Stand · Termin · jetzt dran · bei dir offen · Entscheidungen · Arbeitsstände · Geplant · Entschieden · Verlauf) und einer Erklärpflicht: jeder Punkt trägt ein Aufklappfeld „Was heißt das?“. Am 29.07. hat ein Kopf sie durch die maschinell erzeugte Registerseite ersetzt und das Artefakt auf „AUSSER DIENST“ gesetzt. Pedi hat den Verlust am 30.07. benannt: „Rechts hast du auch wieder nicht meinen Überblick geschaffen.“ **Am 30.07. abends wieder in Dienst gestellt**, Reihenfolge und Erklärfelder wie festgeschrieben | Die Registerseite zeigt alle Zeilen und trotzdem keinen Überblick — sie ist für die Vollständigkeit gebaut, nicht für die Entscheidung. Eine Regel, die Pedi selbst gesetzt hat, wird nicht von einem Kopf kassiert, weil ihm etwas anderes besser gefiel. |
| I30 | ERLEDIGT | — | **Der Text, den Pedi in ein neues Fenster kopiert, war veraltet — und er musste beim Kopieren daran denken.** `UEBERGABE-PROMPT.txt` und `UEBERGABE.md` trugen den Stand vom 29.07.: „der nächste Schritt heißt S6“ (längst geschlossen), „mega54 bis mega57 liegen unkommittiert“ (drei Ships her). **Die Ursache ist die Bauart, nicht die Nachlässigkeit:** beide Dateien trugen Zustand, und Zustand altert. **Beide sind am 30.07. abends zustandsfrei neu geschrieben** — kein Commit, keine Scheibe, kein Termin, nur Rolle, Verfahren und Fallen. Alles, was altern kann, steht ab jetzt ausschließlich hier | Zum zweiten Mal derselbe Fehler: schon am 29.07. wurde `STATUS.md` aus der Übergabe genommen, weil sie vier Ships hinterherhing. Der Ersatz erbte die Bauart und damit den Fehler. |
| W4 | OFFEN | SOFORT | **Klara ist eine Grundsatzaufgabe von Anfang an und bekommt keinen eigenen Termin (Pedi, 30.07.).** Sie stand in jeder Runde hinter etwas zurück, das dringender aussah, und wurde am 30.07. zweimal verändert und ausgeliefert (mega61 E, mega62 D), **ohne dass in einem der beiden Aufträge ein Wort dazu stand, was das für ein installiertes Add-in bedeutet.** Gezählt, weil Pedi es verlangt hat: **null** Erinnerungen des Kopfes, dass Klara nicht geht · **null** Warnungen vor diesen beiden Änderungen · **null** Arbeit am Eingabefeld für die Bildbeschreibung an diesem Tag. In `AUFTRAG-mega62.md:53` hat der Kopf der Schnittstelle sogar selbst eine Ausstiegsklausel mitgegeben. **Ab jetzt Regel:** ein Auftrag, der eine von Pedi als wichtig benannte Fläche anfasst, sagt, was die Änderung für diese Fläche bedeutet | Es ist der kürzeste Hebel, den das Produkt für die Adoption hat, und der einzige Weg, auf dem Wissen ohne Öffnen der Anwendung hereinkommt. |
| I1 | OFFEN | SOFORT | **Diese Datei.** Ersetzt die unerfüllbare Gedächtnis-Regel aus SCRUM-532 (Gedächtnis ist für das Konto abgeschaltet) | Die alte Regel verlangte ein Gedächtnis, das es technisch nicht gibt. Eine Regel, die niemand erfüllen kann, wird nicht befolgt, sondern ignoriert — und mit ihr alles andere. |
| P1 | OFFEN | SOFORT | **Der Prozess-Wächter im Prüftor** — siehe unten | Ohne ihn hängt dieser ganze Abschnitt an Disziplin, und Disziplin überlebt keinen Kopfwechsel. Genau daran ist es schon einmal gescheitert. |
| I8 | OFFEN | SOFORT | Scheiben-Zusatz an ben-Auftragsnamen wieder anhängen **und in der Regel verankern** | Ohne den Zusatz weiß niemand mehr, welche Prüfung zu welcher Scheibe gehörte — bei fünfundzwanzig Verdikten ist das der Unterschied zwischen Archiv und Haufen. |
| I6 | OFFEN | SOFORT | Täglicher Kassensturz — ab jetzt gefahren, nicht mehr nur beschrieben | Er hätte die vier unbeantworteten Vorschläge am zweiten Tag sichtbar gemacht statt am neunten. |
| I2 | OFFEN | NACH-VORTEST | KOMMUNIKATIONSWEGE und SCRUM-530 zu **einem** Dokument zusammenführen, Widerspruch beim Startbefehl auflösen | Zwei Übergabedokumente heißt: ein neuer Kopf liest das falsche. |
| I12 | OFFEN | NACH-VORTEST | Inventur aller Verhaltensbehauptungen; sechste Regel für SCRUM-553: jede Behauptung braucht eine Deckung | Der hohle Rauchproben-Fall war kein Ausrutscher, sondern die Bauart. Ungedeckte Zusagen sind teurer als fehlende. |
| I3 | ERLEDIGT | — | SCRUM-531 ist kein Statusbericht mehr, sondern ein Wegweiser auf `OFFEN.md` und `KOMMUNIKATIONSWEGE.md` | Es war als lebende Übergabe-Wahrheit gedacht und genau daran gescheitert: es hing fünf Ships hinterher, weil niemand es nachzog. Ein Zeiger veraltet nicht. |
| I15 | ERLEDIGT | — | `_relay/kopf/KOMMUNIKATIONSWEGE.md` §2 trägt seit 29.07. abends `1fc55aa` samt Ship-Kette und dem Satz „wer die Basis ändert, ändert diese Zeile" — vom Kopf am 29.07. in der Datei nachgelesen | Das Dokument, das die Quelle der Wahrheit sein soll, war ausgerechnet an der Stelle veraltet, an der es vor veralteten Aufträgen warnt. |
| I17 | OFFEN | SOFORT | **Die Dateiübertragung liefert Altfassungen — jede gestagte Datei gegen die gemeldete Bytezahl prüfen.** Am 29.07. beim Nachrechnen von mega58 real passiert: der Endzustand von `provider.ts` kam mit 64.256 Bytes und altem Code herüber, während die Maschine 65.655 Bytes und den neuen trug. Der Selbsttest kostet nichts: die Übertragung meldet die Bytezahl des Geräts mit, ein Vergleich mit der angekommenen Datei entlarvt den Fall sofort. Sicherer Weg für Zustandsfragen: direkt auf der Maschine lesen | Es ist dieselbe Ursache, die schon vier falsche „erledigt" erzeugt hat — diesmal hätte sie beinahe einen korrekt gebauten Fix als nicht gebaut gemeldet. Ein Kopf, der eine Altfassung liest, widerspricht der Hand zu Unrecht und kostet eine ganze Runde. |
| I24 | ERLEDIGT | — | **Das Repository hat ZWEI Remotes, und der Ship-Push geht an `github` — nicht an `origin`.** Am 30.07. um 17:2x abgeprallt, weil die Ship-Sequenz des Kopfes nur `git push` nannte. `origin` zeigt auf ein **lokales Gitea** (`http://localhost:3000/klarwerk/klarwerk.git`) und trägt eine **fremde, parallele Linie**: `78082c6` „feat(import): ehrliche Quellen-/Datei-Galerie", davor `c4a6a5b` „Ship 10", `6a28ffc` „Ship 9 Slice 3", `985b086` „Ship 9 Slice 2", AI-STATE und FUNKE-Fundament — eine andere Ship-Zählung und andere Arbeit. `github` (`git@github.com:Klarwerkai/klarwerk.git`) trägt **unsere** Kette; `github/main` stand vor Ship 9 exakt auf `bcf1f15`. **Belegt am Reflog, nicht vermutet:** `.git/logs/refs/remotes/github/main` zeigt `84060d2 → 1fc55aa → 46aac36 → bcf1f15`, jeweils „update by push". Der lokale Zweig `main` folgt jedoch `origin` (`branch.main.remote = origin`) — ein nacktes `git push` geht also an das Gitea. **Nichts wurde beschädigt:** der Push wurde abgelehnt, das Gitea ist unverändert, `d0b9e5e` liegt lokal. **Der Ship-Befehl heißt ab jetzt `git push github main`** und steht so im Verfahren. **Kopf-Fehler:** ein `cat .git/config` vor der Sequenz hätte gereicht; er kam erst nach dem Fehlschlag | Die Übergabe und das Verfahren haben den Remote nie genannt — sie sprachen nur von „dem Push, der Coolify auslöst". Vier Ships lang ging das gut, weil Pedi den Befehl aus dem Gedächtnis tippte. Beim fünften schrieb der Kopf ihn auf, und da fiel auf, dass niemand ihn je aufgeschrieben hatte. **Eine mündliche Gewohnheit ist kein Verfahren.** |
| I23 | ERLEDIGT | — | **Der Kopf hat gegen seine eigene Regel `git` im Arbeitsbaum ausgeführt — und genau der angekündigte Schaden trat ein.** Die Übergabe warnt seit jeher: „Nie `git` im Arbeitsbaum ausführen, auch nicht lesend. Ein `.git/index.lock` blockiert Pedis `git add`." Am 30.07. hat der Kopf mehrfach `git rev-parse`, `git log` und einmal `git status --porcelain` über die Brücke laufen lassen. Die ersten beiden sind harmlos, `git status` **schreibt den Index** — es entstand `.git/index.lock`, und die Brücke konnte ihn nicht wieder entfernen („Operation not permitted"), weil sie grundsätzlich nicht löschen kann. **Gefunden, weil die Warnzeile in der Ausgabe stand und nicht überlesen wurde.** Behoben über den einzigen erlaubten Weg: `mv .git/index.lock _relay/_to_delete/index.lock.0`. **Ohne diesen Griff wäre Ship 9 an Pedis erstem `git add` gescheitert**, und zwar mit einer Meldung, die nach einem Repository-Schaden aussieht. **Regel geschärft:** der Kopf rechnet Hashes mit `shasum`, liest Dateien mit `cat`/`sed`/`grep` und fragt Zustände bei Pedi — `git` gar nicht, auch nicht die scheinbar lesenden Formen, weil die Grenze zwischen lesend und indexschreibend nicht am Namen des Unterbefehls abzulesen ist | Die Regel stand seit dem ersten Tag in der Übergabe, mit Begründung, und wurde trotzdem gebrochen — nicht aus Eile, sondern weil `git rev-parse HEAD` harmlos aussieht und `git status` sich danach wie dasselbe anfühlt. Genau deshalb steht die neue Fassung ohne Ausnahme da: eine Regel mit einer Ermessensgrenze ist eine Regel, die im Stress fällt. |
| I22 | ERLEDIGT | — | **Dreimal am 30.07. dieselbe Fehlerklasse, und der dritte Fall war der Kopf selbst: ein Kommentar behauptet mehr, als der Code trägt.** Erstens der Themenkommentar, der einen Farbwert festschrieb, den das Thema längst erbte — die stille Mitwanderung, die er bestritt, **war** der Kontrastbefund (mega63). Zweitens der Kommentar, der richtig erklärte, dass ein Dump in jedes CI-Protokoll läuft, und die Kennwörter danach über **denselben** Kanal ausgab (mega64 ROT-1). Drittens mein eigener Satz „genau einen Aufrufer", von mir in einen Bauauftrag geschrieben, von der Hand übernommen, von ben widerlegt — **eine Zeile `grep` hätte gereicht.** **Regel, ab jetzt im Verfahren:** eine Zahl, ein Zählwort oder ein „nur/genau/einzig" in einem Auftrag oder Kommentar ist eine Zusage und wird vor dem Schreiben belegt, nicht danach geprüft. Wer keinen Beleg hat, schreibt „nach meiner Lesart" — das kostet vier Wörter und ist der Unterschied zwischen einer Vermutung und einer Falschaussage | Alle drei Fälle waren richtig gemeint und alle drei falsch, und keiner wäre durch mehr Sorgfalt beim Bauen verhindert worden — nur durch einen Beleg beim Behaupten. Der teuerste war der eigene: er ist über einen Bauauftrag in den Quelltext gewandert und stand dort als Begründung für eine Entscheidung, die aus einem anderen Grund richtig ist. |
| I19 | ERLEDIGT | — | **Die Modellfrage von 28.07. ist geklärt und im Verfahren.** `hand --help` sagt: unbekannte Flags gehen an `claude` weiter, aber der Wrapper setzt `--model` selbst — bei `hand <name> --model …` bekommt `claude` **zwei** Modell-Flags, und welches gewinnt, sagt die Hilfe nicht. **Es muss aber niemand raten:** der Wrapper schreibt das verwendete Modell ins Protokoll, `"model":"…"` im Init-Ereignis der ersten Zeile von `_relay/hand/logs/<name>-<zeitstempel>.jsonl`. Verfahren steht in KOMMUNIKATIONSWEGE §3: Pedi startet mit Flag, Kopf liest sofort das Protokoll, bei falschem Modell Abbruch. **Nie wieder die stumme Langform ohne diesen Schritt** (Vorfall mega40) | Zwei Tage lang hat niemand die Frage beantwortet, und beim ersten Fable-Versuch am 28.07. hat der Kopf deshalb die Langform gegeben — Pedi sah nichts und wusste nicht, ob Code arbeitet. Die Antwort lag die ganze Zeit im Protokoll, das der Wrapper selbst schreibt. |
| I18 | ERLEDIGT | — | **Zwei Kopf-Fehler derselben Klasse an einem Tag, beide von Code gefangen, beide als Regel abgelegt.** Erstens: bens Zahlen (`stan`/`widerstan`, `Gegenstand`) ungeprüft in einen Bauauftrag übernommen. Zweitens: aus einem kleingeschriebenen `grep "confidential"` geschlossen, die Ask-Route filtere nicht — die Barriere heißt `dropConfidential` und war für die Suche unsichtbar. **Neue Regel in KOMMUNIKATIONSWEGE §5: ein Befund aus einem Nulltreffer braucht eine Kalibrierung, genau wie der NUL-Prüfer** — vorher an einem Positivfall zeigen, dass die Suche anschlagen kann | Die Projektregel „ein Prüfer ohne Kalibrierung ist keine Prüfung" gab es längst — aber nur für Dateien. Auf Suchen angewandt hat sie niemand, und genau dort ist sie zweimal gebrochen worden. Ohne diese Zeile passiert es dem nächsten Kopf wieder. |
| P2 | ENTSCHEIDUNG | SOFORT | **Der Rückfallplan für morgen — und er entspannt den Abend: der Vortest braucht Ship 9 nicht.** Nataschas sieben Aufgaben messen das Produkt, nicht die Rechtsflächen: der Bestand ist auf Ship 8 (`bcf1f15`) live geprüft (A2, E9, G3b), Ship 8 ist grün und stabil, und die sieben Aufgaben liefen auf diesem Stand. Rechtsseiten, Hinweisbanner und die Unterrichtung nach Artikel 4 sind für den Vortest **Zugabe**, nicht Voraussetzung — sie werden vor dem Publikumsgang gebraucht, nicht vor einer internen Generalprobe. **Kopf-Empfehlung: kein Ship gegen die Uhr.** Konvergieren mega65 und die Gegenprüfung bis heute Abend, fährt Ship 9; wenn nicht, läuft der Vortest auf Ship 8 und Ship 9 fährt danach. **Was in KEINEM Fall passiert:** eine Auslieferung mit einer bekannten falschen Tatsachenaussage in einer Rechtsfläche, oder ein Ship, das nach 22 Uhr noch eine Coolify-Runde und eine Live-Prüfung braucht | Der feste Termin ist der Vortest, nicht das Ship — und die beiden zu verwechseln ist der teuerste Fehler, der an so einem Abend möglich ist. Ein Ship, das um Mitternacht durchgedrückt wird, riskiert genau die Fläche, auf der morgen um vierzehn Uhr eine Testerin ohne Vorwissen sitzt. Ship 8 hat drei Gegenprüfungen und einen Live-Beleg hinter sich; das ist der bessere Untergrund für eine Messung, deren Ergebnis wir glauben wollen. |
| I20 | OFFEN | NACH-WIEDERVERBINDEN | **Der Atlassian-Zugang wird für ein anderes Projekt abgeklemmt — die Konfiguration ist gesichert, Zugangsdaten gibt es keine.** `_relay/kopf/ATLASSIAN-Wiederverbinden.md`, aufgenommen am 30.07. **solange der Zugang noch stand**: Seite, Cloud-ID `a43f55b1-…-e9bd5c697140`, Account, die **acht** Jira-Projekte mit Schlüssel und ID (`SCRUM` ist das Arbeitsprojekt), die zwei Confluence-Bereiche und **die zwei erteilten Rechtebündel** — beim erneuten Autorisieren müssen beide wieder erteilt werden, denn fehlt `write:jira-work`, kann der Kopf lesen aber nicht pflegen, und das fällt erst auf, wenn ein Vorgang stillschweigend nicht geschrieben wird. **Zugangsdaten sind nicht sicherbar und sollen es nicht sein:** die Verbindung läuft über OAuth in der App, es gehen weder Kennwort noch Token durch den Kopf, und ein Schlüssel in einer Markdown-Datei im Repository wäre ein Befund, kein Komfort. **Nachzutragen nach dem Wiederverbinden:** Ship 9 mit mega61, mega62 und mega63 im Bauplan (SCRUM-558), die Ship-Kette und die drei Auflagen aus ben sammel60, die neuen Registerpunkte T6 und I20, sowie C1 als zweimal zurückgestellt | `OFFEN.md` ist die Wahrheitsquelle und liegt im Repository — Jira ist die Zweitschrift. Deshalb geht nichts verloren, es sammelt sich nur an, und der Freitagstermin hängt nicht daran: der Vortest hängt am Produkt, am Bestand und an den Rechtsflächen, nicht an einem Vorgangssystem. Aufgeschrieben wurde es trotzdem sofort, weil eine Konfiguration, die man erst nach dem Abklemmen braucht, dann nicht mehr abfragbar ist. |
| I4 | OFFEN | NACH-VORTEST | SCRUM-530: Code-Freeze und VIP-2 als KO streichen, Prozess-Freeze streichen | Regeln, die sich auf einen abgesagten Termin beziehen, blockieren Arbeit ohne Nutzen. |
| I5 | ERLEDIGT | — | Die Verdikte sammel45–54 und die Ship-Kette dieses Tages sind in SCRUM-469 verankert, mit Inhalts-SHA je Runde und der Fehlerklasse, die sich durch fünf Runden zieht | `_relay/` steht in `.gitignore` und reist mit keinem Klon mit — genau der Mechanismus, der schon einmal das ganze Projektgedächtnis gekostet hat. Die älteren Runden sammel26–44 stehen weiterhin aus. |
| I7 | OFFEN | SOFORT | Projekt-Instruktionen laden nicht; Selbstprüfung zu Sitzungsbeginn wird Pflicht | Ein Kopf, der ohne seine Instruktionen startet und es nicht merkt, arbeitet stundenlang nach Regeln, die er sich selbst ausdenkt. |
| I9 | OFFEN | SOFORT | Stichprobengröße nennen, wenn über eine Menge geurteilt wird | „Ich habe die Tests geprüft" bei drei von vierzig ist eine Falschaussage, auch wenn sie nicht so gemeint war. |
| I10 | OFFEN | SOFORT | Keine interaktiven Datenbank-Transaktionen mehr | Eine offene Transaktion blockiert die Datenbank für alle anderen, bis jemand sie bemerkt. |
| I11 | OFFEN | NACH-VORTEST | Coolify-Eigenheiten selbst nachprüfen oder als unbelegt kennzeichnen | |
| I13 | OFFEN | NACH-VORTEST | Ship-Tor braucht ein Cloud-Geheimnis (SCRUM-552) — die Frist „bis nach VIP-2" ist verfallen | |
| I14 | OFFEN | SOFORT | **Kein Kopf-Befehl ohne `cd`.** Am 29.07. lief eine Ship-Sequenz im Home-Verzeichnis ins Leere, weil der Kopf das Arbeitsverzeichnis nicht mitgegeben hat | Es ist glimpflich ausgegangen, weil `git` sich verweigert hat. Bei einem Befehl, der auch außerhalb des Repos etwas tut, wäre es das nicht. |
| K3 | ERLEDIGT | — | **Das Artefakt „Klarwerk Board" ist außer Dienst**, von Pedi am 29.07. freigegeben. Es gibt genau eine Lesefläche: „Was offen ist", erzeugt aus dieser Datei. Die Aufklapp-Erklärung je Punkt ist als fünfte Spalte „Warum" nachgerüstet | Zwei Leseflächen heißt zwei Wahrheiten, und die zweite veraltet immer. Genau daran sind die dreimal wiederholten „das ist doch erledigt"-Rückfragen entstanden. |
| J | ERLEDIGT | — | **Dateileichen weg**, am 29.07. nachgeprüft: alle vier `_to_delete`- und `_trash`-Ordner gelöscht, null Dubletten, null Board-Sicherungen. Der neue Rest steht als I16 | |
| K6 | ERLEDIGT | — | **`_relay/kopf/INDEX.md`** sagt je Datei, ob sie gilt oder überholt ist und wodurch sie ersetzt wurde. Sechs konkurrierende Übergabe- und Statusdokumente sind damit eingeordnet, keines gelöscht | Genau dagegen ist die fünfte Regel des Prozess-Wächters geschrieben. Am 28.07. lagen sechs sorgfältig geschriebene Dokumente nebeneinander, keines vollständig aktuell — ein neuer Kopf las das falsche. |
| K2 | ERLEDIGT | — | `_relay/kopf/wartet/` aufgelöst: der Import-Auftrag liegt als Entwurf in `_relay/hand/queue/` | |
| E1v | ERLEDIGT | — | VIP-2 entfällt endgültig; der Vermerk „nach VIP-2" wird überall durch einen Ereignis-Anker ersetzt | |

---

## Der Bauplan (SCRUM-558, Verfahren in SCRUM-530 §9)

Sieben Themen für den offenen Bestand: **S1** wie Wissen hineinkommt · **S2** was herauskommt und ob man ihm trauen kann · **S3** wer was sehen darf · **S4** Klara und die Nähe zum Dokument · **S5** der Großkunde am 12.08. · **S6** das Wissensnetz · **S7** das Fundament aus Tor, Härtung, Betrieb und Sprache.

**Er gruppiert, er terminiert nicht.** Ein Thema sagt, was zusammengehört — nicht, wann es drankommt. Die Reihenfolge entscheidet Pedi bei jedem Auftrag neu.

**Zwei Bahnen.** Die Schnellbahn nimmt, was einen Ship oder Termin blockiert: eng, sofort, ohne Umplanung. Die Themenbahn alles Übrige. Einen dritten Weg gibt es nicht, damit nichts „irgendwo" landet.

**Neu geschnitten wird auf drei Auslöser, nicht nach Kalender:** ein Thema ist fertig · drei Vorgänge in Folge passen in kein Thema · ein Termin fällt weg oder kommt hinzu.

## Der Prozess-Wächter (P1)

Alles in diesem Projekt, was halten soll, hängt an einem Sammler im Prüftor. Der Prozess bisher nicht — er hing an Disziplin, und Disziplin überlebt keinen Kopfwechsel. Der Wächter prüft, was eine Maschine prüfen kann:

**Erstens: keine Datums-Anker.** Ein Anker, der ein Kalenderwort trägt („nach VIP-2", „Anfang August", ein Datum), macht das Tor rot. Das ist die Regel, die fünf Vorgänge und zwei Konzepte gerettet hätte.

**Zweitens: kein zweiter Ablageort.** Eine auftragsförmige Datei irgendwo unter `_relay/kopf/` außerhalb von `queue/` macht das Tor rot. Genau so sind zwei fertige Aufträge verschwunden.

**Drittens: keine Waisen.** Jeder ben-Bericht in `outbox/` und jeder Hand-Bericht braucht eine Zeile hier. Wer prüft, ohne dass es hier ankommt, prüft ins Leere.

**Viertens: nichts altert stumm.** Eine `ENTSCHEIDUNG`-Zeile, die älter ist als drei Ship-Runden, macht das Tor gelb und steht namentlich im Kassensturz.

**Fünftens: eine Übergabe, nicht drei.** Mehr als ein Übergabedokument ohne Überholt-Vermerk macht das Tor rot.

**Sechstens: keine Erklärungslücke bei den ältesten Punkten.** Ein offener Punkt ohne fünfte Spalte, der älter ist als zwei Ship-Runden, macht das Tor gelb.

## Der Kassensturz

Einmal je Arbeitstag, ungefragt, drei Zeilen: was sich bewegt hat · was auf Pedi wartet, **mit der ältesten zuerst** · was am längsten liegt.

## Der Anlauf beim Kopfwechsel

Bevor irgendetwas anderes passiert: diese Datei lesen, den Prozess-Wächter fahren, und die drei ältesten offenen Punkte sowie alle unbeantworteten Entscheidungen melden. Laden die Projekt-Instruktionen nicht, ist das ein Befund für Pedi, kein Achselzucken.

## Die Bündelgröße

Ein Auftrag wird nach **Thema** geschnitten, nicht nach Größe: Blöcke gehören zusammen, wenn sie dieselbe Fläche oder denselben Vertrag berühren. Sechs bis zehn Blöcke sind normal. Die Ausnahme sind Ship-Blocker — die bleiben klein und schnell, wie mega48. Alles andere bündelt, weil jede Runde einen vollen Kopf-Code-ben-Takt kostet und ein Rückstau schneller wächst, als kleine Scheiben ihn abtragen.
