node_modules/
dist/
.env
.env.local
docs/generated/coverage/
*.log
.DS_Store

# Vite/Vitest erzeugen beim Transpilieren der TS-Configs flüchtige Temp-Dateien.
*.timestamp-*.mjs
desktop-app/app.log
vitest.config.ts.timestamp-*
desktop-app/.run-state

# SCRUM-387: lokale Dev-Persistenz der Desktop-App (Journal mit Nutzer-/Session-Daten) — nie versionieren.
.localdb/
.local/run/
.local/logs/

# Playwright-Smoke (SCRUM-381)
test-results/
playwright-report/
.fuse_hidden*

_relay/

# AUFTRAG-mega50 D2: der Jahresring-Testbestand (LOT-Pakete, Begleittabellen, Übersichten) — 343 MB
# binäre .zip/.xlsx aus Testläufen, kein Quellstand. Er lag untracked und in KEINER Ignore-Zeile;
# ein einziges `git add -A` hätte ihn vollständig ins Repository gezogen.
LOT/

# JOB 3080: Arbeitsordner von Agenten-Werkzeugen (Claude Code, Codex) in einem Bahn-Klon — Sitzungs-
# zustand eines Menschen oder Agenten, nie Produktquelle. Ein `git add -A` beim Ship hätte einen
# solchen Rest sonst versehentlich versioniert (derselbe Fehler wie LOT/ oben, anderer Ordner).
.claude/
.codex/
